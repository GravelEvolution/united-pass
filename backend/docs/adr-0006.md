# ADR-0006: Account Security — Sessions, Factors and Step-up Reauthentication (Phase 4)

## Status

**Accepted (P4.0 architecture gate).** No Phase 4 business code may be
written before this ADR lands. This document freezes the session inventory
data model, the session management HTTP contract, the session display
metadata policy, the reauthentication generalization for account actions,
the provider ownership of password/TOTP/passkey factors, and the recovery
code capability decision. Every capability in the matrix below was
verified live against the pinned ZITADEL **v2.71.18** local instance and
the pinned `zitadel-go/v3 v3.29.2` SDK; the only provider operation not
yet live-verified is the full browser WebAuthn attestation ceremony,
whose deferral to P4.5 is stated explicitly (§8).

> **Provider security baseline (mandatory).** The provider pin is
> **v2.71.18**, not v2.71.0. ZITADEL v2.55.0–v2.71.17 are affected by
> **GHSA-cfjq-28r2-4jv5 / CVE-2025-64103** (CVSS v4 **8.7 High**): the
> Session API may treat a single-factor-authenticated session as fully
> authenticated for accounts that have a second factor enrolled, unless
> the login policy sets `requireMFA`/`requireMFAForLocalUsers`. United
> Pass login, reauthentication and every P4 acceptance flow build on the
> Session API, so a vulnerable pin would silently weaken the exact
> second-factor guarantees this phase is meant to enforce; the 2.x line is
> fixed in >= v2.71.18. This is a provider security-baseline upgrade only;
> it does not reopen Phase 3 code — Phase 3's topology, login (password +
> TOTP), interactive authorize, grant reuse, revoke → consent-again and the
> full `prompt=none` matrix were re-run green on the patched instance as
> P4.0 review evidence.

> **Revision 2026-08-08 (P4.0 architecture review).** Resolves five gate
> findings without changing commit history: (B0) provider baseline raised
> to v2.71.18 and the whole capability matrix re-probed on the patched
> instance; (B1) `user_sessions:{userId}` frozen as a **ZSET** keyed by
> absolute expiry so mixed-TTL sessions can never orphan a live session,
> and every multi-key write requires MULTI/EXEC or Lua — a pipeline is not
> atomic; (B2) bulk revoke returns `200 {"revoked": N}` instead of an
> invalid `204` with body; (B3) the password flow freezes one single mode —
> SA-privileged `SetPassword` without `currentPassword`, proven live — and
> the in-memory-thread fallback is removed as unimplementable across two
> HTTP requests; (B4) `account.passkey.remove` binds `Target` to the
> `passkeyId` and consume fails closed on mismatch. Plus: the
> consumer-less `account.sessions.revoke_others` action is reserved (the
> reauth endpoint never mints a grant for it), and the TOTP `otpauthUri`
> is classified as secret-bearing material alongside the secret.

> **Revision 2026-08-08 (P4.0 freeze-quality review).** Closes the two
> final findings: (F1) the session index freezes the **effective expiry**
> — `min(ExpiresAt, LastSeenAt + idleTTL)` — as the ZSET score and `List`
> re-applies the authoritative record-level `IsExpired` check, so an
> idle-expired session can no longer surface as active in
> `GET /me/sessions` (supersedes the first revision's "absolute expiry"
> score); (F2) the passkey RPCs in the frozen scope — `ListPasskeys`,
> `RemovePasskey`, `VerifyPasskeyRegistration` — are live-verified on the
> patched v2.71.18 instance over the same gRPC channel the backend uses,
> TOTP confirm/remove are live-probed too, and the capability matrix now
> claims live verification only where live evidence exists.

> **Revision 2026-08-08 (final F2 closure).** `RegisterPasskey` is
> live-verified on the patched v2.71.18 instance over the same gRPC
> channel the backend uses (pinned SDK, SA JWT profile, disposable user):
> the RPC is served and returns a `passkeyId` plus non-empty
> `PublicKeyCredentialCreationOptions`; the pending registration was
> removed again via `RemovePasskey` (cleanup OK). No caveat remains on any
> passkey provider call; only the full browser attestation ceremony is
> deferred to P4.5.

> **Revision 2026-08-09 (P4.3 review — narrow reopen of §6).** The P4.3
> implementation review (`acd35f2`) returned BLOCKED (B1–B5): the frozen
> §6 flow models the provider call but not the moment after the provider
> commits — rotation failure leaves other pre-change sessions live (B1),
> ambiguous Redis outcomes can resurrect the old generation (B2),
> pre-change reauth grants and enrollments survive a password change (B3),
> concurrent grants admit two winners of the password mutation (B4), and
> provider-committed degraded paths write no durable audit (B5). This ADR
> is therefore amended by **ADR-0007** (password credential settlement and
> security-generation invalidation), which freezes the settlement state
> machine, a per-user security generation (epoch), a durable single-winner
> mutation lease, provider-outcome classification and the settlement
> audit. Narrow reopens exactly two seams: the P4.1 session
> security-generation validation seam and the P4.2 reauth/enrollment
> credential-generation binding seam. Everything else stays frozen, and
> every preserved §6 surface (wire shape, SA newPassword-only mode,
> SecretPassword redaction, rotation semantics, stable failure code) is
> re-listed in ADR-0007.

> **Revision 2026-08-09 (freeze-quality closure of ADR-0007, F1–F5).**
> The first draft of ADR-0007 was blocked on five freeze-quality findings;
> its same-day revision closes them in place and supersedes the seam
> description above. The P4.1 narrow reopen is precisely: **session
> security-generation validation and stale-generation settlement cleanup**
> — one shared authoritative security-state validator invoked by **every**
> session-to-principal promotion path (at minimum `RequireSession` and
> `OptionalSession`; the P3 interaction gateway resolves through
> `OptionalSession` and surfaces epoch-stale sessions as anonymous with
> zero consent-code changes), plus generation-scoped post-change cleanup
> that never touches sessions stamped with the new epoch. Also pinned:
> executable legacy-decode normalization (missing/zero epoch ⇒ 1 at the
> persistence-adapter boundary), the active-intent barrier with provider
> deadline / lease-expiry / CAS / stale-worker fencing and a detached
> settlement context, and `providerOutcome` preserved in the durable
> settlement audit. The P4.2 seam description and the non-reopen list are
> unchanged; no additional surface is reopened.

> **Revision 2026-08-09 (F6 closure of ADR-0007 — post-provider intent
> recovery).** A final freeze-quality revision of ADR-0007 closes the last
> finding: every non-terminal mutation-intent state (`active` /
> `outcome_recorded` / `local_settlement`) now has a CAS-fenced,
> detached, bounded takeover exit to `settled` — recovery never
> re-invokes the provider, never rewrites a recorded outcome, never
> advances the epoch twice — and the intent barrier is two-phased:
> pre-outcome `active` denies all promotion; post-epoch states deny only
> stale-epoch sessions, sensitive capability consumption and further
> password mutations, allowing ordinary promotion of current-epoch
> sessions once the credential boundary is established. No additional
> surface is reopened.

Phase 3 frozen surfaces are untouched: the ConsentResolution union, the
decision lifecycle, grant reuse, local-consent revoke semantics, the
`prompt=none` matrix, LoginV2 gateway routing, callback containment and
single-winner/reconciliation are all read-only context for this phase. The
interaction gateway resolves sessions through the same session service
(`OptionalSession`), so session revocation propagates to `prompt=none`
(`login_required`) with zero consent-code changes.

## Verified provider capability matrix (ZITADEL v2.71.18, live local instance)

All probes below were executed against the running **v2.71.18** (patched)
instance with a service-account bearer token; "business error" means the
request passed authentication and authorization and reached provider
business validation. The matrix was first probed on v2.71.0 and re-probed
end-to-end on the patched v2.71.18 baseline after the security upgrade.

| # | Capability | Probe / evidence | Result |
| --- | --- | --- | --- |
| V-1 | Read user authentication factors | `GET /v2beta/users/{id}/authentication_methods` → `{"authMethodTypes":["AUTHENTICATION_METHOD_TYPE_TOTP","AUTHENTICATION_METHOD_TYPE_PASSWORD"]}` | AVAILABLE |
| V-2 | TOTP lifecycle | Live probes on the patched instance: `POST /v2beta/users/{id}/totp` → business error `COMMAND-do9se` ("already set up") on an enrolled user **and** `200` with `secret` + `otpauth://` URI on a disposable user (begin served); `POST .../totp/verify` with a wrong code → business error `EVENT-8isk2` ("Invalid code") — the confirm path reaches business validation; `DELETE .../totp` → `200` — removal served | AVAILABLE |
| V-3 | Set password | `POST /v2beta/users/{id}/password` (JSON body maps to the proto `newPassword` `Password` object). Two live probes on v2.71.18: (a) SA-privileged, `newPassword` **only**, no `currentPassword`, against a disposable user → `200`; readback via the Session API shows the new password authenticates and the old one is rejected; (b) with a wrong `currentPassword` → business error `COMMAND-3M0fs` ("Password is invalid") | AVAILABLE |
| V-4 | Passkey management | Live probes on the patched v2.71.18 over the **same gRPC channel the backend uses** (pinned SDK, SA JWT profile), against a disposable user: `RegisterPasskey` → OK, returns `passkeyId` + non-empty `PublicKeyCredentialCreationOptions` (challenge, rp, user, pubKeyCredParams all present); `ListPasskeys` → OK (confirmed passkeys only — a pending registration does not appear in the list, so enrollment state is tracked by the returned `passkeyId`, never inferred from the list); `VerifyPasskeyRegistration` with a garbage attestation → provider business validation reached (`WEBAU-5M9so` "WebAuthN Token could not be found", `InvalidArgument`) — RPC served, never `Unimplemented`; `RemovePasskey` → OK (removed the probe's pending registration; a repeat against the gone token returned business error `COMMAND-DAfb2` "NotFound"). The full browser attestation ceremony is deferred to the P4.5 live implementation | AVAILABLE (live verified) |
| V-5 | Provider session revocation | existing `Authenticator.RevokeProviderSession` (P1), already used by logout | AVAILABLE |
| V-6 | Recovery code login check | Session API v2 `Checks.RecoveryCode` is accepted by the session endpoint (session created without error for a `recoveryCode` check) | PARTIAL |
| V-7 | Recovery code generation/removal | `POST/DELETE /v2beta/users/{id}/recovery_codes` → **404 Not Found** on v2.71.0 and re-confirmed on the patched v2.71.18 (the pinned SDK proto declares `GenerateRecoveryCodes`/`RemoveRecoveryCodes`, the pinned server does not serve them) | **UNAVAILABLE** |

Consequence: password, TOTP and passkey management are real provider
operations; recovery codes cannot be issued against the pinned server
(§9).

## §1 Session inventory data model

The current `session.Store` is keyed exclusively by `session:{sha256(token)}`
with no user→sessions index. Listing account sessions must never use Redis
`KEYS`/`SCAN`.

Frozen key layout (all under the existing namespace prefix):

```text
{prefix}session:{tokenHash}            JSON SessionRecord                        (unchanged)
{prefix}user_sessions:{userId}         ZSET: member=SessionID, score=effectiveExpiryUnix  (new)
{prefix}session_locator:{sessionId}    string: tokenHash                         (new)
```

**Why a ZSET, not a plain SET.** One user can hold sessions with different
TTLs at the same time (session A with `remember=false`, session B with
`remember=true`). A single shared SET key cannot carry one TTL per member:
if every Create/Touch re-expires the shared key to *the current session's*
TTL, a later short-TTL session can expire the shared index while a
longer-TTL session is still live — the long session's record and locator
survive, but it vanishes from `GET /me/sessions` and from
revoke-all-others, becoming an inventory orphan. That is a security
semantics defect (an active session escapes the user's own inventory and
revocation surface), not a cleanup detail. The ZSET stores each member's
**effective expiry timestamp** as its score, so member lifetime is
independent of the shared key's TTL and self-healing is driven by score.

**Effective expiry is the inventory clock.** Session validity is not only
absolute: the P1 record expires when *either* `ExpiresAt` passes *or* the
idle deadline `LastSeenAt + idleTTL` passes (idleTTL > 0). A key whose
absolute expiry lies tomorrow but whose last activity was hours ago is
dead already. The frozen score is therefore

```text
effectiveExpiry = min(ExpiresAt, LastSeenAt + idleTTL)   // idleTTL > 0
effectiveExpiry = ExpiresAt                              // idleTTL = 0
```

so the inventory itself agrees with the authoritative validity check
(`SessionRecord.IsExpired(now, idleTTL)` in `RequireSession`); a score
based on `ExpiresAt` alone would let idle-expired sessions surface as
active rows in `GET /me/sessions`. `RequireSession` remains the only
authority that admits a request; the score only keeps the inventory
honest.

Design rules:

1. **Index content is SessionID only.** Neither the raw session token nor
   the token hash of any session other than through the locator is placed
   in the index; the locator is the only SessionID→tokenHash bridge and is
   server-side only. `SessionID` is never a bearer secret (§1.4), but raw
   tokens still never appear in any index.
2. **Create is all-or-nothing.** The session record, the `ZADD` into the
   user index (score = the new session's effective expiry) and the
   locator `SET` are written in **one MULTI/EXEC transaction or one Lua
   script**. Transaction failure ⇒ no session is created (fail closed).
   Record and locator receive the session's TTL; the index key's TTL
   follows rule 5 — never "same TTL as the session".
3. **Delete removes all three atomically.** The record `DEL`, the index
   `ZREM` and the locator `DEL` run in one MULTI/EXEC transaction or one
   Lua script. A Redis *pipeline* is explicitly **not** sufficient here —
   pipelines batch round-trips but provide no atomicity; the word
   "atomically" in this ADR always means MULTI/EXEC or Lua. Idempotent.
4. **Token rotation keeps the SessionID stable.** Rotation writes the new
   record, points the locator at the new token hash, deletes the old record
   and refreshes the member's score to the new effective expiry, all in one
   transaction; ZSET membership is untouched otherwise. The brief
   dual-validity window documented for `Rotate` in ADR-0002 §7 is unchanged.
5. **Shared index TTL never expires ahead of any live member.** "Live"
   means *effective*-expiry live: after any write that adds or extends a
   member (Create/Rotate/Touch), the index key is re-expired to the
   **maximum member score** (the furthest effective expiry among current
   members). The shared key therefore always outlives every session it
   indexes; a short-TTL or idle-bound session can never truncate another
   session's visibility.
6. **Touch refreshes record, locator and member score.** `Touch` sets
   `LastSeenAt = now`, re-expires the record and the locator to the
   session's TTL (remember policy included), `ZADD`s the member with its
   new effective expiry score `min(ExpiresAt, now + idleTTL)` (or
   `ExpiresAt` when idleTTL = 0), and re-aligns the index key TTL per
   rule 5.
7. **Self-healing on read.** `List` reads the ZSET, drops every member with
   `score < now` (absolute- **or idle-expired**), resolves each survivor
   through the locator and then the record, and re-applies the same
   authoritative `SessionRecord.IsExpired(now, idleTTL)` semantics to the
   loaded record (defense in depth against a stale score). A survivor that
   fails the record check, or whose locator or record is gone (TTL race,
   crash between writes), is removed best-effort — record `DEL` + locator
   `DEL` + `ZREM`, rule 3 semantics — and is **never returned as active**.
   No full Redis scan exists anywhere in this path.
8. **Revoke-all-others** iterates exactly the user index of the calling
   user, never deleting the member whose SessionID equals the current
   session, then applies rule 3 to each victim.

### 1.4 SessionID becomes a browser-visible revoke identifier

`SessionID` is returned to the browser as the `sessionId` of list entries
and accepted in the revoke path. Therefore:

- the `crypto/rand` **timestamp fallback in `generateSessionID` is
  removed**; a random-read failure fails session creation closed
  (`errors.New`, no ID, no session);
- entropy stays at 128 bits (16 random bytes, hex-encoded);
- a SessionID that is not in the current user's index, belongs to another
  user, or resolves to nothing yields the same stable **404
  `session.not_found`** outcome (non-enumerating; never 403, never a
  different error for "exists but not yours").

## §2 Session management HTTP contract

All routes live under `/api/v1`; mutations require `RequireSession` +
`RequireCSRF` (the existing middleware group pattern).

| Method & path | Semantics | Success | Errors |
| --- | --- | --- | --- |
| `GET /me/sessions` | list the caller's UP sessions | `200` array of SessionView (§3) | 401 unauthenticated |
| `DELETE /me/sessions/{sessionId}` | revoke exactly one *other* session of the caller | `204` | 404 stable (unknown/foreign/already gone); 409 `session.current` when `sessionId` is the caller's current session |
| `DELETE /me/sessions` | revoke **all other** sessions of the caller; the current session is always preserved | `200` with body `{"revoked": N}` | 401/CSRF |
| `DELETE /auth/session` | logout the current session (existing endpoint, unchanged) | `204`, both cookies cleared | unchanged |

The bulk route returns `200` (not `204`): the count body is the whole
point of the response, and `204 No Content` must not carry a body —
several HTTP clients and runtimes discard it outright.

Revocation ordering per victim session:

1. delete record + index entry + locator (rule 3);
2. best-effort provider session revocation: decrypt the sealed credential,
   call `RevokeProviderSession`; provider failure **never** fails the local
   revocation and never pretends the provider call happened (audit event
   records the outcome class);
3. security event `session.revoked` (self) / `session.revoked_other`
   (targeted) / `session.revoked_others` (bulk) — payloads carry session
   IDs and outcome only, never tokens, hashes or provider references.

A revoked browser's next request fails session lookup ⇒ `GET /me → 401`,
and the interaction gateway's `OptionalSession` resolves no session ⇒
`prompt=none → login_required` (P3 cross-invariant, re-verified in P4.9).

No reauthentication is required to revoke sessions (session + CSRF is the
gate); this is frozen as **not required** for either revoke route (both are
already strictly current-user-scoped), keeping the reauth surface minimal
and keeping the suspected-compromise "kick my other devices" recovery path
free of an extra ceremony that could lock the real account holder out. The
`account.sessions.revoke_others` action is reserved but never accepted by
the reauth endpoint (§4): no handler demands it and no grant can be minted
for it.

## §3 Session display metadata

`SessionRecord` today stores only `UserAgentHash`; the frozen frontend
`UserSession` shape requires display data. **No location or device fact is
fabricated.** Frozen policy:

- At session creation the server normalizes the `User-Agent` header with a
  small built-in allowlist parser into two display strings:
  `DeviceDisplay` (OS family, e.g. `macOS`, `Windows`, `Android`, `iOS`,
  `Linux`) and `ClientDisplay` (browser family, e.g. `Chrome`, `Safari`,
  `Firefox`, `Edge`). Unrecognized/absent UA ⇒ empty strings, rendered as
  `未知设备` / `未知浏览器`. The normalized strings (max 64 bytes each) are
  stored plaintext in the record — they are display-only, non-secret data.
  The raw UA continues to be stored only as `UserAgentHash`.
- At creation the server stores the **masked** client IP only: IPv4 keeps
  the first three octets (`203.0.113.*`), IPv6 keeps the first four groups
  (`2001:db8:1:2:*`). The raw IP is never persisted. Missing/untrusted
  remote address ⇒ empty ⇒ rendered `未知 IP`. No proxy-header trust is
  added in P4.
- `approximateLocation` is **optional/null** in the API; the server always
  returns null (no GeoIP dependency). UI renders `未知位置`.
- `isCurrent` is computed by comparing the entry's SessionID with the
  caller's principal SessionID; it is never client-supplied.
- `lastActiveAt` is `LastSeenAt` from the record.

Frozen wire type (`GET /me/sessions` entry):

```json
{
  "sessionId": "3f2c…",
  "deviceName": "macOS",
  "clientName": "Chrome",
  "approximateLocation": null,
  "ipAddressMasked": "127.0.0.*",
  "lastActiveAt": "2026-08-08T10:00:00Z",
  "createdAt": "2026-08-08T09:00:00Z",
  "authenticationMethods": ["password", "totp"],
  "isCurrent": true
}
```

The frontend `UserSession` type adapts: `approximateLocation: string |
null`; unknown values render the `未知…` copy. The mock keeps richer fake
data in mock mode only.

## §4 Reauthentication generalization

The P1 reauth domain (opaque challenge/grant tokens, SHA-256-hash-only
Redis keys, user+session+action binding, atomic claim/consume, single-use
grants, short TTL, rate limiting, MFA continuation, expired-challenge
provider-session cleanup, fail-closed Redis semantics) is **extended, not
duplicated**. No second password-confirm token mechanism is introduced.

Frozen action set (additions to the existing three):

```text
account.password.change
account.totp.enroll
account.totp.remove
account.passkey.enroll
account.passkey.remove             (Target = passkeyId, §8)
```

`account.sessions.revoke_others` is **reserved, not active**: §2 revokes
sessions behind `RequireSession` + `RequireCSRF` with no step-up, so no
handler consumes this action. The reauthentication endpoint must **refuse
to mint a grant** for it (unknown/unaccepted action ⇒ fail closed), and no
code path may consume it. Keeping a consumer-less action mintable would
only widen the grant surface for nothing; the name is reserved here solely
so a future step-up decision has a stable identifier. Until then it is not
part of the accepted action set.

(`account.recovery_codes.rotate` is intentionally absent — §9.)

### Generic target binding

`ReauthChallengeData` and `ReauthGrantData` gain one optional field:

```text
Target string `json:"target,omitempty"`
```

- Existing `application.delete` / `client.delete` / `client.secret.rotate`
  keep their exact semantics: `ApplicationID` (+ `ClientID`) binding,
  `Target` unused. Full backward compatibility is a freeze requirement.
- Account actions bind `UserID` + `SessionID` + `Action`; `ApplicationID`
  and `ClientID` **must be empty** for `account.*` actions (a fake
  applicationId is forbidden).
- `Target` is empty for every P4 account action **except
  `account.passkey.remove`**, whose `Target` is the `passkeyId` being
  removed (§8). A single-use grant issued for passkey A must never
  authorize the removal of passkey B; `Target` exists precisely to close
  that hole.
- Consume validates every binding: wrong user / wrong session / wrong
  action / **wrong target** / non-empty application-client fields on
  account actions / expired / already-consumed ⇒ fail closed.

Password-only accounts receive the immediate single-use grant path;
password+TOTP accounts go `202 challenge → MFA → single-use grant`,
reusing the existing reauth MFA continuation exactly.

## §5 Remember + MFA completion (P3 freeze debt, fixed in P4.1)

Frozen fix: `auth.MFAChallengeData` gains `Remember bool`; the login
handler stores the request's `remember` into the challenge; `CompleteMFA`
passes `challenge.Remember` (instead of the hard-coded `false`) into
session creation, so the session TTL policy and the `Remember` record flag
match the user's choice. In-flight pre-fix challenges decode with
`Remember=false` (safe default). This is a P1/P4 session-semantics fix and
does not reopen Phase 3.

## §6 Password management

> **Amended by ADR-0007 (2026-08-09, P4.3 review).** The flow below stays
> frozen up to and including the provider call; the post-provider
> settlement (what happens after the credential has changed) is redefined
> by ADR-0007: security-generation epoch advancement is the first local
> effect after a committed/unknown provider outcome, a durable
> single-winner mutation lease fences the provider call, and a settlement
> audit row is attempted on every provider-committed terminal path.

The provider is the password authority. United Pass never stores, hashes
or mirrors user passwords.

Frozen flow for `POST /api/v1/me/security/password`
(`{newPassword}` body; **no** `currentPassword` field):

1. `RequireSession` + `RequireCSRF`;
2. consume a valid reauth grant for `account.password.change`
   (`X-Reauthentication-Token`); the reauth ceremony — which already
   proved the current password (and TOTP where enrolled) — is the sole
   proof of identity; the mutation never re-accepts `currentPassword`;
3. provider `SetPassword` for the user's provider identity, in the single
   frozen mode: the service account sets the password **without**
   `currentPassword`. This mode was **proven live on the patched v2.71.18
   instance** (V-3(a)): against a disposable user the SA-privileged call
   with `newPassword` only returned `200`, and Session-API readback
   confirmed the new password authenticates while the previous one is
   rejected. It requires the SA's org-level user-management permission,
   which the acceptance SA holds. **No fallback exists.** The previously
   sketched "thread the reauth-verified password through in-memory" option
   is unimplementable: reauthentication and the password mutation are two
   separate HTTP requests, and the plaintext password ceases to exist the
   moment the reauth request ends, so there is nothing to thread without
   persisting it, embedding it in the reauth token, or re-asking the user —
   each of which breaks the frozen secret lifecycle. An environment whose
   SA lacks this permission must fix the permission, never the flow;
4. frozen session policy after provider success:
   - the **current** UP session is preserved but rotated (new session
     token + new CSRF token; cookies re-issued; SessionID stable);
   - **all other** UP sessions of the user are revoked (§2 rule 3 +
     best-effort provider session revocation);
   - the sealed provider session credential of the current session is
     re-sealed from the provider response when the password call yields a
     fresh session handle; otherwise the existing credential survives
     (the provider session remains valid for the user);
5. security event `account.password_changed` (no password material in the
   payload); provider failure ⇒ no local side effects, stable error
   `provider.password_change_failed` (fail closed, never pretend success).

Acceptance proof (P4.9): old password → login fails; new password → login
succeeds; other session revoked; current session rotated.

## §7 TOTP management

Frozen endpoints (all `RequireSession` + `RequireCSRF`; provider is the
factor authority; no local factor state beyond reauth/enrollment
transients):

| Endpoint | Reauth action | Provider call |
| --- | --- | --- |
| `GET /api/v1/me/security` | — | factor summary from `ListAuthenticationMethodTypes` (+ provider profile readback) |
| `POST /api/v1/me/security/totp/enrollment` | `account.totp.enroll` (consumed here) | `RegisterTOTP` → `{enrollmentToken, secret, otpauthUri}` |
| `POST /api/v1/me/security/totp/enrollment/confirm` | enrollmentToken (below) | `VerifyTOTPRegistration` |
| `DELETE /api/v1/me/security/totp` | `account.totp.remove` | `RemoveTOTP` + readback |

Enrollment secret handling (freeze):

- **both `secret` and `otpauthUri` are secret-bearing material** — the
  `otpauth://` URI embeds the secret in its `secret=` parameter, so every
  rule that applies to the secret applies to the URI unchanged;
- the secret/URI appear **only** in the begin response; the response
  carries `Cache-Control: no-store`;
- neither is ever logged, written to audit payloads, analytics or
  PostgreSQL, nor returned again — after the response the only
  authoritative copy is the provider's pending registration;
- the begin step consumes the reauth grant and mints a short-lived,
  single-use `enrollmentToken` (same pattern as MFA tokens: SHA-256 hash
  key in Redis, user+session bound, claim/consume Lua semantics) so the
  confirm step does not require a second reauth ceremony;
- confirm is single-winner via the claim lock; double registration fails
  on the provider side (`already set up`), which maps to a stable 409 —
  the flow is idempotency-safe;
- removal requires reauth and performs a provider readback afterwards;
  the factor list endpoint never infers state from local memory.

## §8 Passkey management

Included in P4 scope (V-4). Every provider call in the table below —
`RegisterPasskey`, `VerifyPasskeyRegistration`, `RemovePasskey` — is
live-verified on the patched instance (V-4); the only unverified surface
is the full browser attestation ceremony itself, which is exercised live
during P4.5 implementation before the corresponding handler ships.
Frozen endpoints:

| Endpoint | Reauth action | Provider call |
| --- | --- | --- |
| `POST /api/v1/me/security/passkeys/enrollment` | `account.passkey.enroll` | `RegisterPasskey` → `{enrollmentToken, passkeyId, publicKeyCredentialCreationOptions}` |
| `POST /api/v1/me/security/passkeys/enrollment/confirm` | enrollmentToken | `VerifyPasskeyRegistration` with the browser's attestation |
| `DELETE /api/v1/me/security/passkeys/{passkeyId}` | `account.passkey.remove` with `Target = {passkeyId}` (§4) | `RemovePasskey` + readback |

- `publicKeyCredentialCreationOptions` is passed through verbatim to
  `navigator.credentials.create()`; real mode never runs the mock WebAuthn
  simulator;
- `passkeyId` is returned only to its owner session and accepted in the
  delete path with the same stable-404 non-enumeration rule as sessions;
- the delete path consumes the reauth grant only when
  `grant.Target == route.passkeyId`; a grant minted for passkey A can
  never remove passkey B (wrong target ⇒ fail closed, §12);
- attestation payloads are never logged or audited (only sizes/outcomes).

### Factor summary + multiple passkeys

The legacy `SecurityFactor[]` ("one row per kind") cannot represent
multiple passkeys and is not forced to. Frozen real response shape for
`GET /api/v1/me/security`:

```json
{
  "password": { "set": true },
  "totp": { "enabled": true },
  "passkeys": [
    { "passkeyId": "…", "createdAt": "…", "state": "active" }
  ],
  "recoveryCodes": { "available": false, "deferredReason": "provider_unsupported" }
}
```

The frontend adapts its view model: summary rows derived from this
payload, one real row per passkey. Compressing several passkeys into one
fake factor row is forbidden.

## §9 Recovery codes — DEFERRED BY ARCHITECTURE (Option B)

Findings: the pinned SDK proto declares `GenerateRecoveryCodes` /
`RemoveRecoveryCodes` and the session API accepts a `recoveryCode` check
(V-6), but the pinned server **does not serve** the generate/remove
endpoints (V-7: live 404 on both POST and DELETE on v2.71.0 and
re-confirmed on the patched v2.71.18). United Pass can therefore verify
codes in a login chain it cannot issue; a self-built code store could
never enter the real login chain safely.

Frozen decision:

- P4.6 is **DEFERRED BY ARCHITECTURE**;
- real mode hides all Recovery Codes UI (the seam returns
  `recoveryCodes.available=false`; the panel does not render);
- mock mode keeps the existing mock behavior;
- the login backend keeps rejecting `recovery_code` MFA methods
  (P3.9 state, unchanged);
- this deferral does not block the Phase 4 freeze. Reopens only when the
  pinned provider gains a verifiable issuance lifecycle.

## §10 Authorization model for provider factor writes

Factor writes run under the backend's service account via the pinned SDK
(gRPC), not under a user token. The acceptance instance already grants the
service account org-level ownership (init script adds `ORG_OWNER`); the
frozen requirement for any environment is: the backend SA must hold an
org-level user-management role for the orgs it serves. The password write
mode is already proven live at P4.0 (V-3(a)); the TOTP lifecycle
(register/verify/remove) and every passkey provider call
(register/list/remove/verify) were live-probed at P4.0 under the same SA
(V-2, V-4), so the permission model is proven for every frozen write
path.
Missing permission ⇒ stable `provider.forbidden` error, fail closed, no
local side effects.

## §11 Non-goals and frozen boundaries (Phase 4)

Explicitly out of scope: OAuth grant/consent semantics, provider-issued
token revocation, application/client lifecycle, user/staff admin,
directory sync, ABAC/policy, admin audit explorer, email/phone profile
changes, account deletion, data export, logged-out password-reset email
flows, and P3 historical reconciliation debt.

```text
Account Session revoke ≠ OAuth Grant revoke ≠ provider OAuth token revoke
```

If any implementation appears to require changes under
`backend/internal/consent/*`: STOP, report, evaluate against the Phase 3
reopen criteria, and do not proceed without approval.

## §12 Security hardening contract (binding for P4.8)

- Redis: no `KEYS`/`SCAN` in any session-listing or inventory path;
- browser-visible identifiers (SessionID, passkeyId) are cryptographically
  random; token hashes, provider session references and sealed
  credentials never leave the server;
- reauth matrix all fail closed: wrong user / wrong session / wrong action
  / wrong target / expired / consumed / parallel consume / parallel MFA
  completion / rate limit / provider outage / Redis outage;
- session matrix: cross-user revoke ⇒ 404; random sessionId ⇒ 404;
  expired-index self-healing; concurrent/double revoke idempotent;
  revoke-others preserves current; logout clears both cookies; rotation
  invalidates the old token;
- sensitive-data sweep across logs, slog, HTTP errors, JSON responses,
  DOM, security events, DB and Redis non-secret fields: password, TOTP
  secret, TOTP code, recovery code, WebAuthn assertion, session cookie,
  CSRF raw token, reauth raw token, provider session credential — zero
  persistence, zero log output.
