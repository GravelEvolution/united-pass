# Environment files
.env
.env.local
.env.*.local
!.env.template

# Go build artifacts
/bin/
/api
*.exe
*.out
coverage.txt

# SSH tunnel runtime state (scripts/tunnel.sh)
.tunnel.pid
.tunnel.log

# ZITADEL development instance state (scripts/zitadel-init.sh)
.zitadel/

# Ephemeral acceptance/dev scripts; never committed
.tmp-*
