#
# Copyright (c) 2026 Chen Jiajie(Ariakage)
#
# Author: Chen Jiajie(Ariakage) <ariakage233@gmail.com>
# Date: 2026-08-05
# Description: OpenAPI contract for the United Pass backend API
#

openapi: 3.1.0

info:
  title: United Pass Backend API
  description: |
    United Pass is a unified identity, account, organization, OAuth application,
    authorization, Provider synchronization and audit platform.

    This OpenAPI specification is the machine-readable contract. The human-readable
    detail lives in `frontend/docs/api-contracts.md`. Both must stay synchronized.

    Phase 0 scope: operational endpoints only.
    Phase 1 scope: session establishment, MFA, logout, current user and
    permission capabilities.
    Phase 2 scope: OAuth application and client management plane (scope
    catalog, application/client lifecycle, secret rotation, reauthentication).
    Phase 5 scope: stable user administration, optional employee profiles,
    departments, offboarding and durable access-revocation convergence.
    Phase 6 scope: Feishu federated login, durable directory staging,
    synchronization history and explicit identity-link conflict resolution.
    Phase 7 scope: Cerbos-backed ABAC policy drafts/version publication and
    durable audit search/export jobs.
    Phase 8 scope: controlled legal-document publication, requester-owned
    personal-data exports and cancellable delayed account deletion.
    DreamUP administration scope: event-scoped roles, administrator step-up,
    admission review and restricted identity-access approvals.
    See `../docs/adr-0004.md`, `../docs/adr-0011.md` and
    `../docs/adr-0012.md` and `../docs/adr-0013.md` for the architecture decisions.

    Business API paths use the full `/api/v1/...` prefix explicitly rather than
    relying on a global server base path, so that root-level operational endpoints
    and versioned API endpoints can coexist accurately.
  version: 0.10.0
  contact:
    name: United Pass backend team

servers:
  - url: http://localhost:8080
    description: Local development
  - url: /
    description: Same-origin production

tags:
  - name: Operational
    description: Health and readiness endpoints
  - name: Session
    description: |
      Browser session establishment, MFA challenge completion and logout.
      Sessions are carried by the `up_session` HttpOnly cookie; state-changing
      operations require the `X-CSRF-Token` header matching the `up_csrf` cookie.
  - name: WeChat Mini Program
    description: |
      Default-off server-side verification of one-time Mini Program codes.
      The client never sends an openId, unionId, session_key, AppSecret or a
      claimed phone number. Legacy login restores only a pre-existing explicit
      binding; the separately gated onboarding routes can create or link an
      account after password/MFA and email controls.
  - name: Registration
    description: |
      Default-off public account creation and email verification. Every
      mutation requires an exact same-origin `Origin` header, JSON, a bounded
      body, and an independent Redis rate-limit namespace.
  - name: Account
    description: Current user profile and permission capabilities
  - name: Account Security
    description: Provider-authoritative account factors and passkey lifecycle
  - name: Privacy and Legal
    description: Personal-data rights and controlled public legal-document status
  - name: Reauthentication
    description: |
      High-risk operation reauthentication. A completed grant is required
      (single-use, action-bound, session-bound) before delete or secret
      rotation operations execute.
  - name: Administrator Step-up
    description: |
      Owner-only DreamUP administrator question-and-answer step-up. The entire
      namespace is absent unless the server-side DreamUP administration gate is
      explicitly enabled. Challenge secrets are never returned by mutations.
  - name: Restricted Identity Access
    description: |
      Event-scoped, time-limited access to an allowlisted set of identity fields.
      Requests never include legal documents and responses never expose the
      encrypted reason, internal authorization stamp, or one-time claim nonce.
  - name: Admin Users
    description: Stable identity administration and target user session lifecycle
  - name: Admin Employees
    description: Employee profiles linked explicitly to stable user identities
  - name: Admin Departments
    description: Cycle-safe organization hierarchy and membership views
  - name: Provider Login
    description: Feishu OAuth login initiation and callback; provider credentials remain server-only
  - name: Admin Identity Providers
    description: Provider status, durable directory synchronization and explicit identity linking
  - name: Admin Policies
    description: Versioned ABAC drafts, simulation and Cerbos publication
  - name: Admin Audit
    description: Durable security-event filtering and short-lived asynchronous CSV exports
  - name: Admin Registration Defense
    description: Audited system-administrator recovery for false-positive registration blocks
  - name: Scope Catalog
    description: Backend-authoritative OAuth scope catalog
  - name: Admin Applications
    description: |
      OAuth application management plane. All mutating operations require
      session + CSRF + server-side authorization; deleting an application
      additionally requires a reauthentication grant. Provider identifiers
      never appear in responses.
  - name: Admin OAuth Clients
    description: |
      OAuth client management nested under its owning application. Every
      nested route asserts `client.applicationId == path.applicationId`
      (404 on mismatch, anti-enumeration). Secret rotation requires the
      `application.secret.rotate` permission plus a reauthentication grant;
      the new secret is returned exactly once with `Cache-Control: no-store`.

paths:
  /.well-known/dreamup-admin-jwks.json:
    get:
      tags: [Administrator Step-up]
      summary: DreamUP delegation verification keys
      operationId: getDreamUPAdminJWKS
      responses:
        "200":
          description: Public Ed25519 JWK set
          content:
            application/json:
              schema:
                type: object
                required: [keys]
                properties:
                  keys:
                    type: array
                    items: { type: object }
    head:
      tags: [Administrator Step-up]
      summary: Check DreamUP administrator delegation verification keys
      operationId: headDreamUPAdminJWKS
      responses:
        "200":
          description: Verification-key representation metadata without a response body

  /.well-known/dreamup-mobile-jwks.json:
    get:
      tags: [WeChat Mini Program]
      summary: DreamUP participant delegation verification keys
      operationId: getDreamUPMobileJWKS
      responses:
        "200":
          description: Public Ed25519 JWK set for request-bound Mini Program assertions
          content:
            application/json:
              schema:
                type: object
                required: [keys]
                properties:
                  keys:
                    type: array
                    items: { type: object }
    head:
      tags: [WeChat Mini Program]
      summary: Check DreamUP participant delegation verification keys
      operationId: headDreamUPMobileJWKS
      responses:
        "200":
          description: Verification-key representation metadata without a response body

  /api/v1/admin/dreamup/events:
    get:
      tags: [Administrator Step-up]
      summary: List authorized DreamUP events
      operationId: listAuthorizedDreamUPEvents
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      responses:
        "200":
          description: Authorized event summaries
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/dreamup/session:
    get:
      tags: [Administrator Step-up]
      summary: Bootstrap the first-party DreamUP administrator session
      operationId: getDreamUPAdminBrowserSession
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      responses:
        "200":
          description: Authenticated user; browser-cookie callers also receive their CSRF value in the body.
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/dreamup/eligibility:
    get:
      tags: [Administrator Step-up]
      summary: Check whether the current account may enter DreamUP administration
      operationId: getDreamUPAdminEligibility
      description: |
        Low-disclosure authenticated bootstrap check. Returns true only when the
        caller has at least one active event binding authorized for
        `event.dashboard.read`. Generic account personas are not authority. This
        operation deliberately returns no event, role, count or step-up data and
        does not require administrator step-up.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      responses:
        "200":
          description: Eligibility result containing one boolean only.
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DreamUPAdminEligibilityResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/dreamup/step-up/challenge:
    get:
      tags: [Administrator Step-up]
      summary: Read the DreamUP administrator step-up challenge
      operationId: getDreamUPAdminStepUpChallenge
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/DreamUPEventIdQuery"
        - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      responses:
        "200":
          description: Current administrator step-up state and public question.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminStepUpChallengeResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/admin/dreamup/step-up/enroll:
    post:
      tags: [Administrator Step-up]
      summary: Enroll the DreamUP administrator step-up answer
      operationId: enrollDreamUPAdminStepUp
      description: Cookie callers provide CSRF; native bearer callers provide the Mini Program marker instead. A fresh provider-backed primary login is always required. Existing TOTP, passkey, or recovery MFA is accepted; only an active system-scoped super_admin or top_admin with no existing challenge may use the initial-enrollment bootstrap exception without existing MFA. A federated session lacking provider session proof receives session.reauthentication_required.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
        - $ref: "#/components/parameters/Idempotency-Key"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminStepUpEnrollRequest" }
      responses:
        "201":
          description: Step-up answer enrolled.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminStepUpMutationResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/dreamup/step-up/verify:
    post:
      tags: [Administrator Step-up]
      summary: Verify the DreamUP administrator step-up answer
      operationId: verifyDreamUPAdminStepUp
      description: |
        Cookie callers provide CSRF; native bearer callers provide the Mini
        Program marker instead. The provider-backed primary login must be no
        older than 30 minutes. Every mutation, including application review
        and check-in scan, is high risk. Verification returns a five-minute,
        action-and-target-bound single-use token, which a native Mini Program
        client must hold in memory and send exactly once as
        `X-Reauthentication-Token`. Verification also refreshes the durable
        session-bound proof used by the existing cookie website; that website
        may omit the header only while this proof is no more than five minutes
        old. A cookie request that supplies a token gets strict single-use
        semantics and cannot fall back after token rejection.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
        - $ref: "#/components/parameters/Idempotency-Key"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminStepUpVerifyRequest" }
      responses:
        "200":
          description: Step-up grant established for the current session.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminStepUpMutationResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "423": { $ref: "#/components/responses/Locked" }
        "429": { $ref: "#/components/responses/TooManyRequests" }

  /api/v1/admin/dreamup/events/{eventId}/operations/status:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
    get:
      tags: [Administrator Step-up]
      summary: Read the durable outcome of a DreamUP administration mutation
      operationId: getDreamUPAdminMutationStatus
      description: |
        Returns only the authoritative United Pass mutation-ledger status for
        the supplied idempotency key. Access requires the initiating account,
        a current event dashboard binding, and active administrator step-up.
        This endpoint never reissues the mutation and never returns the private
        DreamUP receipt hash or the original request/response body. After a
        succeeded result, refetch the authoritative DreamUP resource state.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/Idempotency-Key"
      responses:
        "200":
          description: Durable mutation outcome metadata.
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DreamUPMutationStatusResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/FieldValidationFailed" }
        "502":
          description: The durable outcome ledger is temporarily unavailable or internally inconsistent.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }

  /api/v1/admin/dreamup/events/{eventId}/teams:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
    get:
      tags: [Administrator Step-up]
      summary: List review-safe DreamUP team summaries
      operationId: listDreamUPAdminTeams
      description: Requires the event dashboard-read capability and an active administrator session. Participant contact details and application answers are not disclosed.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      responses:
        "200":
          description: Review-safe team summaries for the selected event
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/dreamup/events/{eventId}/content:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
    get:
      tags: [Administrator Step-up]
      summary: Read DreamUP introduction and announcement drafts
      operationId: getDreamUPAdminContent
      description: Requires the high-risk `event.content.manage` capability and a fresh five-minute administrator proof. Native Mini Program bearer callers require an action-and-target-bound one-shot token; the existing cookie website may use its session-bound durable proof. Draft content is private and no-store.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/DreamUP-X-Reauthentication-Token"
      responses:
        "200":
          description: Current introduction and announcement administration state.
          headers:
            ETag: { $ref: "#/components/headers/ETag" }
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DreamUPContentResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/admin/dreamup/events/{eventId}/content/intro:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
    put:
      tags: [Administrator Step-up]
      summary: Save or publish the DreamUP event introduction
      operationId: putDreamUPAdminIntroduction
      description: Requires `event.content.manage`, a fresh five-minute administrator proof, CSRF for cookie callers, `If-Match`, and an idempotency key. Native Mini Program bearer callers require an action-and-target-bound one-shot token; the existing cookie website may use its session-bound durable proof. The JSON object is passed through unchanged to the private DreamUP data plane.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/DreamUP-X-Reauthentication-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
        - $ref: "#/components/parameters/DreamUP-If-Match"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DreamUPIntroMutationRequest" }
      responses:
        "200":
          description: Persisted content state.
          headers:
            ETag: { $ref: "#/components/headers/ETag" }
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DreamUPContentResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/FieldValidationFailed" }

  /api/v1/admin/dreamup/events/{eventId}/announcements:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
    post:
      tags: [Administrator Step-up]
      summary: Create a DreamUP announcement draft or publication
      operationId: postDreamUPAdminAnnouncement
      description: Requires `event.content.manage`, a fresh five-minute administrator proof, CSRF for cookie callers, `If-Match`, and an idempotency key. Native Mini Program bearer callers require an action-and-target-bound one-shot token; the existing cookie website may use its session-bound durable proof. The JSON object is passed through unchanged to the private DreamUP data plane.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/DreamUP-X-Reauthentication-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
        - $ref: "#/components/parameters/DreamUP-If-Match"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DreamUPAnnouncementMutationRequest" }
      responses:
        "200":
          description: Created announcement state.
          headers:
            ETag: { $ref: "#/components/headers/ETag" }
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DreamUPContentResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/FieldValidationFailed" }

  /api/v1/admin/dreamup/events/{eventId}/announcements/{contentId}:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      - $ref: "#/components/parameters/DreamUPContentIdPath"
    patch:
      tags: [Administrator Step-up]
      summary: Save, publish or archive an existing DreamUP announcement
      operationId: patchDreamUPAdminAnnouncement
      description: Requires `event.content.manage`, a fresh five-minute administrator proof, CSRF for cookie callers, `If-Match`, and an idempotency key. Native Mini Program bearer callers require an action-and-target-bound one-shot token; the existing cookie website may use its session-bound durable proof. The JSON object is passed through unchanged to the private DreamUP data plane.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/DreamUP-X-Reauthentication-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
        - $ref: "#/components/parameters/DreamUP-If-Match"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DreamUPAnnouncementMutationRequest" }
      responses:
        "200":
          description: Persisted announcement state.
          headers:
            ETag: { $ref: "#/components/headers/ETag" }
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DreamUPContentResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/FieldValidationFailed" }

  /api/v1/admin/dreamup/events/{eventId}/contact-submissions:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
    get:
      tags: [Administrator Step-up]
      summary: List audited DreamUP feedback and sponsorship submissions
      operationId: listDreamUPContactSubmissions
      description: Requires the high-risk `event.contact_submission.manage` capability and a fresh five-minute administrator proof. Native Mini Program bearer callers require an action-and-target-bound one-shot token; the existing cookie website may use its session-bound durable proof. Generic audit-read authority is insufficient. Results are audit-logged before disclosure.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/DreamUP-X-Reauthentication-Token"
        - name: kind
          in: query
          schema: { type: string, enum: [feedback, sponsorship] }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100 }
        - name: cursor
          in: query
          description: Opaque base64url cursor returned by the previous page.
          schema: { type: string, minLength: 1, maxLength: 512, pattern: "^[A-Za-z0-9_-]+$" }
      responses:
        "200":
          description: Audited contact submissions with the latest immutable handling state. Participant-provided contact data is private and no-store.
          x-data-classification: participant_contact
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [submissions, nextCursor]
                properties:
                  submissions:
                    type: array
                    maxItems: 100
                    items:
                      type: object
                      additionalProperties: false
                      required: [id, kind, category, payload, createdAt, resolution]
                      properties:
                        id: { type: string }
                        kind: { type: string, enum: [feedback, sponsorship] }
                        category: { type: string, enum: [feedback, emergency, sponsorship] }
                        payload:
                          oneOf:
                            - type: object
                              additionalProperties: false
                              required: [content]
                              properties:
                                content: { type: string, minLength: 10, maxLength: 4000 }
                            - type: object
                              additionalProperties: false
                              required: [category, eventType, location, contact, details]
                              properties:
                                category: { type: string, const: emergency }
                                eventType: { type: string }
                                location: { type: string }
                                contact: { type: string }
                                details: { type: string }
                            - type: object
                              additionalProperties: false
                              required: [organization, contact, intent]
                              properties:
                                organization: { type: string, minLength: 2, maxLength: 160 }
                                contact: { type: string, minLength: 3, maxLength: 254 }
                                intent: { type: string, minLength: 10, maxLength: 2000 }
                        createdAt: { type: string, format: date-time }
                        resolution:
                          oneOf:
                            - type: object
                              additionalProperties: false
                              required: [status, version, note, updatedAt]
                              properties:
                                status: { type: string, enum: [acknowledged, closed] }
                                version: { type: integer, minimum: 1 }
                                note: { type: string, minLength: 2, maxLength: 1000 }
                                updatedAt: { type: string, format: date-time }
                            - type: "null"
                  nextCursor:
                    type: [string, "null"]
                    maxLength: 512
                    pattern: "^[A-Za-z0-9_-]+$"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/dreamup/events/{eventId}/contact-submissions/{submissionId}:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      - $ref: "#/components/parameters/DreamUPContactSubmissionIdPath"
    get:
      tags: [Administrator Step-up]
      summary: Read one audited DreamUP contact submission
      operationId: getDreamUPContactSubmission
      description: Requires the high-risk `event.contact_submission.manage` capability and a fresh five-minute administrator proof. Native Mini Program bearer callers require an action-and-target-bound one-shot token; the existing cookie website may use its session-bound durable proof. Generic audit-read authority is insufficient.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/DreamUP-X-Reauthentication-Token"
      responses:
        "200":
          description: One private contact submission and its latest immutable handling state.
          x-data-classification: participant_contact
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [submission]
                properties:
                  submission: { $ref: "#/components/schemas/DreamUPContactSubmission" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/admin/dreamup/events/{eventId}/contact-submissions/{submissionId}/resolution:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      - $ref: "#/components/parameters/DreamUPContactSubmissionIdPath"
    post:
      tags: [Administrator Step-up]
      summary: Append a DreamUP contact-submission handling state
      operationId: postDreamUPContactSubmissionResolution
      description: Requires the dedicated high-risk `event.contact_submission.manage` capability, a fresh five-minute administrator proof, CSRF for cookie callers, `If-Match`, and an idempotency key. Native Mini Program bearer callers require an action-and-target-bound one-shot token; the existing cookie website may use its session-bound durable proof. The signed delegation binds both state-control headers.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/DreamUP-X-Reauthentication-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
        - $ref: "#/components/parameters/DreamUP-If-Match"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [status, note]
              properties:
                status: { type: string, enum: [acknowledged, closed] }
                note: { type: string, minLength: 2, maxLength: 1000 }
      responses:
        "200":
          description: Persisted append-only handling state. Support notes are private and no-store.
          x-data-classification: participant_contact_resolution
          headers:
            ETag: { $ref: "#/components/headers/ETag" }
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [resolution]
                properties:
                  resolution:
                    type: object
                    additionalProperties: false
                    required: [status, version, note, updatedAt]
                    properties:
                      status: { type: string, enum: [acknowledged, closed] }
                      version: { type: integer, minimum: 1 }
                      note: { type: string, minLength: 2, maxLength: 1000 }
                      updatedAt: { type: string, format: date-time }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/dreamup/events/{eventId}/applications:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
    get:
      tags: [Administrator Step-up]
      summary: List review-safe DreamUP applications
      operationId: listDreamUPReviewApplications
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      responses:
        "200":
          description: Review-safe applications
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/dreamup/events/{eventId}/applications/{applicationId}:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      - name: applicationId
        in: path
        required: true
        schema: { type: string, minLength: 1, maxLength: 160 }
    get:
      tags: [Administrator Step-up]
      summary: Read one review-safe DreamUP application
      operationId: getDreamUPReviewApplication
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      responses:
        "200":
          description: Review-safe application and caller review
          content:
            application/json:
              schema: { type: object }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/admin/dreamup/events/{eventId}/applications/{applicationId}/reviews/me:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      - name: applicationId
        in: path
        required: true
        schema: { type: string, minLength: 1, maxLength: 160 }
    put:
      tags: [Administrator Step-up]
      summary: Save the caller's application review
      operationId: putDreamUPOwnReview
      description: Requires the high-risk `event.application.review` capability and a fresh five-minute administrator proof. Native Mini Program bearer callers require an application-target-bound one-shot token; the existing cookie website may use its session-bound durable proof.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/DreamUP-X-Reauthentication-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
        - $ref: "#/components/parameters/DreamUP-If-Match"
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: false }
      responses:
        "200":
          description: Saved own review
          content:
            application/json:
              schema: { type: object }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/admin/dreamup/events/{eventId}/applications/{applicationId}/admission-consensus:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      - name: applicationId
        in: path
        required: true
        schema: { type: string, minLength: 1, maxLength: 160 }
    get:
      tags: [Administrator Step-up]
      summary: Read three-administrator admission consensus
      operationId: getDreamUPAdmissionConsensus
      description: Requires the high-risk `event.application.approve_admission` capability and a fresh five-minute administrator proof. Native Mini Program bearer callers require an application-target-bound one-shot token; the existing cookie website may use its session-bound durable proof.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/DreamUP-X-Reauthentication-Token"
      responses:
        "200":
          description: Admission consensus
          content:
            application/json:
              schema: { type: object }

  /api/v1/admin/dreamup/events/{eventId}/applications/{applicationId}/admission-consensus/approval:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      - name: applicationId
        in: path
        required: true
        schema: { type: string, minLength: 1, maxLength: 160 }
    put:
      tags: [Administrator Step-up]
      summary: Add the caller's admission approval
      operationId: putDreamUPAdmissionApproval
      description: Requires the high-risk `event.application.approve_admission` capability and a fresh five-minute administrator proof. Native Mini Program bearer callers require an application-target-bound one-shot token; the existing cookie website may use its session-bound durable proof.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/DreamUP-X-Reauthentication-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
        - $ref: "#/components/parameters/DreamUP-If-Match"
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, maxProperties: 0 }
      responses:
        "200":
          description: Updated consensus
          content:
            application/json:
              schema: { type: object }
    delete:
      tags: [Administrator Step-up]
      summary: Withdraw the caller's admission approval
      operationId: deleteDreamUPAdmissionApproval
      description: Requires the high-risk `event.application.approve_admission` capability and a fresh five-minute administrator proof. Native Mini Program bearer callers require an application-target-bound one-shot token; the existing cookie website may use its session-bound durable proof.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/DreamUP-X-Reauthentication-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
        - $ref: "#/components/parameters/DreamUP-If-Match"
      responses:
        "200":
          description: Updated consensus
          content:
            application/json:
              schema: { type: object }

  /api/v1/admin/dreamup/events/{eventId}/applications/{applicationId}/decision:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      - name: applicationId
        in: path
        required: true
        schema: { type: string, minLength: 1, maxLength: 160 }
    post:
      tags: [Administrator Step-up]
      summary: Record a non-accept admission decision
      operationId: postDreamUPApplicationDecision
      description: Requires the high-risk `event.application.decide` capability and a fresh five-minute administrator proof. Native Mini Program bearer callers require an application-target-bound one-shot token; the existing cookie website may use its session-bound durable proof.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/DreamUP-X-Reauthentication-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
        - $ref: "#/components/parameters/DreamUP-If-Match"
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, additionalProperties: false }
      responses:
        "200":
          description: Updated application
          content:
            application/json:
              schema: { type: object }

  /api/v1/admin/dreamup/events/{eventId}/checkins/scan:
    parameters:
      - $ref: "#/components/parameters/DreamUPEventIdPath"
      - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
    post:
      tags: [Administrator Step-up]
      summary: Validate and consume a participant entry code
      operationId: postDreamUPCheckinScan
      description: Requires the high-risk `event.checkin.scan` capability and a fresh five-minute administrator proof. Native Mini Program bearer callers require an event-target-bound one-shot token; the existing cookie website may use its session-bound durable proof.
      security: [{ SessionCookie: [] }, { MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/DreamUP-X-Reauthentication-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
        - $ref: "#/components/parameters/DreamUP-If-Match"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token]
              properties:
                token: { type: string, pattern: "^[A-Za-z0-9_-]{43}$" }
              additionalProperties: false
      responses:
        "200":
          description: Check-in result without participant identity data
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "429": { $ref: "#/components/responses/TooManyRequests" }

  /healthz:
    get:
      tags: [Operational]
      summary: Process liveness
      description: |
        Reports that the process is alive and able to respond. This endpoint must
        not depend on downstream services so it remains useful during outages.
      operationId: getHealthz
      responses:
        "200":
          description: Process is alive
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
              example:
                status: ok

  /readyz:
    get:
      tags: [Operational]
      summary: Process readiness
      description: |
        Reports whether the process is ready to serve traffic. All registered
        readiness checks must pass. Failures return 503 without leaking
        dependency names or error text.
      operationId: getReadyz
      responses:
        "200":
          description: Process is ready
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
              example:
                status: ready
        "503":
          description: At least one readiness check failed
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
              example:
                status: not ready

  /api/v1/registrations/form-intents:
    post:
      tags: [Registration]
      summary: Issue a short-lived registration form intent
      description: |
        Creates a one-use, server-timed intent bound to digests of the current
        user agent, trusted client network, and optional server-issued risk
        device. The registration mutation rejects missing, expired, replayed,
        or implausibly immediate intents before provisioning an identity.
      operationId: issueRegistrationFormIntent
      parameters:
        - $ref: "#/components/parameters/RegistrationOrigin"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
      responses:
        "201":
          description: Short-lived form intent issued.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/RegistrationFormIntentResponse"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "413": {$ref: "#/components/responses/BadRequest"}
        "415": {$ref: "#/components/responses/BadRequest"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "502": {$ref: "#/components/responses/ProviderUnavailable"}

  /api/v1/registrations:
    post:
      tags: [Registration]
      summary: Create a pending public account registration
      description: |
        Creates a ZITADEL human user with a United Pass-controlled user ID and
        transactionally pre-creates the matching local pending identity link
        and consumer persona. The account cannot authenticate until its email
        is verified. Passwords are write-only and never logged. Duplicate
        account names and emails share one generic conflict response. This
        legacy browser surface is absent unless
        `UP_PUBLIC_REGISTRATION_ENABLED=true`; enabling Mini Program
        registration does not expose it.
      operationId: createRegistration
      parameters:
        - $ref: "#/components/parameters/RegistrationOrigin"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/RegistrationCreateRequest"}
      responses:
        "201":
          description: Pending account created and verification email requested.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/RegistrationCreateResponse"}
        "403":
          description: Origin rejected, or objective abuse signals require step-up.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ErrorResponse"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "413": {$ref: "#/components/responses/BadRequest"}
        "415": {$ref: "#/components/responses/BadRequest"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "502": {$ref: "#/components/responses/ProviderUnavailable"}

  /api/v1/registrations/email/verify:
    post:
      tags: [Registration]
      summary: Verify a pending account email and activate the account
      description: |
        The browser reads userId, code and requestId from the URL fragment,
        erases the fragment from history, and sends them in this JSON body.
        Verification codes therefore never enter HTTP or reverse-proxy access
        logs. Provider verification precedes the transactional local activation.
        This shared pending-account lifecycle is present when either legacy
        browser registration or Mini Program registration is enabled. It
        cannot create an account and does not expose the legacy create route.
      operationId: verifyRegistrationEmail
      parameters:
        - $ref: "#/components/parameters/RegistrationOrigin"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/RegistrationVerifyRequest"}
      responses:
        "200":
          description: Email verified and local account activated.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/RegistrationVerifyResponse"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "413": {$ref: "#/components/responses/BadRequest"}
        "415": {$ref: "#/components/responses/BadRequest"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "502": {$ref: "#/components/responses/ProviderUnavailable"}

  /api/v1/registrations/email/resend:
    post:
      tags: [Registration]
      summary: Resend the pending registration email verification
      description: |
        Accepts only the short-lived opaque registration token. Redis stores
        only its SHA-256 hash mapped to the provider user ID and validated
        authorization request ID; callers cannot submit or select a user ID.
        This shared pending-account lifecycle is present when either legacy
        browser registration or Mini Program registration is enabled. The
        opaque token cannot create or select an arbitrary account.
      operationId: resendRegistrationEmail
      parameters:
        - $ref: "#/components/parameters/RegistrationOrigin"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/RegistrationResendRequest"}
      responses:
        "202":
          description: A fresh verification email was requested.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/RegistrationResendResponse"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "413": {$ref: "#/components/responses/BadRequest"}
        "415": {$ref: "#/components/responses/BadRequest"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "502": {$ref: "#/components/responses/ProviderUnavailable"}

  /api/v1/registrations/wechat:
    post:
      tags: [WeChat Mini Program, Registration]
      summary: Create a pending account with mandatory verified WeChat identity and phone binding
      description: |
        This Mini Program-only registration flow accepts only fresh one-time
        `wx.login` and `getPhoneNumber` codes. The API verifies them server-side
        and durably reserves a retryable pending identity before provider
        creation. Activation remains fail-closed until the verified phone,
        primary provider link, WeChat identity link and consumer persona are
        all complete. The route is absent unless both
        `UP_WECHAT_MINIPROGRAM_ENABLED=true` and the independent
        `UP_WECHAT_MINIPROGRAM_REGISTRATION_ENABLED=true` gate are set. It does
        not depend on, or enable, the legacy `UP_PUBLIC_REGISTRATION_ENABLED`
        browser surface. Requests must carry the exact
        `X-UnitedPass-Client: dreamup-miniprogram` transport marker; the marker
        grants no authority and a missing or incorrect value fails closed.
      operationId: createWeChatRegistration
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/WeChatRegistrationCreateRequest"}
      responses:
        "201":
          description: Pending account reserved and provider verification requested; email verification is still required.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/RegistrationCreateResponse"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "413": {$ref: "#/components/responses/BadRequest"}
        "415": {$ref: "#/components/responses/BadRequest"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "502": {$ref: "#/components/responses/ProviderUnavailable"}

  /api/v1/admin/registration-defense/blocks/revoke:
    post:
      tags: [Admin Registration Defense]
      summary: Revoke one false-positive registration abuse block
      description: |
        Requires a browser session, matching CSRF token, an active system
        `super_admin` or `top_admin` binding, an active/non-offboarding
        principal, and authoritative `policy.manage` plus `audit.read`
        capabilities. It also consumes a single-use reauthentication grant
        for `registration.abuse.unblock`, bound to `<dimension>_<sha256>`, and
        is rate limited per administrator. The request accepts one explicit
        raw IP address or a server-issued risk-device token. Because that token
        is HttpOnly, device recovery may instead use the exact lowercase SHA-256
        digest already present in the abuse audit. IP recovery always
        canonicalizes and hashes the raw address at the HTTP boundary. Redis
        prefixes and arbitrary keys are never accepted. The authorized request
        and terminal outcome are written to the durable security audit log.
        Audit targets use a purpose-separated HMAC, so neither the raw
        fingerprint nor the enumerable SHA-256 of an IPv4 address is stored.
      operationId: revokeRegistrationAbuseBlock
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/RegistrationAbuseBlockRevokeRequest"}
      responses:
        "204": {description: The selected block dimension and associated strike state were revoked.}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "413": {$ref: "#/components/responses/BadRequest"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/auth/sessions:
    post:
      tags: [Session]
      summary: Establish a browser session
      description: |
        Authenticates the caller with a unified identifier (account name or email)
        and password, then establishes a browser session.

        On success the server issues two cookies:
          * `up_session` — HttpOnly, Secure, SameSite session cookie.
          * `up_csrf`   — CSRF token cookie readable by browser JavaScript.

        Both cookies are returned via `Set-Cookie`. The browser must not receive
        raw authentication-provider Refresh Tokens or long-lived tokens.

        When the account requires multi-factor authentication, the server responds
        `202` with an opaque `mfaToken` and the list of available methods instead
        of establishing the session. The MFA challenge is completed via
        `POST /api/v1/auth/sessions/mfa`.

        Authentication failures return generic errors that do not reveal whether
        the account, email or phone number exists. Passwords are never logged.

        Rate limited. Repeated failures return `429` with a `Retry-After` header.
        Objective, server-observed abuse signals may instead return `403` with
        `error.code=step_up_required`. No name, display name, writing style, or
        other subjective content participates in that decision.
      operationId: createSession
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
            examples:
              credentials:
                value:
                  identifier: zhixing.lin
                  password: user-entered-password
                  remember: true
              resumeAuthorization:
                value:
                  identifier: zhixing.lin
                  password: user-entered-password
                  remember: false
                  resumeRequestId: req_01ABCdefGHIjkl
      responses:
        "204":
          description: |
            Session established successfully. No response body. The
            `up_session` and `up_csrf` cookies are set.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
            Set-Cookie:
              $ref: "#/components/headers/Set-Session-Cookies"
        "202":
          description: |
            Multi-factor authentication is required before the session can be
            established. The caller must complete the challenge at
            `POST /api/v1/auth/sessions/mfa`.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MFARequiredResponse"
              example:
                status: mfa_required
                mfaToken: opaque_mfa_token
                availableMethods: [totp, passkey, recovery_code]
                expiresAt: "2026-08-05T12:05:30Z"
        "400":
          description: Malformed request body or missing required fields.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: validation.failed
                  message: "请求参数有误，请检查后重试。"
                  requestId: req_0123456789abcdef
                  fieldErrors:
                    - field: identifier
                      message: "该字段不能为空。"
        "401":
          description: |
            Authentication was rejected. Invalid credentials and provider-side
            authorization faults use the same generic response so the result
            does not reveal whether the identifier corresponds to an existing
            account.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: session.unauthenticated
                  message: "账户名或密码错误。"
                  requestId: req_0123456789abcdef
        "403":
          description: Objective abuse signals require a single-use step-up challenge.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ErrorResponse"}
        "413":
          description: Request body exceeds the configured maximum size.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: request.too_large
                  message: "请求体过大。"
                  requestId: req_0123456789abcdef
        "429":
          description: |
            Too many login attempts. The caller must wait before retrying.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
            Retry-After:
              $ref: "#/components/headers/Retry-After"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: auth.rate_limited
                  message: "尝试次数过多，请稍后再试。"
                  requestId: req_0123456789abcdef
        "500":
          description: Unexpected server failure.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: internal.unexpected
                  message: "服务暂时无法处理该请求。"
                  requestId: req_0123456789abcdef

  /api/v1/auth/step-up:
    post:
      tags: [Session]
      summary: Complete an objective registration/login abuse step-up
      description: |
        Completes the single-use challenge returned as
        `error.stepUp.challengeToken`. `automation_cost` is a computational
        anti-automation cost only and is not human verification. High-risk
        `interactive_captcha` requires a configured interactive provider; the
        server fails closed with `503` when no provider is configured. Success
        sets a short-lived HttpOnly `up_device_trust` cookie. Tokens are stored
        only by SHA-256 hash and cannot be replayed.
      operationId: completeAuthRiskStepUp
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/StepUpCompletionRequest"}
      responses:
        "204":
          description: Challenge consumed and short-lived device trust issued.
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "503":
          description: Interactive step-up provider or challenge store unavailable; fail closed.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/ErrorResponse"}

  /api/v1/auth/miniprogram/sessions:
    post:
      tags: [WeChat Mini Program]
      summary: Establish a native Mini Program session with United Pass credentials
      description: |
        Native-only password login. Browser-shaped requests are rejected and
        the exact `X-UnitedPass-Client: dreamup-miniprogram` marker is required;
        the marker is transport routing only and grants no authority. Successful
        authentication returns a 256-bit opaque bearer for process memory only.
        The server persists only its SHA-256 hash and binds the record to the
        native client kind and the user's security epoch. No cookie is read or
        written, the fixed lifetime is 30 minutes, and remember mode is rejected.
        An MFA challenge is bound to this native channel.
      operationId: createMiniProgramPasswordSession
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/MiniProgramLoginRequest"}
      responses:
        "200":
          description: Short-lived native bearer established; no Set-Cookie header is emitted.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/MiniProgramSessionResponse"}
        "202":
          description: MFA is required; complete only through the matching native MFA route.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/MFARequiredResponse"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "404": {$ref: "#/components/responses/NotFound"}
        "415": {$ref: "#/components/responses/BadRequest"}
        "429": {$ref: "#/components/responses/TooManyRequests"}

  /api/v1/auth/miniprogram/sessions/mfa:
    post:
      tags: [WeChat Mini Program]
      summary: Complete a native Mini Program password-login TOTP challenge
      description: |
        Completes only an MFA challenge minted by the native password-login
        route. Challenges from browser login fail closed. Only TOTP is accepted;
        passkey-only or recovery-only accounts must use the trusted browser flow.
      operationId: completeMiniProgramPasswordSessionMfa
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/MiniProgramMFAChallengeRequest"}
      responses:
        "200":
          description: Short-lived native bearer established; no Set-Cookie header is emitted.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/MiniProgramSessionResponse"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "404": {$ref: "#/components/responses/NotFound"}
        "415": {$ref: "#/components/responses/BadRequest"}
        "429": {$ref: "#/components/responses/TooManyRequests"}

  /api/v1/auth/wechat/onboarding:
    post:
      tags: [WeChat Mini Program]
      summary: Begin explicit WeChat account onboarding or restore an existing link
      description: |
        Exchanges a fresh `wx.login` code only on the server. A
        `getPhoneNumber` code may accompany the same Mini Program action; it is
        best effort and its absence or rejection does not create another client
        flow. An already-linked active account receives a normal short-lived
        native bearer. An unlinked WeChat identity receives only a restricted,
        single-use onboarding token, never an account session. An exact linked
        pending reservation may re-enter only to reconcile that same pending
        user after a prior provider/token-store interruption. OpenID, UnionID,
        `session_key`, decoded phone data and provider credentials are never
        returned to the Mini Program. All three onboarding routes are absent
        unless `UP_WECHAT_MINIPROGRAM_ENABLED`,
        `UP_WECHAT_MINIPROGRAM_REGISTRATION_ENABLED` and the independent,
        default-off `UP_WECHAT_MINIPROGRAM_ONBOARDING_ENABLED` gate are true.
      operationId: beginWeChatMiniProgramOnboarding
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/WeChatOnboardingBeginRequest"}
      responses:
        "200":
          description: The WeChat identity was already linked and a native bearer was established.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/MiniProgramSessionResponse"}
        "202":
          description: Account details must be completed before any normal session is issued.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/WeChatOnboardingRequiredResponse"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "404": {$ref: "#/components/responses/NotFound"}
        "413": {$ref: "#/components/responses/BadRequest"}
        "415": {$ref: "#/components/responses/BadRequest"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "502": {$ref: "#/components/responses/ProviderUnavailable"}

  /api/v1/auth/wechat/onboarding/complete:
    post:
      tags: [WeChat Mini Program]
      summary: Complete United Pass account details for a verified WeChat identity
      description: |
        Consumes a restricted onboarding challenge. A new email creates a
        pending account through the existing provider and email-verification
        lifecycle. An email already owned by exactly one active account treats
        `password` only as that account's current credential proof; it never
        changes the password or copies provisional profile data. Accounts with
        MFA receive a dedicated onboarding MFA challenge before the WeChat link
        can be committed. Global registration buckets are consumed only after
        the restricted challenge is claimed. The exact existing-email password
        error is available only inside this rate-limited, verified WeChat workflow.
      operationId: completeWeChatMiniProgramOnboarding
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/WeChatOnboardingCompleteRequest"}
      responses:
        "200":
          description: Existing account authentication and binding completed; a native bearer was established.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/MiniProgramSessionResponse"}
        "201":
          description: A pending new account was created and email verification is required.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/WeChatOnboardingVerificationRequiredResponse"}
        "202":
          description: The existing account requires its registered second factor.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/WeChatOnboardingMFARequiredResponse"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/WeChatOnboardingCompleteUnauthorized"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/WeChatOnboardingConflict"}
        "413": {$ref: "#/components/responses/BadRequest"}
        "415": {$ref: "#/components/responses/BadRequest"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "502": {$ref: "#/components/responses/ProviderUnavailable"}

  /api/v1/auth/wechat/onboarding/mfa:
    post:
      tags: [WeChat Mini Program]
      summary: Complete the existing account's MFA before linking WeChat
      description: |
        Accepts only the dedicated single-use onboarding MFA token. Successful
        provider MFA is followed by the allowlisted identity-link transaction;
        no provisional display, email, role or permission field overwrites the
        existing account.
      operationId: completeWeChatMiniProgramOnboardingMFA
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/WeChatOnboardingMFARequest"}
      responses:
        "200":
          description: MFA and account binding completed; a native bearer was established.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/MiniProgramSessionResponse"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/WeChatOnboardingMFAUnauthorized"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/WeChatOnboardingConflict"}
        "413": {$ref: "#/components/responses/BadRequest"}
        "415": {$ref: "#/components/responses/BadRequest"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "502": {$ref: "#/components/responses/ProviderUnavailable"}

  /api/v1/auth/wechat/sessions:
    post:
      tags: [WeChat Mini Program]
      summary: Establish a United Pass session from a verified WeChat Mini Program code
      description: |
        Exchanges a fresh `wx.login` one-time code only on the server and
        resolves it to an existing explicit WeChat identity link. It is also
        the only cold-start recovery path: success mints a new short-lived
        native bearer and failure leaves the client logged out. No cookie or
        provider credential is returned. This route is absent unless the
        server-side WeChat Mini Program switch is enabled.
      operationId: createWeChatMiniProgramSession
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/WeChatLoginRequest"}
      responses:
        "200":
          description: Short-lived native bearer established; no Set-Cookie header is emitted.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/MiniProgramSessionResponse"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "413": {$ref: "#/components/responses/BadRequest"}
        "415": {$ref: "#/components/responses/BadRequest"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "502": {$ref: "#/components/responses/ProviderUnavailable"}

  /api/v1/me/wechat/phone:
    post:
      tags: [WeChat Mini Program]
      summary: Verify and bind the current user's WeChat-authorized phone
      description: |
        Requires the current native bearer and exact Mini Program marker. Both
        one-time codes are independently exchanged by the server; the resolved
        WeChat identity must already link to the current user, preventing a
        cross-account phone bind.
      operationId: bindWeChatMiniProgramPhone
      security: [{MiniProgramBearer: []}]
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/WeChatPhoneBindRequest"}
      responses:
        "200":
          description: Phone proof verified and reflected on the current account.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/WeChatPhoneBindResponse"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "413": {$ref: "#/components/responses/BadRequest"}
        "415": {$ref: "#/components/responses/BadRequest"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "502": {$ref: "#/components/responses/ProviderUnavailable"}

  /api/v1/auth/sessions/mfa:
    post:
      tags: [Session]
      summary: Complete a multi-factor authentication challenge
      description: |
        Submits a verification code or assertion to complete an MFA challenge
        started by `POST /api/v1/auth/sessions`.

        The `mfaToken` is the opaque token returned in the `202` login response.
        It is short-lived and single-use per attempt. Excessive failed attempts
        result in temporary lockout (`429`).

        On success the server issues the `up_session` and `up_csrf` cookies,
        identical to a successful password-only login.

        Rate limited.
      operationId: completeSessionMfa
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MFAChallengeRequest"
            example:
              mfaToken: opaque_mfa_token
              method: totp
              code: "123456"
      responses:
        "204":
          description: |
            MFA challenge completed and session established successfully. No
            response body. The `up_session` and `up_csrf` cookies are set.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
            Set-Cookie:
              $ref: "#/components/headers/Set-Session-Cookies"
        "400":
          description: Malformed request body or missing required fields.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: validation.failed
                  message: "请求参数有误，请检查后重试。"
                  requestId: req_0123456789abcdef
                  fieldErrors:
                    - field: code
                      message: "请输入 6 位数字验证码。"
        "401":
          description: |
            The MFA token is invalid, expired, or the submitted code is wrong.
            The response is generic and does not reveal which condition failed.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: auth.mfa_challenge_failed
                  message: "验证失败，请检查后重试。"
                  requestId: req_0123456789abcdef
        "429":
          description: |
            Too many MFA attempts. The challenge is temporarily locked.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
            Retry-After:
              $ref: "#/components/headers/Retry-After"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: auth.rate_limited
                  message: "尝试次数过多，请稍后再试。"
                  requestId: req_0123456789abcdef
        "500":
          description: Unexpected server failure.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: internal.unexpected
                  message: "服务暂时无法处理该请求。"
                  requestId: req_0123456789abcdef

  /api/v1/dreamup/mobile/assertions:
    post:
      tags: [Account]
      summary: Mint one request-bound DreamUP participant assertion
      operationId: createDreamUPMobileAssertion
      description: |
        Requires the explicit native bearer and Mini Program marker. The
        returned assertion is short-lived and bound to the exact method, path,
        body digest and selected concurrency/idempotency headers. It is not a
        United Pass session credential.
      security: [{ MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [method, path, body, ifMatch, idempotencyKey]
              properties:
                method: { type: string, enum: [GET, PUT, POST, PATCH, DELETE] }
                path: { type: string, minLength: 1, maxLength: 2048 }
                body:
                  type: string
                  maxLength: 65536
                  description: >-
                    UTF-8 request payload. Limits use encoded UTF-8 bytes: 8 KiB
                    for feedback/sponsorship, seat confirmation and admission
                    entry-code routes; 16 KiB for team routes; and 64 KiB for
                    other allowlisted routes.
                ifMatch: { type: string, maxLength: 128 }
                idempotencyKey: { type: string, maxLength: 128 }
      responses:
        "201":
          description: One request-bound assertion.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DreamUPMobileAssertionResponse" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "413": { $ref: "#/components/responses/BadRequest" }
        "415": { $ref: "#/components/responses/BadRequest" }
        "422": { $ref: "#/components/responses/FieldValidationFailed" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }

  /api/v1/dreamup/mobile/resume-upload-assertions:
    post:
      tags: [Account]
      summary: Mint one request-bound DreamUP resume-upload assertion
      operationId: createDreamUPResumeUploadAssertion
      description: The file bytes stay outside United Pass; the assertion binds bounded metadata and the caller-supplied SHA-256 digest.
      security: [{ MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [method, path, fileName, contentType, byteSize, bodySha256]
              properties:
                method: { type: string, const: PUT }
                path: { type: string, minLength: 1, maxLength: 2048 }
                fileName:
                  type: string
                  minLength: 1
                  maxLength: 255
                  x-max-utf8-bytes: 255
                  description: Resume filename encoded as valid UTF-8, limited to 255 UTF-8 bytes (not characters).
                contentType:
                  type: string
                  enum: [application/pdf, application/msword, application/vnd.openxmlformats-officedocument.wordprocessingml.document]
                byteSize: { type: integer, format: int64, minimum: 1, maximum: 5242880 }
                bodySha256:
                  type: string
                  pattern: "^[0-9a-f]{64}$"
                  description: Canonical lowercase SHA-256 hex digest of the bytes uploaded directly to DreamUP.
      responses:
        "201":
          description: One request-bound resume upload assertion.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DreamUPMobileAssertionResponse" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "413": { $ref: "#/components/responses/BadRequest" }
        "415": { $ref: "#/components/responses/BadRequest" }
        "422": { $ref: "#/components/responses/FieldValidationFailed" }
        "429": { $ref: "#/components/responses/RateLimited" }
        "500": { $ref: "#/components/responses/InternalError" }

  /api/v1/auth/qr/challenges:
    post:
      tags: [Session]
      summary: Begin a browser QR-login handoff
      operationId: beginQRLoginChallenge
      description: Browser-only same-origin mutation. The receiver proof is returned only as a scoped HttpOnly cookie; the JSON challenge ID is safe to render as a QR code.
      responses:
        "201":
          description: QR-login challenge created.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [challengeId, expiresInSeconds]
                properties:
                  challengeId: { type: string, pattern: "^[A-Za-z0-9_-]{20,255}$" }
                  expiresInSeconds: { type: integer, minimum: 1, maximum: 600 }
        "429": { $ref: "#/components/responses/TooManyRequests" }
        "503": { $ref: "#/components/responses/ProviderUnavailable" }

  /api/v1/auth/qr/challenges/{challengeId}/consume:
    post:
      tags: [Session]
      summary: Consume a browser QR-login handoff
      operationId: consumeQRLoginChallenge
      description: Browser-only same-origin mutation requiring the independent scoped HttpOnly receiver cookie.
      parameters:
        - $ref: "#/components/parameters/QRChallengeIdPath"
      responses:
        "200":
          description: Browser session established; normal session and CSRF cookies are emitted.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [status, csrfToken]
                properties:
                  status: { type: string, const: authenticated }
                  csrfToken: { type: string, minLength: 1 }
        "202":
          description: Challenge remains pending.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [status]
                properties:
                  status: { type: string, const: pending }
        "404": { $ref: "#/components/responses/NotFound" }
        "503": { $ref: "#/components/responses/ProviderUnavailable" }

  /api/v1/auth/qr/challenges/{challengeId}/approve:
    post:
      tags: [Session]
      summary: Approve a scanned QR-login handoff from the Mini Program
      operationId: approveQRLoginChallenge
      description: Native bearer and marker only. No browser session cookie, receiver proof or CSRF token is accepted as the Mini Program credential.
      security: [{ MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/QRChallengeIdPath"
        - $ref: "#/components/parameters/X-MiniProgram-Client"
      responses:
        "200":
          description: Challenge approved for its waiting browser receiver.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [status]
                properties: { status: { type: string, const: approved } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "503": { $ref: "#/components/responses/ProviderUnavailable" }

  /api/v1/auth/session:
    delete:
      tags: [Session]
      summary: Revoke the current session (logout)
      description: |
        Browser callers authenticate with `up_session` plus matching CSRF and
        receive clearing cookies. Native callers authenticate with the explicit
        Mini Program bearer and marker; no cookie or CSRF value participates.
      operationId: deleteSession
      security:
        - SessionCookie: []
        - MiniProgramBearer: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      responses:
        "204":
          description: |
            Session revoked successfully. No response body. The `up_session`
            and `up_csrf` cookies are cleared.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
            Set-Cookie:
              $ref: "#/components/headers/Clear-Session-Cookies"
        "401":
          description: No valid session cookie was supplied.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: auth.unauthenticated
                  message: "请先登录后再继续。"
                  requestId: req_0123456789abcdef
        "403":
          description: |
            CSRF validation failed. The `X-CSRF-Token` header is missing or does
            not match the `up_csrf` cookie.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: csrf.validation_failed
                  message: "跨站请求验证失败，请刷新页面后重试。"
                  requestId: req_0123456789abcdef
        "500":
          description: Unexpected server failure.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: internal.unexpected
                  message: "服务暂时无法处理该请求。"
                  requestId: req_0123456789abcdef

  /api/v1/me:
    get:
      tags: [Account]
      summary: Get the current user
      description: |
        Returns the profile of the authenticated browser-cookie or native
        Mini Program bearer session.

        The stable United Pass `userId` is the identity primary key. It is not
        an email, phone number or external Provider identifier, and it does not
        change when an external user becomes an employee.

        `employeeProfile` is optional and present only when the user has an
        active employee persona. When an external user is linked to an employee
        profile, the same `userId` is preserved and the consumer persona
        remains.

        Responses must not be cached.
      operationId: getCurrentUser
      security:
        - SessionCookie: []
        - MiniProgramBearer: []
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      responses:
        "200":
          description: Current user profile.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
            Cache-Control:
              description: Prevents the browser and intermediaries from caching the response.
              schema:
                type: string
                enum: [no-store]
                default: no-store
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CurrentUser"
              example:
                userId: usr_01HQFGXKN8H2C4JVMW7K3R4D8Z
                displayName: 林知行
                nickname: 知行
                avatarUrl: https://cdn.example.com/avatars/usr_01HQFGXKN8H2C4JVMW7K3R4D8Z.webp
                email: zhixing.lin@example.com
                phoneMasked: "+86 138****0123"
                personas: [consumer, employee]
                employeeProfile:
                  employeeId: EMP-10032
                  departmentName: 平台架构部
                  title: 高级工程师
        "401":
          description: No valid session cookie was supplied.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: auth.unauthenticated
                  message: "请先登录后再继续。"
                  requestId: req_0123456789abcdef
        "500":
          description: Unexpected server failure.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: internal.unexpected
                  message: "服务暂时无法处理该请求。"
                  requestId: req_0123456789abcdef

  /api/v1/me/miniprogram/profile:
    post:
      tags: [Account]
      summary: Update self-service profile from the DreamUP Mini Program
      description: |
        Transport adaptation for `wx.request`, which does not support PATCH.
        This operation has the same self-service semantics as profile update,
        but requires an explicit native bearer, no `Origin` header and the
        exact `X-UnitedPass-Client: dreamup-miniprogram` marker. The marker
        does not authorize the request; it only selects the native transport.
      operationId: updateCurrentProfileFromMiniProgram
      security: [{ MiniProgramBearer: [] }]
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [displayName]
              properties:
                displayName: {type: string, minLength: 1, maxLength: 64}
                nickname: {type: string, maxLength: 64}
      responses:
        "204": {description: Profile updated}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/me/email-change:
    post:
      tags: [Account]
      summary: Send a verification code to a new primary login email
      description: Requires a single-use, current-session `account.email.change` reauthentication grant before any provider or mail side effect.
      operationId: beginAccountEmailChange
      security: [{ SessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties: { email: { type: string, format: email, maxLength: 254 } }
              additionalProperties: false
      responses:
        "202": { description: Verification sent }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "429": { $ref: "#/components/responses/TooManyRequests" }

  /api/v1/me/email-change/verify:
    post:
      tags: [Account]
      summary: Verify and activate a new primary login email
      operationId: verifyAccountEmailChange
      security: [{ SessionCookie: [] }]
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [requestId, code]
              properties:
                requestId: { type: string, pattern: "^email_change_[0-9a-f]{32}$" }
                code:
                  type: string
                  minLength: 6
                  maxLength: 6
                  pattern: "^[A-Z0-9]{6}$"
                  description: Case-sensitive email verification code containing uppercase ASCII letters and digits.
                  example: A1B2C3
              additionalProperties: false
      responses:
        "200": { description: New primary login email activated }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/FieldValidationFailed" }
        "429": { $ref: "#/components/responses/TooManyRequests" }

  /api/v1/me/permissions:
    get:
      tags: [Account]
      summary: Get current user permission capabilities
      description: |
        Returns the permission capabilities computed for the authenticated user
        from authoritative backend attributes (employee status, department,
        ownership, etc.) and Cerbos policy decisions.

        These capabilities are a user-experience summary only — they drive
        frontend navigation visibility and control availability. Every
        protected backend operation must independently authenticate, load
        authoritative attributes, authorize via Cerbos, enforce the decision
        and audit the operation. Frontend permission checks are not security
        controls.

        Responses must not be cached.
      operationId: getCurrentPermissions
      security:
        - SessionCookie: []
      responses:
        "200":
          description: Current user permission capabilities.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
            Cache-Control:
              description: Prevents the browser and intermediaries from caching the response.
              schema:
                type: string
                enum: [no-store]
                default: no-store
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PermissionCapabilities"
              example:
                userRead: true
                userDisable: false
                employeeManage: true
                employeeOffboard: false
                departmentManage: false
                applicationRead: true
                applicationManage: false
                applicationSecretRotate: false
                policyRead: true
                policyManage: false
                policyPublish: false
                auditRead: true
                auditExport: false
                providerRead: true
                providerManage: false
        "401":
          description: No valid session cookie was supplied.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: auth.unauthenticated
                  message: "请先登录后再继续。"
                  requestId: req_0123456789abcdef
        "500":
          description: Unexpected server failure.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                error:
                  code: internal.unexpected
                  message: "服务暂时无法处理该请求。"
                  requestId: req_0123456789abcdef

  /api/v1/legal-documents:
    get:
      tags: [Privacy and Legal]
      summary: List current controlled legal-document publications
      description: Returns public status only. Approval references and approver identities are never exposed.
      operationId: listLegalDocuments
      responses:
        "200":
          description: Current effective or scheduled publications whose content digest was accepted by the release manifest.
          headers:
            Cache-Control:
              schema: {type: string, const: no-store}
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LegalPublicationList"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/data-exports:
    post:
      tags: [Privacy and Legal]
      summary: Request a short-lived personal-data export
      description: Consumes a single-use `account.data_export` reauthentication grant. Only one pending or processing export is allowed per user.
      operationId: requestPersonalDataExport
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      responses:
        "202":
          description: Export job accepted.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/PersonalDataExport"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/me/data-exports/{exportId}:
    get:
      tags: [Privacy and Legal]
      summary: Read an owned personal-data export job
      description: Foreign and unknown export IDs return the same 404 response.
      operationId: getPersonalDataExport
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/PersonalDataExportIdPath"
      responses:
        "200":
          description: Current job status and a download URL only while the completed artifact remains live.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/PersonalDataExport"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/me/data-exports/{exportId}/download:
    get:
      tags: [Privacy and Legal]
      summary: Download an owned, unexpired personal-data JSON artifact
      operationId: downloadPersonalDataExport
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/PersonalDataExportIdPath"
      responses:
        "200":
          description: JSON attachment. The artifact is physically purged after its 15-minute lifetime.
          headers:
            Cache-Control:
              schema: {type: string, const: no-store}
          content:
            application/json:
              schema: {type: object, additionalProperties: true}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/me/account-deletion:
    get:
      tags: [Privacy and Legal]
      summary: Read the current account-deletion lifecycle
      operationId: getAccountDeletion
      security:
        - SessionCookie: []
      responses:
        "200":
          description: "Current deletion request, or `{status: none}`."
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AccountDeletionState"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "500": {$ref: "#/components/responses/InternalError"}
    post:
      tags: [Privacy and Legal]
      summary: Start a cancellable 30-day account-deletion cooling period
      description: Consumes a single-use `account.delete` reauthentication grant. Provider deletion and local anonymisation run durably after the cooling period.
      operationId: requestAccountDeletion
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      responses:
        "202":
          description: Deletion request accepted.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AccountDeletion"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalError"}
    delete:
      tags: [Privacy and Legal]
      summary: Cancel a deletion request during its cooling period
      operationId: cancelAccountDeletion
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
      responses:
        "200":
          description: Deletion request cancelled.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AccountDeletion"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/auth/reauthentication:
    post:
      tags: [Reauthentication]
      summary: Reauthenticate the current session user for a high-risk action
      description: |
        Verifies the current session user's password for the declared action
        (and optional target resource). Returns the grant immediately, or a
        `202` challenge when a second factor must complete the verification.
        Challenges are random opaque IDs bound to user + session + action,
        with a short TTL and attempt limit. Redis loss only invalidates
        challenges (fail closed).
      operationId: requestReauthentication
      security:
        - SessionCookie: []
        - MiniProgramBearer: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReauthenticationRequest"
      responses:
        "200":
          description: Reauthentication succeeded; single-use grant issued.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReauthenticationGrantResponse"
        "202":
          description: Second factor required to complete reauthentication.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReauthenticationChallengeResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/security:
    get:
      tags: [Account Security]
      summary: Read provider-authoritative account security state
      operationId: getAccountSecuritySummary
      security:
        - SessionCookie: []
      responses:
        "200":
          description: Password, TOTP, individual passkeys and recovery-code capability.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SecuritySummary"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "503":
          $ref: "#/components/responses/ProviderForbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/security/password:
    post:
      tags: [Account Security]
      summary: Change the current user's password after step-up
      description: |
        Consumes an `account.password.change` grant. The reauthentication
        request is the sole current-password proof; this body accepts only
        `newPassword`. On success the current credential is rotated and all
        older-generation sessions are revoked (ADR-0007). Browser callers get
        rotated HttpOnly cookies and `204`; native bearer callers get the fresh
        bearer once in a `200` response.
      operationId: changeOwnPassword
      security:
        - SessionCookie: []
        - MiniProgramBearer: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordChangeRequest"
      responses:
        "200":
          description: Password changed and the native bearer was rotated; no cookie is emitted.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/MiniProgramSessionResponse"}
        "204":
          description: Password changed and the current session was rotated.
          headers:
            Set-Cookie:
              $ref: "#/components/headers/Set-Session-Cookies"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          description: Session invalid or committed outcome requires re-login; cookies are cleared where applicable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/security/totp/enrollment:
    post:
      tags: [Account Security]
      summary: Begin TOTP enrollment
      description: Consumes an `account.totp.enroll` grant and returns secret material exactly once.
      operationId: beginTotpEnrollment
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      responses:
        "200":
          description: Pending provider registration and single-use enrollment capability. Never cached.
          headers:
            Cache-Control:
              description: Always `no-store` because the response contains a TOTP secret.
              schema:
                type: string
                const: no-store
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TotpEnrollmentResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "503":
          $ref: "#/components/responses/ProviderForbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/security/totp/enrollment/confirm:
    post:
      tags: [Account Security]
      summary: Confirm TOTP enrollment
      description: Claims the user/session-bound single-use enrollment capability and verifies the code.
      operationId: confirmTotpEnrollment
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TotpEnrollmentConfirmRequest"
      responses:
        "200":
          description: TOTP registration confirmed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TotpEnrollmentConfirmation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "503":
          $ref: "#/components/responses/ProviderForbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/security/totp/enrollment/cancel:
    post:
      tags: [Account Security]
      summary: Settle an abandoned TOTP enrollment
      description: |
        Claims the user/session-bound enrollment capability and removes the
        provider-side pending registration. No second reauthentication grant
        is accepted or required. Stable provider not-found is already settled.
      operationId: cancelTotpEnrollment
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TotpEnrollmentCancelRequest"
      responses:
        "204":
          description: Pending registration removed or already absent.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "503":
          $ref: "#/components/responses/ProviderForbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/security/totp:
    delete:
      tags: [Account Security]
      summary: Remove the current user's TOTP factor
      description: Consumes an `account.totp.remove` grant and returns provider-authoritative readback.
      operationId: removeTotp
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      responses:
        "200":
          description: Fresh provider-authoritative security summary.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SecuritySummary"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "503":
          $ref: "#/components/responses/ProviderForbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/sessions:
    get:
      tags: [Session]
      summary: List the current user's live sessions
      operationId: listOwnSessions
      security:
        - SessionCookie: []
        - MiniProgramBearer: []
      parameters:
        - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      responses:
        "200":
          description: Provider-independent United Pass session inventory.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UserSession"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags: [Session]
      summary: Revoke all other sessions of the current user
      description: Preserves the caller's current session. Session + CSRF is the complete gate; no reauthentication grant is accepted.
      operationId: revokeAllOtherOwnSessions
      security:
        - SessionCookie: []
        - MiniProgramBearer: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      responses:
        "200":
          description: Number of other sessions revoked.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RevokedSessionCount"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/sessions/{sessionId}:
    delete:
      tags: [Session]
      summary: Revoke one other session of the current user
      description: Unknown, foreign and already-removed identifiers share the stable non-enumerating 404 response.
      operationId: revokeOwnSession
      security:
        - SessionCookie: []
        - MiniProgramBearer: []
      parameters:
        - $ref: "#/components/parameters/SessionIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      responses:
        "204":
          description: Session revoked.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/security/passkeys/enrollment:
    post:
      tags: [Account Security]
      summary: Begin a passkey browser ceremony
      description: Consumes an `account.passkey.enroll` reauthentication grant.
      operationId: beginPasskeyEnrollment
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      responses:
        "200":
          description: Provider registration options and enrollment capability. Never cached.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasskeyEnrollmentResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "503":
          $ref: "#/components/responses/ProviderForbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/security/passkeys/enrollment/confirm:
    post:
      tags: [Account Security]
      summary: Confirm a passkey registration with browser attestation
      operationId: confirmPasskeyEnrollment
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasskeyEnrollmentConfirmRequest"
      responses:
        "200":
          description: Provider confirmed the passkey.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasskeyEnrollmentConfirmation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "503":
          $ref: "#/components/responses/ProviderForbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/security/passkeys/enrollment/cancel:
    post:
      tags: [Account Security]
      summary: Settle an abandoned passkey registration
      description: |
        Uses the user/session/epoch-bound enrollment capability; no second
        reauthentication grant is accepted or required. Stable provider
        not-found is treated as already settled.
      operationId: cancelPasskeyEnrollment
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasskeyEnrollmentCancelRequest"
      responses:
        "204":
          description: Pending registration removed or already absent.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "503":
          $ref: "#/components/responses/ProviderForbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/security/passkeys/{passkeyId}:
    delete:
      tags: [Account Security]
      summary: Remove one passkey
      description: Consumes an `account.passkey.remove` grant bound to this passkey ID.
      operationId: removePasskey
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/PasskeyIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      responses:
        "200":
          description: Fresh provider-authoritative security summary.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SecuritySummary"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "503":
          $ref: "#/components/responses/ProviderForbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/auth/reauthentication/mfa:
    post:
      tags: [Reauthentication]
      summary: Complete a reauthentication challenge with a second factor
      description: |
        Completes the challenge started by `POST /api/v1/auth/reauthentication`
        using TOTP or passkey (same atomic claim/consume pattern as login
        MFA). On success a single-use grant is issued.
      operationId: completeReauthenticationMfa
      security:
        - SessionCookie: []
        - MiniProgramBearer: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-MiniProgram-Client-When-Bearer"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReauthenticationMfaRequest"
      responses:
        "200":
          description: Challenge completed; single-use grant issued.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReauthenticationGrantResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/users:
    get:
      tags: [Admin Users]
      summary: Search managed users
      description: |
        Server-side search and cursor pagination over stable United Pass users.
        The opaque cursor is signed and bound to query, sort and status.
      operationId: listAdminUsers
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CursorQuery"
        - $ref: "#/components/parameters/LimitQuery"
        - $ref: "#/components/parameters/SearchQuery"
        - $ref: "#/components/parameters/SortQuery"
        - $ref: "#/components/parameters/UserStatusQuery"
      responses:
        "200":
          description: Cursor-paginated user summaries
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagedUserListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/users/{userId}:
    get:
      tags: [Admin Users]
      summary: Get a managed user
      operationId: getAdminUser
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: Managed user detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ManagedUserDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/users/{userId}/enable:
    post:
      tags: [Admin Users]
      summary: Enable a user
      operationId: enableAdminUser
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      responses:
        "204":
          description: User enabled
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/users/{userId}/disable:
    post:
      tags: [Admin Users]
      summary: Disable a user
      description: |
        Requires a single-use `user.disable` reauthentication grant bound to
        this exact `userId`. When session cleanup cannot finish synchronously,
        the disabled state and durable cleanup job remain committed and a 202
        response reports convergence pending.
      operationId: disableAdminUser
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DisableUserRequest"
      responses:
        "202":
          description: User disabled; session cleanup is pending
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessCleanupStatus"
        "204":
          description: User disabled and no cleanup remains pending
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/users/{userId}/sessions:
    delete:
      tags: [Admin Users]
      summary: Revoke every session owned by a user
      description: |
        Requires a single-use `user.sessions.revoke` grant bound to the exact
        target user. Failure to finish the synchronous Redis walk is returned
        as an error while the durable cleanup job continues convergence.
      operationId: revokeAdminUserSessions
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      responses:
        "204":
          description: All indexed sessions were revoked
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/users/{userId}/sessions/{sessionId}:
    delete:
      tags: [Admin Users]
      summary: Revoke one session owned by a user
      operationId: revokeAdminUserSession
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
        - $ref: "#/components/parameters/AdminSessionIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      responses:
        "204":
          description: Session revoked
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/employees:
    get:
      tags: [Admin Employees]
      summary: Search employee profiles
      operationId: listAdminEmployees
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CursorQuery"
        - $ref: "#/components/parameters/LimitQuery"
        - $ref: "#/components/parameters/SearchQuery"
        - $ref: "#/components/parameters/SortQuery"
        - $ref: "#/components/parameters/EmployeeStatusQuery"
      responses:
        "200":
          description: Cursor-paginated employee summaries
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/employees/link:
    post:
      tags: [Admin Employees]
      summary: Link an employee profile to an existing user
      description: |
        The request accepts only an explicit stable `userId`. It never matches
        or merges identities by email, phone, display name or domain. Existing
        personas and OAuth grants are preserved.
      operationId: linkAdminEmployee
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmployeeProfileMutation"
      responses:
        "201":
          description: Employee profile linked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeProfileMutationResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/users/{userId}/employee-profile:
    get:
      tags: [Admin Employees]
      summary: Get a user's employee profile
      operationId: getAdminEmployee
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
      responses:
        "200":
          description: Employee profile detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    put:
      tags: [Admin Employees]
      summary: Update a user's active employee profile
      operationId: updateAdminEmployee
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmployeeProfileUpdate"
      responses:
        "200":
          description: Employee profile updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeProfileMutationResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/users/{userId}/offboarding:
    post:
      tags: [Admin Employees]
      summary: Start employee offboarding
      description: |
        Requires a single-use `employee.offboard` grant bound to this user.
        The response confirms that the authoritative offboarding deny, audit
        event and cleanup job committed atomically; external session cleanup
        may still be converging.
      operationId: offboardAdminEmployee
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/UserIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      responses:
        "202":
          description: Offboarding durably started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccessCleanupStatus"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/departments:
    get:
      tags: [Admin Departments]
      summary: Search departments
      operationId: listAdminDepartments
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/LimitQuery"
        - $ref: "#/components/parameters/SearchQuery"
      responses:
        "200":
          description: Bounded department summaries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DepartmentRecord"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalError"
    post:
      tags: [Admin Departments]
      summary: Create a department
      operationId: createAdminDepartment
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DepartmentMutation"
      responses:
        "201":
          description: Department created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DepartmentDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/departments/{departmentId}:
    get:
      tags: [Admin Departments]
      summary: Get department hierarchy and direct members
      operationId: getAdminDepartment
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/DepartmentIdPath"
      responses:
        "200":
          description: Department detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DepartmentDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    patch:
      tags: [Admin Departments]
      summary: Update a department
      description: Parent changes reject self-parenting and all hierarchy cycles.
      operationId: updateAdminDepartment
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/DepartmentIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DepartmentPatch"
      responses:
        "200":
          description: Department updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DepartmentDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags: [Admin Departments]
      summary: Delete an empty department
      description: A department with direct members or child departments is never deleted.
      operationId: deleteAdminDepartment
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/DepartmentIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      responses:
        "204":
          description: Empty department deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/scopes:
    get:
      tags: [Scope Catalog]
      summary: List the authoritative OAuth scope catalog
      description: |
        Returns the backend-authoritative scope catalog. Arbitrary unregistered
        scopes are never accepted on client creation (422).
      operationId: getAdminScopes
      security:
        - SessionCookie: []
      responses:
        "200":
          description: Scope catalog
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ScopeCatalogItem"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/applications/with-initial-client:
    post:
      tags: [Admin Applications]
      summary: Create an application together with its initial OAuth client
      description: |
        The only creation path in Phase 2. The application and its initial
        client are provisioned atomically from the API consumer's view:
        local rows are committed first (`provisioning`), the provider call
        runs outside any database transaction, then provider identifiers and
        the one-time client secret are recorded. Provider failures return a
        safe 5xx; local metadata for a failed provisioning is never exposed
        as active. Standalone application creation without a client is not
        supported.
      operationId: createApplicationWithInitialClient
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplicationWithInitialClientRequest"
      responses:
        "201":
          description: |
            Application and initial client created. For confidential clients
            the `clientSecret` appears exactly once in this response
            (`Cache-Control: no-store`).
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
            Cache-Control:
              description: Always `no-store` when a secret is included.
              schema:
                type: string
                example: no-store
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationWithInitialClientResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/applications:
    get:
      tags: [Admin Applications]
      summary: List OAuth applications (cursor pagination)
      description: |
        Cursor-paginated, server-side filtered listing. Cursors are opaque
        HMAC-signed values bound to the current sort and filters; tampered
        cursors return 400. Only applications in a fully provisioned state
        are returned.
      operationId: listAdminApplications
      security:
        - SessionCookie: []
      parameters:
        - name: cursor
          in: query
          required: false
          description: Opaque pagination cursor from a previous response.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: Page size (1–100, default 20).
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: query
          in: query
          required: false
          description: Server-side name/description search.
          schema:
            type: string
        - name: sort
          in: query
          required: false
          description: |
            Sort key, `-` prefix for descending. Default `-updatedAt`.
            Stable ordering always appends the ID tie-breaker.
          schema:
            type: string
            enum: [updatedAt, -updatedAt, createdAt, -createdAt, name, -name]
            default: -updatedAt
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [active, disabled]
        - name: audience
          in: query
          required: false
          schema:
            type: string
            enum: [internal, external, hybrid]
        - name: ownerId
          in: query
          required: false
          description: Filter by owner user ID.
          schema:
            type: string
      responses:
        "200":
          description: Application page
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/applications/{applicationId}:
    get:
      tags: [Admin Applications]
      summary: Get application detail with clients, grants and audit entries
      description: |
        `grants` is a real array (always empty until Phase 3). `auditEntries`
        contains only security events actually recorded for this application.
        No fake data.
      operationId: getAdminApplication
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ApplicationIdPath"
      responses:
        "200":
          description: Application detail
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthApplicationDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    patch:
      tags: [Admin Applications]
      summary: Update application metadata
      description: |
        Updates name, description, audience and owner. Ownership is always
        resolved from `ownerId` (validated user ID), never from a display
        name. Unknown JSON fields are rejected.
      operationId: updateAdminApplication
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ApplicationIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplicationUpdateRequest"
      responses:
        "200":
          description: Updated application detail
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthApplicationDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags: [Admin Applications]
      summary: Delete an application and all of its clients
      description: |
        High-risk operation: requires a single-use reauthentication grant
        (`X-Reauthentication-Token`). Provider-side removal runs outside any
        database transaction with compensation/reconciliation on partial
        failure.
      operationId: deleteAdminApplication
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ApplicationIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      responses:
        "204":
          description: Application deleted
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/applications/{applicationId}/enable:
    post:
      tags: [Admin Applications]
      summary: Enable a disabled application
      operationId: enableAdminApplication
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ApplicationIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      responses:
        "200":
          description: Application enabled
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthApplicationDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/applications/{applicationId}/disable:
    post:
      tags: [Admin Applications]
      summary: Disable an application
      description: Disables the application and all of its clients at the provider.
      operationId: disableAdminApplication
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ApplicationIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      responses:
        "200":
          description: Application disabled
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthApplicationDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/applications/{applicationId}/clients:
    post:
      tags: [Admin OAuth Clients]
      summary: Add a new OAuth client to the application
      description: |
        The client profile determines grant types, token endpoint auth method,
        redirect URI requirements and `openid` eligibility (see ADR-0004 §3).
        Unsupported combinations return 422; submitted values are never
        silently mutated. Confidential clients receive their secret exactly
        once in the `201` response.
      operationId: createAdminOAuthClient
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ApplicationIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OAuthClientCreateRequest"
      responses:
        "201":
          description: Client created (secret included once for confidential clients)
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
            Cache-Control:
              description: Always `no-store` when a secret is included.
              schema:
                type: string
                example: no-store
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthClientCreationResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/applications/{applicationId}/clients/{clientId}:
    get:
      tags: [Admin OAuth Clients]
      summary: Get OAuth client detail
      description: Secret values are never returned; only secret metadata is listed.
      operationId: getAdminOAuthClient
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ApplicationIdPath"
        - $ref: "#/components/parameters/ClientIdPath"
      responses:
        "200":
          description: Client detail
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthClient"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"
    patch:
      tags: [Admin OAuth Clients]
      summary: Update client name, redirect URIs, logout URI, scopes or consent mode
      description: |
        Redirect and logout URIs are validated with exact-match semantics
        (no normalization, no wildcards; https or loopback http only).
        Profile is immutable after creation.
      operationId: updateAdminOAuthClient
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ApplicationIdPath"
        - $ref: "#/components/parameters/ClientIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OAuthClientUpdateRequest"
      responses:
        "200":
          description: Updated client detail
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthClient"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"
    delete:
      tags: [Admin OAuth Clients]
      summary: Delete an OAuth client
      description: High-risk operation; requires a reauthentication grant.
      operationId: deleteAdminOAuthClient
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ApplicationIdPath"
        - $ref: "#/components/parameters/ClientIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      responses:
        "204":
          description: Client deleted
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/applications/{applicationId}/clients/{clientId}/enable:
    post:
      tags: [Admin OAuth Clients]
      summary: Enable a disabled OAuth client
      operationId: enableAdminOAuthClient
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ApplicationIdPath"
        - $ref: "#/components/parameters/ClientIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      responses:
        "200":
          description: Client enabled
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthClient"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/applications/{applicationId}/clients/{clientId}/disable:
    post:
      tags: [Admin OAuth Clients]
      summary: Disable an OAuth client
      operationId: disableAdminOAuthClient
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ApplicationIdPath"
        - $ref: "#/components/parameters/ClientIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      responses:
        "200":
          description: Client disabled
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OAuthClient"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/applications/{applicationId}/clients/{clientId}/secret-rotations:
    post:
      tags: [Admin OAuth Clients]
      summary: Rotate a confidential client secret
      description: |
        Requires `application.secret.rotate`, rate limiting, reauthentication
        and audit. Concurrency is single-winner; a failed rotation never
        revokes a still-valid old secret. The new secret is returned exactly
        once; against ZITADEL v2.71 the previous secret is invalidated
        immediately (no provider-side grace period — see ADR-0004 §6).
        Public clients cannot rotate (422).
      operationId: rotateAdminOAuthClientSecret
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ApplicationIdPath"
        - $ref: "#/components/parameters/ClientIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      responses:
        "200":
          description: Secret rotated; the new secret appears only here.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
            Cache-Control:
              description: Always `no-store`.
              schema:
                type: string
                example: no-store
            Pragma:
              description: Always `no-cache`.
              schema:
                type: string
                example: no-cache
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SecretRotationResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/auth/providers:
    get:
      tags: [Provider Login]
      summary: List public login Providers
      description: Returns only safe display metadata and effective login availability.
      operationId: listPublicLoginProviders
      responses:
        "200":
          description: Public Provider login choices.
          headers:
            X-Request-ID:
              $ref: "#/components/headers/X-Request-ID"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicLoginProviderList"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/auth/providers/feishu/authorize:
    get:
      tags: [Provider Login]
      summary: Begin Feishu OAuth login
      description: |
        Creates a random, hash-keyed, single-use OAuth state in Redis and
        redirects to Feishu. The optional resumeRequestId is an opaque local
        authorization-request ID, never an arbitrary return URL.
      operationId: beginFeishuLogin
      parameters:
        - name: resumeRequestId
          in: query
          required: false
          schema:
            type: string
            pattern: "^[A-Za-z0-9_:-]{1,256}$"
        - name: remember
          in: query
          required: false
          schema:
            type: boolean
            default: true
      responses:
        "302":
          description: Redirect to the configured Feishu authorization endpoint.
          headers:
            Location:
              schema:
                type: string
                format: uri
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/auth/providers/feishu/callback:
    get:
      tags: [Provider Login]
      summary: Complete Feishu OAuth login
      description: |
        Atomically consumes the Redis state, exchanges the one-time code on
        the server, requires an exact provider/tenant/subject identity link,
        and creates a local session. Email, phone and name never authenticate
        or merge an identity.
      operationId: completeFeishuLogin
      parameters:
        - name: state
          in: query
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 512
        - name: code
          in: query
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 2048
      responses:
        "303":
          description: Redirect to account/consent continuation or a generic login error.
          headers:
            Location:
              schema:
                type: string
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/identity-providers:
    get:
      tags: [Admin Identity Providers]
      summary: List identity Providers
      operationId: listAdminIdentityProviders
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/CursorQuery"
        - $ref: "#/components/parameters/LimitQuery"
        - $ref: "#/components/parameters/SearchQuery"
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [planned, active, disabled]
      responses:
        "200":
          description: Provider page containing only non-sensitive metadata.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdentityProviderListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/identity-providers/{providerId}:
    get:
      tags: [Admin Identity Providers]
      summary: Get identity Provider detail
      operationId: getAdminIdentityProvider
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ProviderIdPath"
      responses:
        "200":
          description: Provider detail. Secrets and access tokens are never returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdentityProviderDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/identity-providers/{providerId}/enable:
    post:
      tags: [Admin Identity Providers]
      summary: Validate and enable Provider login
      operationId: enableAdminIdentityProvider
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ProviderIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      responses:
        "200":
          description: Provider login enabled after live server-side validation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdentityProviderDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/identity-providers/{providerId}/disable:
    post:
      tags: [Admin Identity Providers]
      summary: Disable new Provider logins
      operationId: disableAdminIdentityProvider
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ProviderIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      responses:
        "200":
          description: New Provider logins disabled; existing local sessions are unchanged.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdentityProviderDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/identity-providers/{providerId}/directory-syncs:
    post:
      tags: [Admin Identity Providers]
      summary: Queue a durable Provider directory synchronization
      operationId: startProviderDirectorySync
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ProviderIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      responses:
        "202":
          description: Existing or newly queued single active sync job.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DirectorySyncResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalError"
    get:
      tags: [Admin Identity Providers]
      summary: List Provider synchronization history
      operationId: listProviderDirectorySyncHistory
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ProviderIdPath"
      responses:
        "200":
          description: Most recent synchronization jobs, newest first.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DirectorySyncHistoryEntry"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/identity-providers/{providerId}/sync-conflicts:
    get:
      tags: [Admin Identity Providers]
      summary: List Provider identity-link conflicts
      operationId: listProviderSyncConflicts
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ProviderIdPath"
      responses:
        "200":
          description: Conflict suggestions. A suggestion is never an identity link.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProviderSyncConflict"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/identity-providers/sync-conflicts/{conflictId}/resolve:
    post:
      tags: [Admin Identity Providers]
      summary: Create an explicit Provider identity link
      operationId: resolveProviderSyncConflict
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ConflictIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResolveProviderConflictRequest"
      responses:
        "204":
          description: Conflict resolved and exact stable user link committed atomically.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/identity-providers/sync-conflicts/{conflictId}/ignore:
    post:
      tags: [Admin Identity Providers]
      summary: Ignore a Provider identity-link conflict
      operationId: ignoreProviderSyncConflict
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ConflictIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      responses:
        "204":
          description: Conflict ignored; no identity link is created.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/policies:
    get:
      tags: [Admin Policies]
      summary: List policy working copies
      operationId: listPolicies
      security:
        - SessionCookie: []
      parameters:
        - {name: cursor, in: query, schema: {type: string}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 100}}
        - {name: query, in: query, schema: {type: string, maxLength: 120}}
        - {name: status, in: query, schema: {type: string, enum: [draft, published]}}
      responses:
        "200":
          description: Cursor page of policies. Requires `policy.read`.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/PolicyPage"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalError"}
    post:
      tags: [Admin Policies]
      summary: Create policy draft version 1
      operationId: createPolicy
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/PolicyDraftRequest"}
      responses:
        "201":
          description: Draft created. Requires `policy.manage`.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/PolicyMutationResult"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/policies/{policyId}:
    parameters:
      - $ref: "#/components/parameters/PolicyIdPath"
    get:
      tags: [Admin Policies]
      summary: Get current policy working copy and version history
      operationId: getPolicy
      security:
        - SessionCookie: []
      responses:
        "200":
          description: Policy detail. Requires `policy.read`.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/PolicyDetail"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "500": {$ref: "#/components/responses/InternalError"}
    patch:
      tags: [Admin Policies]
      summary: Create the next immutable draft version
      operationId: updatePolicyDraft
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/PolicyUpdateRequest"}
      responses:
        "200":
          description: New draft version created using optimistic concurrency.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/PolicyMutationResult"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/policies/{policyId}/publish:
    post:
      tags: [Admin Policies]
      summary: Publish an exact policy version to Cerbos
      operationId: publishPolicy
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/PolicyIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [version]
              additionalProperties: false
              properties:
                version: {type: integer, minimum: 1}
      responses:
        "200":
          description: Cerbos accepted the policy and the database selected it atomically.
          content:
            application/json:
              schema:
                type: object
                required: [version]
                additionalProperties: false
                properties:
                  version: {type: integer, minimum: 1}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "502": {$ref: "#/components/responses/ProviderUnavailable"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/policies/{policyId}/simulate:
    post:
      tags: [Admin Policies]
      summary: Simulate the selected working copy without publishing it
      operationId: simulatePolicy
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/PolicyIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/PolicySimulationRequest"}
      responses:
        "200":
          description: Side-effect-free preview decision.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/PolicySimulationResult"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/policies/{policyId}/versions:
    get:
      tags: [Admin Policies]
      summary: List immutable policy versions
      operationId: listPolicyVersions
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/PolicyIdPath"
      responses:
        "200":
          description: Version history, newest first.
          content:
            application/json:
              schema:
                type: array
                items: {$ref: "#/components/schemas/PolicyVersionSummary"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/audit-events:
    get:
      tags: [Admin Audit]
      summary: Filter durable security audit events
      operationId: listAuditEvents
      security:
        - SessionCookie: []
      parameters:
        - {name: cursor, in: query, schema: {type: string}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 100}}
        - {name: query, in: query, schema: {type: string, maxLength: 200}}
        - {name: eventType, in: query, schema: {type: string}}
        - {name: result, in: query, schema: {type: string, enum: [success, denied]}}
        - {name: actorName, in: query, schema: {type: string}}
        - {name: requestId, in: query, schema: {type: string}}
        - {name: from, in: query, schema: {type: string, format: date-time}}
        - {name: to, in: query, schema: {type: string, format: date-time}}
      responses:
        "200":
          description: Redacted audit cursor page. Requires `audit.read`.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AuditEventPage"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/audit-exports:
    post:
      tags: [Admin Audit]
      summary: Create a durable asynchronous audit CSV export
      operationId: createAuditExport
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AuditQuery"}
      responses:
        "202":
          description: Export accepted. Requires `audit.export` and a grant bound to `audit.export + audit`.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AuditExport"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/audit-exports/{exportId}:
    get:
      tags: [Admin Audit]
      summary: Get the requester's audit export job
      operationId: getAuditExport
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/AuditExportIdPath"
      responses:
        "200":
          description: Export status and same-origin download URL when complete.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AuditExport"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/audit-exports/{exportId}/download:
    get:
      tags: [Admin Audit]
      summary: Download a completed unexpired export owned by the requester
      operationId: downloadAuditExport
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/AuditExportIdPath"
      responses:
        "200":
          description: UTF-8 CSV with a BOM and fixed redacted columns.
          content:
            text/csv:
              schema: {type: string}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/events/{eventId}/identity-access/requests:
    post:
      tags: [Restricted Identity Access]
      summary: Request time-limited access to restricted identity fields
      description: |
        A senior administrator may request only the fixed allowlist of identity
        fields for a target in the same event. The normalized reason is encrypted
        server-side and is never returned by this API. Legal documents cannot be
        requested through this workflow.
      operationId: createIdentityAccessRequest
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/DreamUPEventIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/IdentityAccessCreateRequest"}
      responses:
        "201":
          description: Request created or safely replayed.
          headers:
            X-Request-ID: {$ref: "#/components/headers/X-Request-ID"}
            ETag:
              description: Strong request-row version.
              schema: {type: string}
          content:
            application/json:
              schema: {$ref: "#/components/schemas/IdentityAccessRequest"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "500": {$ref: "#/components/responses/InternalError"}
    get:
      tags: [Restricted Identity Access]
      summary: List the current administrator's requests in one event
      operationId: listOwnIdentityAccessRequests
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/DreamUPEventIdPath"
        - $ref: "#/components/parameters/CursorQuery"
        - $ref: "#/components/parameters/LimitQuery"
        - $ref: "#/components/parameters/SortQuery"
        - $ref: "#/components/parameters/IdentityAccessStatusQuery"
      responses:
        "200":
          description: Requester-owned page with an actor- and event-bound cursor.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/IdentityAccessRequestPage"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/events/{eventId}/identity-access/requests/{requestId}:
    get:
      tags: [Restricted Identity Access]
      summary: Read one requester-owned identity-access request
      operationId: getOwnIdentityAccessRequest
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/DreamUPEventIdPath"
        - $ref: "#/components/parameters/IdentityAccessRequestIdPath"
      responses:
        "200":
          description: Request metadata without the protected reason.
          headers:
            ETag:
              description: Strong request-row version.
              schema: {type: string}
          content:
            application/json:
              schema: {$ref: "#/components/schemas/IdentityAccessRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/events/{eventId}/identity-access/approval-queue:
    get:
      tags: [Restricted Identity Access]
      summary: List pending identity-access requests for super-administrator approval
      operationId: listIdentityAccessApprovalQueue
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/DreamUPEventIdPath"
        - $ref: "#/components/parameters/CursorQuery"
        - $ref: "#/components/parameters/LimitQuery"
        - $ref: "#/components/parameters/SortQuery"
        - $ref: "#/components/parameters/IdentityAccessStatusQuery"
      responses:
        "200":
          description: Event-scoped approval page with a signed filter-bound cursor.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/IdentityAccessRequestPage"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/events/{eventId}/identity-access/approval-queue/{requestId}:
    get:
      tags: [Restricted Identity Access]
      summary: Read one event-scoped request for approval
      operationId: getIdentityAccessRequestForApproval
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/DreamUPEventIdPath"
        - $ref: "#/components/parameters/IdentityAccessRequestIdPath"
      responses:
        "200":
          description: Approval metadata without the protected reason.
          headers:
            ETag:
              description: Strong request-row version.
              schema: {type: string}
          content:
            application/json:
              schema: {$ref: "#/components/schemas/IdentityAccessRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/events/{eventId}/identity-access/requests/{requestId}/approve:
    post:
      tags: [Restricted Identity Access]
      summary: Approve a request after fresh administrator step-up
      description: |
        The super administrator may reduce, but never broaden, the requested
        field set. The requester, approver and target subject must be distinct.
      operationId: approveIdentityAccessRequest
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/DreamUPEventIdPath"
        - $ref: "#/components/parameters/IdentityAccessRequestIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
        - $ref: "#/components/parameters/If-Match"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      requestBody:
        required: false
        content:
          application/json:
            schema: {$ref: "#/components/schemas/IdentityAccessDecisionRequest"}
      responses:
        "200":
          description: Approved, or an identical terminal mutation safely replayed.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/IdentityAccessDecisionResponse"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "428": {$ref: "#/components/responses/PreconditionRequired"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/events/{eventId}/identity-access/requests/{requestId}/reject:
    post:
      tags: [Restricted Identity Access]
      summary: Reject a request after fresh administrator step-up
      operationId: rejectIdentityAccessRequest
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/DreamUPEventIdPath"
        - $ref: "#/components/parameters/IdentityAccessRequestIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
        - $ref: "#/components/parameters/If-Match"
        - $ref: "#/components/parameters/X-Reauthentication-Token"
      responses:
        "200":
          description: Rejected, or an identical terminal mutation safely replayed.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/IdentityAccessDecisionResponse"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "428": {$ref: "#/components/responses/PreconditionRequired"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/events/{eventId}/identity-access/grants/{grantId}/claim:
    post:
      tags: [Restricted Identity Access]
      summary: Claim an approved identity-access grant for internal delegation
      description: |
        Creates a one-minute internal claim lease. The browser response contains
        only opaque metadata; the one-time nonce and restricted values remain in
        the trusted server boundary for delegation-token issuance.
      operationId: claimIdentityAccessGrant
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/DreamUPEventIdPath"
        - $ref: "#/components/parameters/IdentityAccessGrantIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
        - $ref: "#/components/parameters/If-Match"
      responses:
        "202":
          description: The internal claim is ready for the trusted BFF signer.
          content:
            application/json:
              schema: {$ref: "#/components/schemas/IdentityAccessClaimResponse"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "428": {$ref: "#/components/responses/PreconditionRequired"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/step-up/challenge:
    get:
      tags: [Administrator Step-up]
      summary: Read the current administrator challenge state and question
      operationId: getAdminStepUpChallenge
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/DreamUPEventIdQuery"
      responses:
        "200":
          description: Owner-only challenge state. The answer hash and encryption metadata are never returned.
          headers:
            X-Request-ID: {$ref: "#/components/headers/X-Request-ID"}
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AdminStepUpChallengeResponse"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/step-up/enroll:
    post:
      tags: [Administrator Step-up]
      summary: Enroll an administrator challenge after fresh provider login
      description: A fresh provider-backed primary login is always required. Existing TOTP, passkey, or recovery MFA is accepted; only an active system-scoped super_admin or top_admin with no existing challenge may use the initial-enrollment bootstrap exception without existing MFA. A federated session lacking provider session proof receives session.reauthentication_required.
      operationId: enrollAdminStepUpChallenge
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AdminStepUpEnrollRequest"}
      responses:
        "201":
          description: Challenge enrolled. No question or answer material is echoed.
          headers:
            X-Request-ID: {$ref: "#/components/headers/X-Request-ID"}
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AdminStepUpMutationResponse"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/step-up/verify:
    post:
      tags: [Administrator Step-up]
      summary: Verify the administrator challenge for one fixed action and target
      operationId: verifyAdminStepUpChallenge
      description: |
        Requires a provider-backed primary login no older than 30 minutes.
        Stale or implausibly future authentication timestamps fail before the
        answer verifier with `session.reauthentication_required`. Sensitive
        DreamUP actions mint a five-minute action-and-target-bound one-shot
        token; application review and check-in scan are high-risk mutations too.
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AdminStepUpVerifyRequest"}
      responses:
        "200":
          description: |
            Verification receipt. A high-risk action may include one new
            single-use reauthentication bearer; an idempotent replay never
            returns or re-mints that bearer.
          headers:
            X-Request-ID: {$ref: "#/components/headers/X-Request-ID"}
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AdminStepUpMutationResponse"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "423": {$ref: "#/components/responses/Locked"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/admin/step-up/rotate:
    post:
      tags: [Administrator Step-up]
      summary: Prove the old answer and atomically rotate the challenge
      operationId: rotateAdminStepUpChallenge
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
        - $ref: "#/components/parameters/Idempotency-Key"
        - $ref: "#/components/parameters/If-Match"
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/AdminStepUpRotateRequest"}
      responses:
        "200":
          description: Challenge rotated and prior freshness and approval state revoked.
          headers:
            X-Request-ID: {$ref: "#/components/headers/X-Request-ID"}
          content:
            application/json:
              schema: {$ref: "#/components/schemas/AdminStepUpMutationResponse"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/FieldValidationFailed"}
        "423": {$ref: "#/components/responses/Locked"}
        "428": {$ref: "#/components/responses/PreconditionRequired"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "500": {$ref: "#/components/responses/InternalError"}

  /api/v1/password-reset-requests:
    post:
      tags: [Account Lifecycle]
      summary: Request password-reset instructions
      description: |
        Always returns the same accepted response after syntax and rate-limit
        checks. Account absence, unverified contact, missing local binding and
        provider-notification failure are intentionally indistinguishable.
        Requires JSON and an exact same-origin `Origin`.
      operationId: requestPublicPasswordReset
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordResetRequest"
      responses:
        "202":
          description: Request accepted; no account-existence signal is returned.
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /api/v1/password-resets:
    post:
      tags: [Account Lifecycle]
      summary: Complete a provider-verified password reset
      description: |
        Opens the short-lived encrypted United Pass lifecycle token, verifies
        the provider code, changes the provider-owned password and advances the
        user's security epoch so all older sessions are invalidated. The local
        token contains no email, provider subject, password or code.
      operationId: completePublicPasswordReset
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordResetCompletionRequest"
      responses:
        "204":
          description: Password reset completed and older sessions invalidated.
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/email-verifications:
    post:
      tags: [Account Lifecycle]
      summary: Verify a pending registration email
      description: |
        Verifies the provider-issued email code for the exact provider subject
        bound inside a short-lived encrypted lifecycle token, then activates
        only the matching pending United Pass user. Provider readback repairs a
        consumed-code/response-loss retry without trusting browser identity data.
      operationId: verifyPublicRegistrationEmail
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailVerificationRequest"
      responses:
        "204":
          description: Email verified and pending account activated.
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "410":
          $ref: "#/components/responses/Gone"
        "415":
          $ref: "#/components/responses/UnsupportedMediaType"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/avatar:
    post:
      tags: [Account]
      summary: Replace the current user's avatar
      description: |
        Accepts exactly one multipart file, capped at 2 MiB. Current clients
        use the `file` field and receive a versioned, session-protected JPEG
        URL; earlier clients may use `avatar` and receive an immutable public
        PNG URL. Both paths decode, bound and re-encode the image before it is
        stored. Supplying both fields is rejected.
      operationId: uploadCurrentUserAvatar
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              oneOf:
                - type: object
                  required: [file]
                  additionalProperties: false
                  properties:
                    file:
                      type: string
                      format: binary
                - type: object
                  required: [avatar]
                  additionalProperties: false
                  properties:
                    avatar:
                      type: string
                      format: binary
      responses:
        "200":
          description: Filesystem-backed avatar stored for a current client.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvatarUploadResponse"
        "201":
          description: Immutable PostgreSQL-backed avatar stored for an earlier client.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AvatarUploadResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "413":
          description: Multipart request or avatar file exceeds the configured limit.
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/media/avatars/{avatarFile}:
    get:
      tags: [Account]
      summary: Read a sanitized avatar
      description: |
        An opaque `avt_<random>.png` identifier reads the earlier public,
        immutable PNG object. A stable user identifier reads the current
        private JPEG object and requires a valid session. Both forms serve
        only server-reencoded bytes.
      operationId: getSanitizedAvatar
      security:
        - {}
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/AvatarFilePath"
        - name: If-None-Match
          in: header
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Sanitized PNG avatar.
          headers:
            ETag:
              schema: { type: string }
            Cache-Control:
              schema: { type: string, example: "public, max-age=31536000, immutable" }
          content:
            image/png:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
        "304":
          description: The supplied ETag matches the current immutable object.
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/email-change-requests:
    post:
      tags: [Account]
      summary: Begin a verified email change
      description: |
        Persists a short-lived, session-bound request and asks the identity
        provider to send a verification code. The returned capability is
        stored only as a SHA-256 digest and prior active requests are superseded.
      operationId: requestCurrentUserEmailChange
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContactChangeRequest"
      responses:
        "201":
          description: Verification request created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactChangeResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/email-change-requests/{requestId}/verify:
    post:
      tags: [Account]
      summary: Verify and commit an email change
      description: |
        Claims the exact user- and session-bound request, verifies the code at
        the provider, then mirrors the provider-verified email locally in one
        transaction. Concurrent verification has a single live claimant.
      operationId: verifyCurrentUserEmailChange
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ContactRequestIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContactVerificationRequest"
      responses:
        "204":
          description: Provider-verified email committed.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/phone-change-requests:
    post:
      tags: [Account]
      summary: Begin a verified phone change
      operationId: requestCurrentUserPhoneChange
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContactChangeRequest"
      responses:
        "201":
          description: Verification request created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactChangeResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/me/phone-change-requests/{requestId}/verify:
    post:
      tags: [Account]
      summary: Verify and commit a phone change
      operationId: verifyCurrentUserPhoneChange
      security:
        - SessionCookie: []
      parameters:
        - $ref: "#/components/parameters/ContactRequestIdPath"
        - $ref: "#/components/parameters/X-CSRF-Token"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContactVerificationRequest"
      responses:
        "204":
          description: Provider-verified phone committed.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/FieldValidationFailed"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "502":
          $ref: "#/components/responses/ProviderUnavailable"
        "500":
          $ref: "#/components/responses/InternalError"

  /api/v1/admin/dashboard:
    get:
      tags: [Admin Dashboard]
      summary: Get the permission-scoped administration overview
      description: |
        Returns only aggregates independently authorized for the caller:
        `user.read` unlocks user and employee counts, `application.read`
        unlocks application counts, and `audit.read` unlocks denied-event
        counts plus three already-redacted recent audit events. Possessing a
        different administration capability permits the overview shell but
        does not leak unauthorized counts.
      operationId: getAdminDashboard
      security:
        - SessionCookie: []
      responses:
        "200":
          description: Authorized dashboard slices.
          headers:
            Cache-Control:
              schema: { type: string, enum: [no-store] }
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminDashboard"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalError"

components:
  securitySchemes:
    SessionCookie:
      type: apiKey
      in: cookie
      name: up_session
      description: |
        Browser session cookie (`up_session`) issued by United Pass. The cookie
        is HttpOnly, Secure and carries an explicit SameSite policy. The browser
        must not receive raw authentication-provider Refresh Tokens or
        long-lived tokens.

    MiniProgramBearer:
      type: http
      scheme: bearer
      bearerFormat: opaque-256-bit-base64url
      description: |
        Short-lived opaque DreamUP Mini Program credential returned by a
        native password, TOTP or verified WeChat login. The raw 43-character
        base64url value is held only in module memory; only its SHA-256 hash is
        stored server-side. Requests must also carry the exact
        `X-UnitedPass-Client: dreamup-miniprogram` marker and no browser Origin
        or Fetch Metadata headers. Native bearer requests are not subject to
        ambient-cookie CSRF because no cookie participates in authentication.

  headers:
    X-Request-ID:
      description: |
        The request ID used for log, error and audit correlation. The server
        accepts a valid upstream ID (alphanumeric, dash, underscore, colon;
        8-128 chars) or generates a cryptographically random `req_`-prefixed ID.
      schema:
        type: string
        pattern: "^[a-zA-Z0-9_:-]{8,128}$"
        example: req_0123456789abcdef0123456789abcdef

    Retry-After:
      description: |
        Seconds the client must wait before retrying a rate-limited request.
        Returned with `429` responses.
      schema:
        type: integer
        format: int64
        minimum: 1
        example: 30

    Set-Session-Cookies:
      description: |
        Sets the browser session and CSRF cookies on successful authentication.
        Two cookies are issued:

          * `up_session` — HttpOnly, Secure, SameSite session cookie. Never
            contains a raw authentication-provider Refresh Token.
          * `up_csrf`   — CSRF token cookie readable by browser JavaScript,
            used to validate the `X-CSRF-Token` header on subsequent
            state-changing requests.

        Both cookies use restricted Path and explicit lifetime. Server-side
        revocation is supported.
      schema:
        type: string
        description: |
          A `Set-Cookie` header value. Two `Set-Cookie` headers are emitted,
          one for `up_session` and one for `up_csrf`.
        example: up_session=...; Path=/; HttpOnly; Secure; SameSite=Lax

    Clear-Session-Cookies:
      description: |
        Clears the browser session and CSRF cookies on logout. Both
        `up_session` and `up_csrf` are set with `Max-Age=0` so the browser
        discards them immediately.
      schema:
        type: string
        description: |
          A `Set-Cookie` header value with `Max-Age=0`. Two `Set-Cookie`
          headers are emitted, one for `up_session` and one for `up_csrf`.
        example: up_session=; Path=/; Max-Age=0; HttpOnly; Secure; SameSite=Lax

  parameters:
    AvatarFilePath:
      name: avatarFile
      in: path
      required: true
      description: Immutable avatar object name or current stable user identifier.
      schema:
        oneOf:
          - type: string
            pattern: "^avt_[0-9a-f]{32}\\.png$"
          - type: string
            pattern: "^[A-Za-z0-9_-]{1,80}$"

    ContactRequestIdPath:
      name: requestId
      in: path
      required: true
      description: Opaque, short-lived contact-change capability returned once to this session.
      schema:
        type: string
        minLength: 1
        maxLength: 4096

    RegistrationOrigin:
      name: Origin
      in: header
      required: true
      description: Exact configured browser origin; cross-origin and missing values are rejected.
      schema: {type: string, format: uri}

    X-MiniProgram-Client:
      name: X-UnitedPass-Client
      in: header
      required: true
      description: Native transport marker only; it is not authentication and grants no authority.
      schema:
        type: string
        const: dreamup-miniprogram

    X-MiniProgram-Client-When-Bearer:
      name: X-UnitedPass-Client
      in: header
      required: false
      description: |
        Required and fixed to `dreamup-miniprogram` whenever the
        `MiniProgramBearer` security alternative is selected. Browser-cookie
        callers omit this transport marker.
      schema:
        type: string
        const: dreamup-miniprogram

    X-CSRF-Token:
      name: X-CSRF-Token
      in: header
      required: true
      description: |
        CSRF token that must match the value of the `up_csrf` cookie. Required
        on all browser-originated state-changing requests using cookie
        authentication. Safe HTTP methods must not mutate state.
      schema:
        type: string
        minLength: 1
        example: csrf_0123456789abcdef

    ApplicationIdPath:
      name: applicationId
      in: path
      required: true
      description: United Pass application ID (`app_…`).
      schema:
        type: string
        example: app_01HQFGXKN8H2C4JVMW7K3R4D8Z

    ClientIdPath:
      name: clientId
      in: path
      required: true
      description: |
        United Pass OAuth client ID (`clt_…`). The server asserts the client
        belongs to the path application; mismatch answers 404.
      schema:
        type: string
        example: clt_01HQFGXKN8H2C4JVMW7K3R4D8Z

    PasskeyIdPath:
      name: passkeyId
      in: path
      required: true
      description: Opaque provider-issued passkey identifier.
      schema:
        type: string
        minLength: 1

    SessionIdPath:
      name: sessionId
      in: path
      required: true
      description: Opaque 128-bit United Pass session identifier owned by the current user.
      schema:
        type: string
        minLength: 1

    QRChallengeIdPath:
      name: challengeId
      in: path
      required: true
      description: Opaque short-lived QR handoff identifier; never a session or receiver credential.
      schema:
        type: string
        pattern: "^[A-Za-z0-9_-]{20,255}$"

    UserIdPath:
      name: userId
      in: path
      required: true
      description: Stable United Pass user ID; never an email or provider subject.
      schema:
        type: string
        pattern: "^[A-Za-z0-9_-]{1,128}$"
        example: usr_01HQFGXKN8H2C4JVMW7K3R4D8Z

    DepartmentIdPath:
      name: departmentId
      in: path
      required: true
      description: United Pass department ID (`dep_…`).
      schema:
        type: string
        pattern: "^dep_[A-Za-z0-9_-]{1,124}$"

    ProviderIdPath:
      name: providerId
      in: path
      required: true
      description: United Pass Provider ID (`provider_…`).
      schema:
        type: string
        pattern: "^provider_[A-Za-z0-9_-]{1,119}$"

    ConflictIdPath:
      name: conflictId
      in: path
      required: true
      description: Durable Provider identity-link conflict ID (`conflict_…`).
      schema:
        type: string
        pattern: "^conflict_[A-Za-z0-9_-]{1,119}$"

    PolicyIdPath:
      name: policyId
      in: path
      required: true
      description: Stable United Pass authorization policy ID (`pol_…`).
      schema:
        type: string
        pattern: "^pol_[A-Za-z0-9_-]{8,76}$"

    AuditExportIdPath:
      name: exportId
      in: path
      required: true
      description: Durable audit export job ID (`exp_…`).
      schema:
        type: string
        pattern: "^exp_[A-Za-z0-9_-]{8,76}$"

    PersonalDataExportIdPath:
      name: exportId
      in: path
      required: true
      description: Requester-owned personal-data export job ID (`pexp_…`).
      schema:
        type: string
        pattern: "^pexp_[A-Za-z0-9_-]{13,74}$"

    AdminSessionIdPath:
      name: sessionId
      in: path
      required: true
      description: Opaque United Pass session ID asserted to belong to the target user.
      schema:
        type: string
        minLength: 1
        maxLength: 128

    CursorQuery:
      name: cursor
      in: query
      required: false
      description: Opaque signed cursor returned by the previous page.
      schema:
        type: string
        minLength: 1
        maxLength: 4096

    LimitQuery:
      name: limit
      in: query
      required: false
      description: Page or bounded-list size; defaults to 20 and is capped at 100.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20

    SearchQuery:
      name: query
      in: query
      required: false
      description: Server-side free-text search term.
      schema:
        type: string
        maxLength: 200

    SortQuery:
      name: sort
      in: query
      required: false
      description: Endpoint-specific stable sort name.
      schema:
        type: string

    UserStatusQuery:
      name: status
      in: query
      required: false
      schema:
        type: string
        enum: [active, disabled, pending]

    EmployeeStatusQuery:
      name: status
      in: query
      required: false
      schema:
        type: string
        enum: [active, offboarding]

    X-Reauthentication-Token:
      name: X-Reauthentication-Token
      in: header
      required: true
      description: |
        Single-use reauthentication grant issued by the reauthentication
        endpoints. Bound to the exact action and target resource; consumed
        atomically before the operation executes.
      schema:
        type: string
        minLength: 43
        maxLength: 43
        pattern: "^[A-Za-z0-9_-]{43}$"
        example: reauth_grant_opaque_token

    DreamUP-X-Reauthentication-Token:
      name: X-Reauthentication-Token
      in: header
      required: false
      description: |
        Required for every high-risk request authenticated with the native Mini
        Program bearer. It is a five-minute, single-use grant bound to the exact
        action and target and is consumed atomically before the operation. The
        existing first-party cookie website may omit this header only while its
        durable proof is bound to the same authenticated session and user,
        matches the current account security epoch, and was verified no more
        than five minutes ago. If a cookie request supplies this header, token
        rejection fails closed and never falls back to the durable proof.
      schema:
        type: string
        minLength: 43
        maxLength: 43
        pattern: "^[A-Za-z0-9_-]{43}$"
        example: reauth_grant_opaque_token

    DreamUPEventIdQuery:
      name: eventId
      in: query
      required: true
      description: Exact DreamUP event scope for the active administrator binding.
      schema:
        type: string
        minLength: 1
        maxLength: 128

    DreamUPEventIdPath:
      name: eventId
      in: path
      required: true
      description: Exact DreamUP event scope for the active administrator binding.
      schema:
        type: string
        minLength: 1
        maxLength: 128

    DreamUPContentIdPath:
      name: contentId
      in: path
      required: true
      description: Opaque DreamUP announcement content identifier.
      schema:
        type: string
        minLength: 1
        maxLength: 160
        pattern: "^[A-Za-z0-9][A-Za-z0-9._:-]*$"

    DreamUPContactSubmissionIdPath:
      name: submissionId
      in: path
      required: true
      description: Opaque DreamUP contact-submission identifier.
      schema:
        type: string
        minLength: 1
        maxLength: 160
        pattern: "^[A-Za-z0-9][A-Za-z0-9._:-]*$"

    IdentityAccessRequestIdPath:
      name: requestId
      in: path
      required: true
      description: Opaque restricted identity-access request ID.
      schema:
        type: string
        minLength: 1
        maxLength: 160

    IdentityAccessGrantIdPath:
      name: grantId
      in: path
      required: true
      description: Opaque time-limited restricted identity-access grant ID.
      schema:
        type: string
        minLength: 1
        maxLength: 160

    IdentityAccessStatusQuery:
      name: status
      in: query
      required: false
      description: Exact request-status filter bound into the signed cursor.
      schema:
        $ref: "#/components/schemas/IdentityAccessStatus"

    Idempotency-Key:
      name: Idempotency-Key
      in: header
      required: true
      description: Globally random mutation key. Same key and different normalized input conflicts.
      schema:
        type: string
        pattern: "^[A-Za-z0-9_-]{32,160}$"

    If-Match:
      name: If-Match
      in: header
      required: true
      description: Strong ETag containing the current challenge row version.
      schema:
        type: string
        pattern: '^"[1-9][0-9]*"$'

    DreamUP-If-Match:
      name: If-Match
      in: header
      required: true
      description: Strong DreamUP BFF resource-version ETag; use `"0"` when the resource has no prior version.
      schema:
        type: string
        pattern: '^"(0|[1-9][0-9]*)"$'

  responses:
    Gone:
      description: The short-lived lifecycle capability has expired.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    RateLimited:
      $ref: "#/components/responses/TooManyRequests"
    UnsupportedMediaType:
      description: The credential-bearing request must use `application/json`.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    BadRequest:
      description: Malformed request, unknown JSON field, or invalid cursor.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Unauthorized:
      description: Missing or invalid session.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Forbidden:
      description: Session is valid but the operation is not authorized.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: |
        Resource does not exist or is not accessible by the caller. Used
        consistently for inaccessible resources (anti-enumeration).
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Conflict:
      description: State conflict (duplicate name, stale version, concurrent mutation).
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    FieldValidationFailed:
      description: Domain validation failed; field-level errors are returned.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    TooManyRequests:
      description: Rate limit exceeded.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
        Retry-After:
          $ref: "#/components/headers/Retry-After"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Locked:
      description: The challenge is temporarily locked after repeated failed verification.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    PreconditionRequired:
      description: A strong If-Match challenge version is required.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    InternalError:
      description: Unexpected server failure. No internal detail is leaked.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    ProviderUnavailable:
      description: |
        The identity provider rejected or failed the provisioning call. The
        response carries a stable internal error class only — never raw
        provider errors.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    ProviderForbidden:
      description: |
        The provider service account lacks the required permission. This is a
        server-side configuration failure, not a caller authorization denial.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    WeChatOnboardingCompleteUnauthorized:
      description: |
        The restricted onboarding challenge expired, the existing-account
        credential was wrong, or the resolved account could not authenticate.
        The password-specific code is returned only after a valid, claimed and
        rate-limited WeChat onboarding challenge.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            expired:
              value:
                error:
                  code: wechat.onboarding_expired
                  message: "微信登录流程已过期，请重新发起。"
                  requestId: req_0123456789abcdef
            existingEmailPasswordMismatch:
              value:
                error:
                  code: wechat.onboarding_email_password_mismatch
                  message: "此邮箱已存在账号，请设定原始密码以进行绑定"
                  requestId: req_0123456789abcdef
            accountValidationFailed:
              value:
                error:
                  code: session.unauthenticated
                  message: "账户验证失败，请重试。"
                  requestId: req_0123456789abcdef
    WeChatOnboardingMFAUnauthorized:
      description: The onboarding MFA challenge expired or MFA/account verification failed.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            expired:
              value:
                error:
                  code: wechat.onboarding_expired
                  message: "微信登录流程已过期，请重新发起。"
                  requestId: req_0123456789abcdef
            accountValidationFailed:
              value:
                error:
                  code: session.unauthenticated
                  message: "账户验证失败，请重试。"
                  requestId: req_0123456789abcdef
    WeChatOnboardingConflict:
      description: The WeChat identity, normalized email, target account, or optional verified phone cannot be linked safely.
      headers:
        X-Request-ID:
          $ref: "#/components/headers/X-Request-ID"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            error:
              code: wechat.onboarding_conflict
              message: "无法安全绑定该账户，请检查信息或联系支持。"
              requestId: req_0123456789abcdef

  schemas:
    PasswordResetRequest:
      type: object
      required: [identifier]
      additionalProperties: false
      properties:
        identifier:
          type: string
          minLength: 1
          maxLength: 254
          description: Account name or email; the response never signals existence.

    PasswordResetCompletionRequest:
      type: object
      required: [token, code, newPassword]
      additionalProperties: false
      properties:
        token:
          type: string
          minLength: 1
          maxLength: 4096
          writeOnly: true
        code:
          type: string
          minLength: 4
          maxLength: 20
          pattern: "^[A-Za-z0-9]+$"
          writeOnly: true
        newPassword:
          type: string
          format: password
          minLength: 12
          maxLength: 200
          writeOnly: true

    EmailVerificationRequest:
      type: object
      required: [token, code]
      additionalProperties: false
      properties:
        token:
          type: string
          minLength: 1
          maxLength: 4096
          writeOnly: true
        code:
          type: string
          minLength: 4
          maxLength: 20
          pattern: "^[A-Za-z0-9]+$"
          writeOnly: true

    AvatarUploadResponse:
      type: object
      required: [avatarUrl]
      additionalProperties: false
      properties:
        avatarUrl:
          type: string
          pattern: "^/api/v1/media/avatars/(?:avt_[0-9a-f]{32}\\.png|[A-Za-z0-9_-]{1,80}(?:\\?v=[0-9]+)?)$"

    ContactChangeRequest:
      type: object
      required: [value]
      additionalProperties: false
      properties:
        value:
          type: string
          minLength: 1
          maxLength: 320
          description: Email or E.164 phone selected by the endpoint.

    ContactChangeResponse:
      type: object
      required: [requestId]
      additionalProperties: false
      properties:
        requestId:
          type: string
          minLength: 1
          description: Opaque capability returned once and bound to the current session.

    ContactVerificationRequest:
      type: object
      required: [code]
      additionalProperties: false
      properties:
        code:
          type: string
          minLength: 4
          maxLength: 20
          pattern: "^[A-Za-z0-9]+$"
          writeOnly: true

    DashboardMetric:
      type: object
      required: [label, value, change, tone]
      additionalProperties: false
      properties:
        label: { type: string }
        value: { type: string }
        change: { type: string }
        tone: { type: string, enum: [neutral, positive, attention] }

    AdminDashboard:
      type: object
      required: [metrics, recentEvents]
      additionalProperties: false
      properties:
        metrics:
          type: array
          items:
            $ref: "#/components/schemas/DashboardMetric"
        recentEvents:
          type: array
          maxItems: 3
          items:
            $ref: "#/components/schemas/AuditEvent"

    DreamUPAdminEligibilityResponse:
      type: object
      additionalProperties: false
      required: [eligible]
      properties:
        eligible: { type: boolean }

    DreamUPMutationStatusResponse:
      type: object
      additionalProperties: false
      required: [operation]
      properties:
        operation:
          type: object
          additionalProperties: false
          required: [status, requestId, responseStatus, resultVersion, updatedAt]
          properties:
            status:
              type: string
              enum: [pending, succeeded, failed, needs_operator]
            requestId:
              type: string
              pattern: "^[A-Za-z0-9._:-]{8,128}$"
            responseStatus:
              type: [integer, "null"]
              minimum: 200
              maximum: 599
            resultVersion:
              type: [integer, "null"]
              minimum: 1
            updatedAt:
              type: string
              format: date-time

    DreamUPIntroMutationRequest:
      type: object
      additionalProperties: false
      required: [title, summary, blocks, action]
      properties:
        title: { type: string }
        summary: { type: string }
        blocks:
          type: array
          items: { $ref: "#/components/schemas/DreamUPContentBlock" }
        action:
          type: string
          enum: [save_draft, publish]

    DreamUPAnnouncementMutationRequest:
      type: object
      additionalProperties: false
      required: [title, summary, blocks, action]
      properties:
        title: { type: string }
        summary: { type: string }
        blocks:
          type: array
          items: { $ref: "#/components/schemas/DreamUPContentBlock" }
        action:
          type: string
          enum: [save_draft, publish, archive]

    DreamUPContentBlock:
      oneOf:
        - type: object
          additionalProperties: false
          required: [id, type, text]
          properties:
            id: { type: string }
            type: { type: string, enum: [heading, paragraph, quote] }
            text: { type: string }
        - type: object
          additionalProperties: false
          required: [id, type, items]
          properties:
            id: { type: string }
            type: { type: string, const: bullets }
            items:
              type: array
              items: { type: string }
        - type: object
          additionalProperties: false
          required: [id, type, label, value]
          properties:
            id: { type: string }
            type: { type: string, const: stat }
            label: { type: string }
            value: { type: string }
        - type: object
          additionalProperties: false
          required: [id, type, label, action]
          properties:
            id: { type: string }
            type: { type: string, const: cta }
            label: { type: string }
            action: { type: string, enum: [apply, team, feedback, sponsor] }

    DreamUPContentResponse:
      type: object
      additionalProperties: false
      required: [intro, announcements]
      properties:
        intro:
          oneOf:
            - $ref: "#/components/schemas/DreamUPContentDocument"
            - type: "null"
        announcements:
          type: array
          items:
            $ref: "#/components/schemas/DreamUPContentDocument"

    DreamUPContentDocument:
      type: object
      additionalProperties: false
      required: [id, kind, status, title, summary, blocks, version, revision, publishedAt, updatedAt]
      properties:
        id: { type: string }
        kind: { type: string, enum: [intro, announcement] }
        status: { type: string, enum: [draft, published, archived] }
        title: { type: string }
        summary: { type: string }
        blocks:
          type: array
          items: { $ref: "#/components/schemas/DreamUPContentBlock" }
        version: { type: integer, minimum: 1 }
        revision: { type: integer, minimum: 1 }
        publishedAt: { type: [string, "null"], format: date-time }
        updatedAt: { type: string, format: date-time }

    DreamUPContactSubmission:
      type: object
      additionalProperties: false
      required: [id, kind, category, payload, createdAt, resolution]
      properties:
        id: { type: string }
        kind: { type: string, enum: [feedback, sponsorship] }
        category: { type: string, enum: [feedback, emergency, sponsorship] }
        payload:
          oneOf:
            - type: object
              additionalProperties: false
              required: [content]
              properties:
                content: { type: string, minLength: 10, maxLength: 4000 }
            - type: object
              additionalProperties: false
              required: [category, eventType, location, contact, details]
              properties:
                category: { type: string, const: emergency }
                eventType: { type: string }
                location: { type: string }
                contact: { type: string }
                details: { type: string }
            - type: object
              additionalProperties: false
              required: [organization, contact, intent]
              properties:
                organization: { type: string, minLength: 2, maxLength: 160 }
                contact: { type: string, minLength: 3, maxLength: 254 }
                intent: { type: string, minLength: 10, maxLength: 2000 }
        createdAt: { type: string, format: date-time }
        resolution:
          oneOf:
            - type: object
              additionalProperties: false
              required: [status, version, note, updatedAt]
              properties:
                status: { type: string, enum: [acknowledged, closed] }
                version: { type: integer, minimum: 1 }
                note: { type: string, minLength: 2, maxLength: 1000 }
                updatedAt: { type: string, format: date-time }
            - type: "null"

    IdentityAccessField:
      type: string
      description: |
        Fixed restricted-field allowlist. Legal-document contents and legal-file
        identifiers are deliberately absent from this enum.
      enum: [legal_name, identity_number, identity_photo, contact_email, contact_mobile]

    IdentityAccessStatus:
      type: string
      enum: [pending, approved, rejected, revoked, expired]

    IdentityAccessCreateRequest:
      type: object
      additionalProperties: false
      required: [targetType, targetId, fields, reason]
      properties:
        targetType:
          type: string
          enum: [application, check_in]
        targetId:
          type: string
          minLength: 1
          maxLength: 160
          description: Opaque same-event application or check-in target.
        fields:
          type: array
          minItems: 1
          maxItems: 5
          uniqueItems: true
          items: {$ref: "#/components/schemas/IdentityAccessField"}
        reason:
          type: string
          writeOnly: true
          description: |
            NFKC-normalized business reason containing 20–500 UAX #29
            grapheme clusters. Stored encrypted and never returned.

    IdentityAccessDecisionRequest:
      type: object
      additionalProperties: false
      properties:
        fields:
          type: array
          minItems: 1
          maxItems: 5
          uniqueItems: true
          description: Optional strict subset used to reduce an approval.
          items: {$ref: "#/components/schemas/IdentityAccessField"}

    IdentityAccessRequest:
      type: object
      additionalProperties: false
      required: [requestId, eventId, targetType, targetId, fields, status, version]
      properties:
        requestId: {type: string}
        eventId: {type: string}
        targetType:
          type: string
          enum: [application, check_in]
        targetId: {type: string}
        fields:
          type: array
          items: {$ref: "#/components/schemas/IdentityAccessField"}
        status: {$ref: "#/components/schemas/IdentityAccessStatus"}
        version: {type: integer, format: int64, minimum: 1}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}
        expiresAt: {type: string, format: date-time}

    IdentityAccessRequestPage:
      type: object
      additionalProperties: false
      required: [items, page]
      properties:
        items:
          type: array
          items: {$ref: "#/components/schemas/IdentityAccessRequest"}
        page: {$ref: "#/components/schemas/CursorPageMetadata"}

    IdentityAccessDecisionResponse:
      type: object
      additionalProperties: false
      required: [requestId, status, version]
      properties:
        requestId: {type: string}
        status: {$ref: "#/components/schemas/IdentityAccessStatus"}
        version: {type: integer, format: int64, minimum: 1}
        replayed: {type: boolean, default: false}

    IdentityAccessClaimResponse:
      type: object
      additionalProperties: false
      required: [grantId, status, version]
      properties:
        grantId: {type: string}
        status:
          type: string
          enum: [claimed]
        version: {type: integer, format: int64, minimum: 1}
        replayed: {type: boolean, default: false}

    AdminStepUpState:
      type: string
      enum: [pending_enrollment, recovery_pending, active, must_rotate, disabled]

    AdminStepUpChallengeResponse:
      type: object
      additionalProperties: false
      required: [state]
      properties:
        state: {$ref: "#/components/schemas/AdminStepUpState"}
        question:
          type: string
          description: NFKC-normalized owner-only challenge question; absent while enrollment is pending.
        version: {type: integer, format: int64, minimum: 1}
        credentialVersion: {type: integer, format: int64, minimum: 1}
        lockedUntil: {type: string, format: date-time}

    AdminStepUpEnrollRequest:
      type: object
      additionalProperties: false
      required: [eventId, question, answer]
      properties:
        eventId: {type: string, minLength: 1, maxLength: 128}
        question:
          type: string
          description: NFKC-normalized question containing 5–200 UAX #29 grapheme clusters.
        answer:
          type: string
          writeOnly: true
          description: NFKC-normalized answer containing 7–256 UAX #29 grapheme clusters after outer trim.

    AdminStepUpVerifyRequest:
      type: object
      additionalProperties: false
      required: [eventId, answer, action, target]
      properties:
        eventId: {type: string, minLength: 1, maxLength: 128}
        answer: {type: string, writeOnly: true}
        action:
          type: string
          enum:
            - event.dashboard.read
            - event.application.read_basic
            - event.application.review
            - event.application.approve_admission
            - event.application.decide
            - event.registration.manage
            - event.checkin.scan
            - event.checkin.manage_window
            - event.identity_access.request
            - event.identity_access.validate_target
            - event.identity_access.consume
            - event.identity.read_restricted
            - event.legal.read
            - event.application.export
            - event.audit.read
            - event.contact_submission.manage
            - event.content.manage
            - system.role_migration.read
            - dreamup.admin.role.manage
            - dreamup.admin.event_registry.manage
            - dreamup.admin.oa.approve
            - dreamup.admin.restricted.view
            - dreamup.admin.legal.view
            - dreamup.admin.export
            - dreamup.admin.challenge.rotate
            - dreamup.admin.checkin_window.manage
        target:
          type: string
          maxLength: 900
          description: For high-risk DreamUP BFF actions, the exact server-defined canonical JSON tuple `["dreamup-admin-target/v1", eventId, resourceKind, resourceId]`; alternate JSON spellings, cross-event tuples and action/resource-kind mismatches are rejected. Ordinary and legacy actions use their documented stable target.

    AdminStepUpRotateRequest:
      type: object
      additionalProperties: false
      required: [eventId, oldAnswer, question, answer]
      properties:
        eventId: {type: string, minLength: 1, maxLength: 128}
        oldAnswer: {type: string, writeOnly: true}
        question:
          type: string
          description: Replacement question containing 5–200 UAX #29 grapheme clusters after NFKC.
        answer:
          type: string
          writeOnly: true
          description: Replacement answer containing 7–256 UAX #29 grapheme clusters after normalization and outer trim.

    AdminStepUpMutationResponse:
      type: object
      additionalProperties: false
      required: [state]
      properties:
        state: {$ref: "#/components/schemas/AdminStepUpState"}
        version: {type: integer, format: int64, minimum: 1}
        challengeVersion: {type: integer, format: int64, minimum: 1}
        verifiedAt: {type: string, format: date-time}
        expiresAt: {type: string, format: date-time}
        reauthenticationToken:
          type: string
          writeOnly: true
          minLength: 43
          maxLength: 43
          pattern: "^[A-Za-z0-9_-]{43}$"
          description: Newly minted action-and-target-bound single-use bearer for high-risk verification. Never returned by an idempotent replay.
        grantId:
          type: string
          description: Stable random grant identifier stored with the bearer data; it is not derived from the bearer.
        replayed: {type: boolean, default: false}

    HealthStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [ok, ready, "not ready"]
          description: Liveness or readiness status

    WeChatRegistrationCreateRequest:
      type: object
      additionalProperties: false
      required: [username, displayName, email, password, acceptedTerms, loginCode, phoneCode]
      properties:
        username: {type: string, minLength: 3, maxLength: 64}
        displayName: {type: string, minLength: 1, maxLength: 100}
        email: {type: string, format: email, maxLength: 254}
        password: {type: string, writeOnly: true, minLength: 12, maxLength: 128}
        acceptedTerms: {type: boolean, enum: [true]}
        requestId:
          type: string
          maxLength: 200
          description: Optional correlation value copied into the email-verification return link; it is not an idempotency key or replay receipt.
        loginCode:
          type: string
          writeOnly: true
          minLength: 1
          maxLength: 2048
        phoneCode:
          type: string
          writeOnly: true
          minLength: 1
          maxLength: 2048

    WeChatLoginRequest:
      type: object
      additionalProperties: false
      required: [code]
      properties:
        code:
          type: string
          writeOnly: true
          minLength: 1
          maxLength: 2048
          description: Fresh `wx.login` one-time code. Never stored or returned.

    WeChatOnboardingBeginRequest:
      type: object
      additionalProperties: false
      required: [loginCode]
      properties:
        loginCode:
          type: string
          writeOnly: true
          minLength: 1
          maxLength: 2048
          description: Fresh `wx.login` one-time code; the server derives the AppID-scoped subject.
        phoneCode:
          type: string
          writeOnly: true
          minLength: 1
          maxLength: 2048
          description: Optional code supplied by the same `getPhoneNumber` action. Absence or provider rejection continues without a phone.

    WeChatOnboardingRequiredResponse:
      type: object
      additionalProperties: false
      required: [status, onboardingToken, expiresAt]
      properties:
        status: {type: string, enum: [onboarding_required]}
        onboardingToken:
          type: string
          pattern: "^[A-Za-z0-9_-]{43}$"
          description: Restricted, single-use account-setup challenge. It is not a United Pass session bearer.
        expiresAt: {type: string, format: date-time}

    WeChatOnboardingCompleteRequest:
      type: object
      additionalProperties: false
      required: [onboardingToken, username, displayName, email, password, acceptedTerms]
      properties:
        onboardingToken:
          type: string
          writeOnly: true
          pattern: "^[A-Za-z0-9_-]{43}$"
        username:
          type: string
          minLength: 3
          maxLength: 64
          description: Used only when a new account is created; never overwrites an existing account.
        displayName:
          type: string
          minLength: 1
          maxLength: 100
          description: Used only when a new account is created; never overwrites an existing account.
        email: {type: string, format: email, maxLength: 254}
        password:
          type: string
          writeOnly: true
          minLength: 1
          maxLength: 128
          description: New credential for a new email, or current credential proof for an existing account. It is never persisted by United Pass.
        acceptedTerms: {type: boolean, enum: [true]}
        requestId:
          type: string
          maxLength: 200
          description: Optional correlation value copied into a new account's email-verification return link; it is not an idempotency key or replay receipt.

    WeChatOnboardingVerificationRequiredResponse:
      type: object
      additionalProperties: false
      required: [status, registrationToken, expiresAt]
      properties:
        status: {type: string, enum: [verification_required]}
        registrationToken:
          type: string
          description: Short-lived opaque capability for the existing registration email lifecycle; not an account session.
        expiresAt: {type: string, format: date-time}

    WeChatOnboardingMFARequiredResponse:
      type: object
      additionalProperties: false
      description: |
        The existing account requires its registered second factor before the
        verified WeChat identity can be linked. `mfaToken` is a dedicated,
        single-use onboarding token and must be submitted only to
        `POST /api/v1/auth/wechat/onboarding/mfa`. The current DreamUP Mini
        Program UI completes TOTP and fails closed for unsupported methods.
      required: [status, mfaToken, availableMethods, expiresAt]
      properties:
        status: {type: string, enum: [mfa_required]}
        mfaToken:
          type: string
          pattern: "^[A-Za-z0-9_-]{43}$"
          description: Opaque five-minute onboarding MFA token; it contains no provider credential.
        availableMethods:
          type: array
          minItems: 1
          items: {type: string, enum: [totp, passkey, recovery_code]}
        passkeyRequestOptions:
          type: object
          additionalProperties: true
          description: Provider WebAuthn request options when passkey is available; absent for TOTP-only accounts.
        expiresAt: {type: string, format: date-time}

    WeChatOnboardingMFARequest:
      type: object
      additionalProperties: false
      required: [mfaToken, method]
      properties:
        mfaToken:
          type: string
          writeOnly: true
          pattern: "^[A-Za-z0-9_-]{43}$"
        method:
          type: string
          enum: [totp, passkey, recovery_code]
        code:
          type: string
          writeOnly: true
          maxLength: 256
        passkeyAssertion:
          type: object
          writeOnly: true
          additionalProperties: true

    WeChatPhoneBindRequest:
      type: object
      additionalProperties: false
      required: [loginCode, phoneCode]
      properties:
        loginCode:
          type: string
          writeOnly: true
          minLength: 1
          maxLength: 2048
          description: Fresh `wx.login` code proving the linked WeChat identity.
        phoneCode:
          type: string
          writeOnly: true
          minLength: 1
          maxLength: 2048
          description: Fresh `getPhoneNumber` code. Never treated as a phone number.

    WeChatPhoneBindResponse:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        status: {type: string, enum: [verified]}

    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          $ref: "#/components/schemas/ErrorBody"

    ErrorBody:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
          description: Stable, machine-readable error code
          examples:
            internal: internal.unexpected
            validation: validation.failed
            reauth: session.reauthentication_required
        message:
          type: string
          description: Safe, user-displayable message
          example: "服务暂时无法处理该请求。"
        requestId:
          type: string
          description: Request ID for troubleshooting and audit correlation
          example: req_0123456789abcdef
        fieldErrors:
          type: array
          description: Field-level validation errors (array format)
          items:
            $ref: "#/components/schemas/FieldError"
        stepUp:
          $ref: "#/components/schemas/StepUpDetails"

    StepUpDetails:
      type: object
      additionalProperties: false
      required: [challengeToken, level, method, expiresAt, providerReady, completionPath]
      properties:
        challengeToken: {type: string, minLength: 1}
        level: {type: string, enum: [medium, high]}
        method: {type: string, enum: [automation_cost, interactive_captcha, mfa, reauth]}
        expiresAt: {type: string, format: date-time}
        algorithm:
          type: string
          enum: [sha256_leading_zero_bits]
          description: Present only for automation_cost; this is not human verification.
        difficulty: {type: integer, minimum: 1, maximum: 30}
        provider:
          type: string
          description: |
            Server-selected identifier. Current interactive values are
            `cloudflare_turnstile` and `google_recaptcha`; clients cannot
            choose or override this value.
        providerReady:
          type: boolean
          description: False means high-risk verification is fail-closed until a provider is configured.
        providerPayload:
          type: object
          additionalProperties: true
          description: |
            Server-issued public SDK parameters. Turnstile uses exactly
            `siteKey`, `action`, and `cdata`; reCAPTCHA uses exactly `siteKey`
            and the unique per-challenge `action`. No provider secret or URL
            is returned.
        completionPath:
          type: string
          enum: [/api/v1/auth/step-up, /api/v1/auth/sessions/mfa, /api/v1/auth/reauthentication]
          description: Exact same-origin endpoint that completes the selected method.

    StepUpCompletionRequest:
      type: object
      additionalProperties: false
      required: [challengeToken]
      properties:
        challengeToken: {type: string, minLength: 1, maxLength: 512, writeOnly: true}
        nonce:
          type: string
          maxLength: 256
          description: automation_cost nonce; omitted for interactive_captcha.
        providerProof:
          type: string
          maxLength: 8192
          writeOnly: true
          description: Opaque interactive-provider proof; omitted for automation_cost.

    FieldError:
      type: object
      required: [field, message]
      properties:
        field:
          type: string
          description: The field that failed validation
          example: redirectUris[0]
        message:
          type: string
          description: Safe, user-displayable field error message
          example: "该重定向地址未登记。"

    RegistrationCreateRequest:
      type: object
      additionalProperties: false
      required: [username, displayName, email, password, acceptedTerms, formIntentToken, automationBrief]
      properties:
        username:
          type: string
          pattern: "^[A-Za-z0-9][A-Za-z0-9._-]{2,63}$"
        displayName: {type: string, minLength: 1, maxLength: 100}
        email: {type: string, format: email, maxLength: 254}
        password:
          type: string
          format: password
          minLength: 12
          maxLength: 128
          writeOnly: true
        acceptedTerms: {type: boolean, const: true}
        requestId: {type: string, maxLength: 200}
        formIntentToken:
          type: string
          minLength: 1
          maxLength: 512
          writeOnly: true
          description: One-use server-issued registration form intent.
        automationBrief:
          type: string
          maxLength: 0
          writeOnly: true
          description: Must be present and empty in the supported browser client.

    RegistrationFormIntentResponse:
      type: object
      additionalProperties: false
      required: [formIntentToken, expiresAt]
      properties:
        formIntentToken: {type: string, minLength: 1, maxLength: 512}
        expiresAt: {type: string, format: date-time}

    RegistrationCreateResponse:
      type: object
      additionalProperties: false
      required: [status, registrationToken, expiresAt]
      properties:
        status: {type: string, const: verification_required}
        registrationToken:
          type: string
          minLength: 1
          description: Short-lived opaque token usable only to resend this registration's email.
        expiresAt: {type: string, format: date-time}

    RegistrationVerifyRequest:
      type: object
      additionalProperties: false
      required: [userId, code]
      properties:
        userId: {type: string, pattern: "^user_[0-9a-f]{32}$"}
        code: {type: string, minLength: 1, maxLength: 256, writeOnly: true}
        requestId: {type: string, maxLength: 200}

    RegistrationVerifyResponse:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        status: {type: string, const: verified}
        requestId:
          type: string
          maxLength: 200
          description: Validated authorization request ID to restore through `/login?requestId=`.

    RegistrationResendRequest:
      type: object
      additionalProperties: false
      required: [registrationToken]
      properties:
        registrationToken: {type: string, minLength: 1, maxLength: 512, writeOnly: true}

    RegistrationResendResponse:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        status: {type: string, const: verification_sent}

    RegistrationAbuseBlockRevokeRequest:
      type: object
      additionalProperties: false
      required: [dimension, value, valueEncoding, reasonCode]
      properties:
        dimension:
          type: string
          enum: [device, ip]
        value:
          type: string
          minLength: 1
          maxLength: 64
          writeOnly: true
          description: Canonical IP address, exact server-issued 256-bit base64url risk-device token, or its audited digest when allowed by valueEncoding.
        valueEncoding:
          type: string
          enum: [raw, sha256]
          description: sha256 is accepted only with dimension=device; IP always uses raw canonicalization and boundary hashing.
        reasonCode:
          type: string
          enum: [false_positive, support_verified, test_cleanup, incident_response]
      allOf:
        - if:
            required: [dimension]
            properties:
              dimension: {const: ip}
          then:
            properties:
              valueEncoding: {const: raw}
        - if:
            required: [valueEncoding]
            properties:
              valueEncoding: {const: sha256}
          then:
            properties:
              dimension: {const: device}
              value: {type: string, pattern: "^[0-9a-f]{64}$"}

    LoginRequest:
      type: object
      description: |
        Credentials used to establish a browser session. `identifier` accepts
        either an account name or an email address. Authentication failures
        return generic errors that do not reveal which value was incorrect.
      required: [identifier, password]
      additionalProperties: false
      properties:
        identifier:
          type: string
          minLength: 1
          description: Account name or email address used to identify the user.
          example: zhixing.lin
        password:
          type: string
          minLength: 1
          format: password
          description: User-entered password. Never logged or persisted in plaintext.
          writeOnly: true
        remember:
          type: boolean
          default: false
          description: |
            When true, the session cookie is issued with an extended lifetime
            suitable for a "remember me" experience. When false or omitted, the
            session uses the default browser-session lifetime.
        resumeRequestId:
          type: string
          description: |
            Opaque server-issued authorization transaction ID to resume after
            successful login. When present and valid, login redirects to the
            authorization resume flow instead of the default account or admin
            destination. Only an opaque server-issued ID is accepted — never a
            raw return URL.
          pattern: "^[a-zA-Z0-9_:-]{8,128}$"
          example: req_01ABCdefGHIjkl

    MiniProgramLoginRequest:
      type: object
      additionalProperties: false
      required: [identifier, password]
      properties:
        identifier:
          type: string
          minLength: 1
          description: Account name or email address.
        password:
          type: string
          minLength: 1
          format: password
          writeOnly: true
        remember:
          type: boolean
          const: false
          default: false
          description: Native Mini Program sessions cannot opt into the extended remember lifetime.

    MiniProgramSessionResponse:
      type: object
      additionalProperties: false
      required: [status, sessionBearer, expiresAt]
      properties:
        status:
          type: string
          const: authenticated
        sessionBearer:
          type: string
          pattern: "^[A-Za-z0-9_-]{43}$"
          readOnly: true
          description: Short-lived opaque native bearer. Keep only in process memory and never persist it.
        expiresAt:
          type: string
          format: date-time
          readOnly: true
          description: Fixed server-side absolute expiry, no later than 30 minutes after issuance.

    DreamUPMobileAssertionResponse:
      type: object
      additionalProperties: false
      required: [assertion, expiresAt]
      properties:
        assertion:
          type: string
          minLength: 1
          readOnly: true
          description: One short-lived request-bound DreamUP assertion; it is not a United Pass session credential.
        expiresAt:
          type: string
          format: date-time
          readOnly: true

    MiniProgramMFAChallengeRequest:
      type: object
      additionalProperties: false
      required: [mfaToken, method, code]
      properties:
        mfaToken:
          type: string
          pattern: "^[A-Za-z0-9_-]{43}$"
          writeOnly: true
        method:
          type: string
          const: totp
        code:
          type: string
          pattern: "^[0-9]{6}$"
          writeOnly: true

    MFARequiredResponse:
      type: object
      description: |
        Returned when the account requires multi-factor authentication before a
        session can be established. The `mfaToken` is an opaque, randomly
        generated United Pass token — it never contains provider credentials —
        and must be submitted to `POST /api/v1/auth/sessions/mfa`.
      required: [status, mfaToken, availableMethods, expiresAt]
      additionalProperties: false
      properties:
        status:
          type: string
          enum: [mfa_required]
          description: Indicates that MFA is required to continue.
        mfaToken:
          type: string
          minLength: 1
          description: |
            Opaque, short-lived token used to complete the MFA challenge. It is
            generated server-side and never embeds provider session credentials.
          example: opaque_mfa_token
        availableMethods:
          type: array
          description: MFA methods the user may use to complete the challenge.
          items:
            type: string
            enum: [totp, passkey, recovery_code]
          example: [totp, passkey, recovery_code]
        passkeyRequestOptions:
          type: object
          description: |
            WebAuthn PublicKeyCredentialRequestOptions returned when `passkey`
            is among the available methods. The browser must pass it to
            `navigator.credentials.get()` and submit the resulting assertion
            as `passkeyAssertion` to the MFA endpoint.
          example:
            publicKey:
              challenge: "abc123"
              rpId: login.example.com
              timeout: 60000
        expiresAt:
          type: string
          format: date-time
          description: |
            ISO 8601 UTC timestamp after which the `mfaToken` is no longer
            accepted.
          example: "2026-08-05T12:05:30Z"

    MFAChallengeRequest:
      type: object
      description: |
        Submission of an MFA verification code or assertion to complete a
        challenge started by `POST /api/v1/auth/sessions`. The payload depends
        on `method`: `totp` requires `code`; `passkey` requires
        `passkeyAssertion`; `recovery_code` is not supported in Phase 1.2.
      required: [mfaToken, method]
      additionalProperties: false
      properties:
        mfaToken:
          type: string
          minLength: 1
          description: Opaque MFA token returned in the login `202` response.
          example: opaque_mfa_token
        method:
          type: string
          enum: [totp, passkey, recovery_code]
          description: The MFA method being used to satisfy the challenge.
          example: totp
        code:
          type: string
          minLength: 1
          description: |
            User-entered verification value. Required for `totp` (a 6-digit
            code). Never logged.
          writeOnly: true
          example: "123456"
        passkeyAssertion:
          type: object
          description: |
            The WebAuthn assertion returned by `navigator.credentials.get()`,
            submitted verbatim. Required for `passkey`. Never logged.
          writeOnly: true
          example:
            id: assertion-id
            rawId: "base64"
            response:
              authenticatorData: "base64"
              signature: "base64"
              clientDataJSON: "base64"

    UserPersona:
      type: string
      enum: [consumer, employee]
      description: |
        A persona attached to a stable United Pass user. An external user who
        becomes an employee keeps the same `userId` and retains the consumer
        persona.

    EmployeeProfile:
      type: object
      description: |
        Employee profile linked to the stable user identity. Optional and kept
        as historical display/audit data after offboarding; it is never a
        second account.
      required: [employeeId, departmentName, title]
      additionalProperties: false
      properties:
        employeeId:
          type: string
          description: Employee number unique within its scope.
          example: EMP-10032
        departmentName:
          type: string
          description: Display name of the employee's department.
          example: 平台架构部
        title:
          type: string
          description: Employee position or title.
          example: 高级工程师

    CurrentUser:
      type: object
      description: |
        Profile of the authenticated user. The `userId` is the stable United
        Pass identity primary key — not an email, phone number or external
        Provider identifier. `employeeProfile` is present when an employee
        profile exists, including historical offboarding profiles.
      required: [userId, displayName, email, phoneMasked, personas]
      additionalProperties: false
      properties:
        userId:
          type: string
          description: Stable United Pass user ID, used as the OIDC subject.
          example: usr_01HQFGXKN8H2C4JVMW7K3R4D8Z
        displayName:
          type: string
          description: User-facing display name.
          example: 林知行
        nickname:
          type: string
          nullable: true
          description: Optional user-chosen nickname.
          example: 知行
        avatarUrl:
          type: string
          format: uri
          nullable: true
          description: |
            Same-origin or controlled media domain URL of the user's avatar.
            Stored after server-side validation, re-encoding and metadata
            stripping. The frontend never submits an external image URL.
          example: https://cdn.example.com/avatars/usr_01HQFGXKN8H2C4JVMW7K3R4D8Z.webp
        email:
          type: string
          format: email
          description: Verified or primary email address.
          example: zhixing.lin@example.com
        phoneMasked:
          type: string
          description: Masked phone number safe for user display.
          example: "+86 138****0123"
        personas:
          type: array
          description: Personas attached to the user identity.
          items:
            $ref: "#/components/schemas/UserPersona"
          example: [consumer, employee]
        employeeProfile:
          allOf:
            - $ref: "#/components/schemas/EmployeeProfile"
          nullable: true
          description: Employee profile, including historical offboarding state.

    PermissionCapabilities:
      type: object
      description: |
        User-experience permission summary computed from authoritative backend
        attributes and Cerbos decisions. These capabilities drive frontend
        navigation visibility and control availability only. Every protected
        backend operation must independently authorize via Cerbos; frontend
        checks are not security controls.
      required:
        - userRead
        - userDisable
        - employeeManage
        - employeeOffboard
        - departmentManage
        - applicationRead
        - applicationManage
        - applicationSecretRotate
        - policyRead
        - policyManage
        - policyPublish
        - auditRead
        - auditExport
        - providerRead
        - providerManage
      additionalProperties: false
      properties:
        userRead:
          type: boolean
          description: May read managed user listings and user detail.
        userDisable:
          type: boolean
          description: May disable or enable users and revoke their sessions.
        employeeManage:
          type: boolean
          description: May create, link and update employee profiles.
        employeeOffboard:
          type: boolean
          description: May start employee offboarding and revoke access.
        departmentManage:
          type: boolean
          description: May create, update and delete departments.
        applicationRead:
          type: boolean
          description: May read OAuth application and client metadata.
        applicationManage:
          type: boolean
          description: May create, update and delete applications and clients.
        applicationSecretRotate:
          type: boolean
          description: May rotate confidential client secrets.
        policyRead:
          type: boolean
          description: May read authorization policies and versions.
        policyManage:
          type: boolean
          description: May create and update policy drafts.
        policyPublish:
          type: boolean
          description: May publish a policy version.
        auditRead:
          type: boolean
          description: May query and filter audit events.
        auditExport:
          type: boolean
          description: May create and download audit export jobs.
        providerRead:
          type: boolean
          description: May read identity provider metadata.
        providerManage:
          type: boolean
          description: May create, update, enable and disable identity providers.

    CursorPageMetadata:
      type: object
      required: [nextCursor, hasMore]
      additionalProperties: false
      properties:
        nextCursor:
          type: string
          nullable: true
          description: Opaque signed cursor; null when no next page exists.
        hasMore:
          type: boolean

    ManagedUser:
      type: object
      required: [userId, displayName, email, personaLabel, status, lastActiveAt]
      additionalProperties: false
      properties:
        userId:
          type: string
        displayName:
          type: string
        email:
          type: string
          format: email
        personaLabel:
          type: string
        status:
          type: string
          enum: [active, disabled, pending]
        lastActiveAt:
          type: string
          format: date-time

    ManagedUserListResponse:
      type: object
      required: [items, page]
      additionalProperties: false
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ManagedUser"
        page:
          $ref: "#/components/schemas/CursorPageMetadata"

    ManagedUserSession:
      type: object
      required: [sessionId, deviceName, lastActiveAt, isCurrent]
      additionalProperties: false
      properties:
        sessionId:
          type: string
        deviceName:
          type: string
        lastActiveAt:
          type: string
          format: date-time
        isCurrent:
          type: boolean

    LinkedIdentity:
      type: object
      required: [providerId, providerName, externalSubject, linkedAt]
      additionalProperties: false
      properties:
        providerId:
          type: string
        providerName:
          type: string
        externalSubject:
          type: string
        linkedAt:
          type: string
          format: date-time

    UserAuthorization:
      type: object
      required: [applicationName, scopes, grantedAt, status]
      additionalProperties: false
      properties:
        applicationName:
          type: string
        scopes:
          type: array
          items:
            type: string
        grantedAt:
          type: string
          format: date-time
        status:
          type: string
          enum: [active, revoked]

    AdminAuditEvent:
      type: object
      required:
        [eventId, eventType, actorName, actorId, targetLabel, targetId,
         occurredAt, result, requestId, details]
      additionalProperties: false
      properties:
        eventId:
          type: string
        eventType:
          type: string
        actorName:
          type: string
        actorId:
          type: string
        targetLabel:
          type: string
        targetId:
          type: string
        occurredAt:
          type: string
          format: date-time
        result:
          type: string
          enum: [success, denied]
        requestId:
          type: string
        details:
          type: string

    ManagedUserDetail:
      type: object
      required:
        [userId, displayName, email, phoneMasked, personaLabel, status,
         lastActiveAt, personas, linkedIdentities, activeSessions,
         authorizedApplications, recentAuditEvents]
      additionalProperties: false
      properties:
        userId:
          type: string
        displayName:
          type: string
        email:
          type: string
          format: email
        phoneMasked:
          type: string
        personaLabel:
          type: string
        status:
          type: string
          enum: [active, disabled, pending]
        lastActiveAt:
          type: string
          format: date-time
        personas:
          type: array
          items:
            $ref: "#/components/schemas/UserPersona"
        employeeProfile:
          allOf:
            - $ref: "#/components/schemas/EmployeeProfile"
          nullable: true
        linkedIdentities:
          type: array
          items:
            $ref: "#/components/schemas/LinkedIdentity"
        activeSessions:
          type: array
          items:
            $ref: "#/components/schemas/ManagedUserSession"
        authorizedApplications:
          type: array
          items:
            $ref: "#/components/schemas/UserAuthorization"
        recentAuditEvents:
          type: array
          items:
            $ref: "#/components/schemas/AdminAuditEvent"

    DisableUserRequest:
      type: object
      required: [revokeSessions]
      additionalProperties: false
      properties:
        revokeSessions:
          type: boolean

    AccessCleanupStatus:
      type: object
      required: [status, sessionCleanupPending]
      additionalProperties: false
      properties:
        status:
          type: string
          enum: [disabled, offboarding]
        sessionCleanupPending:
          type: boolean

    EmployeeRecord:
      type: object
      required:
        [userId, displayName, employeeId, departmentName, title, status]
      additionalProperties: false
      properties:
        userId:
          type: string
        displayName:
          type: string
        employeeId:
          type: string
        departmentName:
          type: string
        title:
          type: string
        status:
          type: string
          enum: [active, offboarding]

    EmployeeListResponse:
      type: object
      required: [items, page]
      additionalProperties: false
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/EmployeeRecord"
        page:
          $ref: "#/components/schemas/CursorPageMetadata"

    EmployeeDetail:
      type: object
      required:
        [userId, displayName, email, employeeId, departmentName,
         departmentId, title, status, supervisorUserId, supervisorName, onboardedAt,
         linkedConsumerAccount]
      additionalProperties: false
      properties:
        userId:
          type: string
        displayName:
          type: string
        email:
          type: string
          format: email
        employeeId:
          type: string
        departmentName:
          type: string
        departmentId:
          type: string
        title:
          type: string
        status:
          type: string
          enum: [active, offboarding]
        supervisorName:
          type: string
          nullable: true
        supervisorUserId:
          type: string
          nullable: true
        onboardedAt:
          type: string
          format: date-time
        linkedConsumerAccount:
          type: boolean

    EmployeeProfileMutation:
      type: object
      required: [userId, departmentId, title]
      additionalProperties: false
      properties:
        userId:
          type: string
          description: Existing stable user ID selected explicitly by an administrator.
        departmentId:
          type: string
        title:
          type: string
          minLength: 1
          maxLength: 120
        supervisorUserId:
          type: string
          description: Optional stable user ID of an active employee supervisor.

    EmployeeProfileUpdate:
      type: object
      required: [departmentId, title]
      additionalProperties: false
      properties:
        departmentId:
          type: string
        title:
          type: string
          minLength: 1
          maxLength: 120
        supervisorUserId:
          type: string

    EmployeeProfileMutationResult:
      type: object
      required:
        [userId, employeeId, departmentName, departmentId, title, status,
         supervisorName, onboardedAt]
      additionalProperties: false
      properties:
        userId:
          type: string
        employeeId:
          type: string
        departmentName:
          type: string
        departmentId:
          type: string
        title:
          type: string
        status:
          type: string
          enum: [active, offboarding]
        supervisorName:
          type: string
          nullable: true
        onboardedAt:
          type: string
          format: date-time

    DepartmentRecord:
      type: object
      required: [departmentId, name, parentName, memberCount, ownerName]
      additionalProperties: false
      properties:
        departmentId:
          type: string
        name:
          type: string
        parentName:
          type: string
        memberCount:
          type: integer
          minimum: 0
        ownerName:
          type: string

    DepartmentChild:
      type: object
      required: [departmentId, name, memberCount]
      additionalProperties: false
      properties:
        departmentId:
          type: string
        name:
          type: string
        memberCount:
          type: integer
          minimum: 0

    DepartmentMember:
      type: object
      required: [userId, displayName, title, employeeId]
      additionalProperties: false
      properties:
        userId:
          type: string
        displayName:
          type: string
        title:
          type: string
        employeeId:
          type: string

    DepartmentDetail:
      type: object
      required:
        [departmentId, name, parentDepartmentId, parentName, ownerUserId, ownerName,
         memberCount, childDepartments, members]
      additionalProperties: false
      properties:
        departmentId:
          type: string
        name:
          type: string
        parentDepartmentId:
          type: string
          nullable: true
        parentName:
          type: string
          nullable: true
        ownerName:
          type: string
        ownerUserId:
          type: string
          nullable: true
        memberCount:
          type: integer
          minimum: 0
        childDepartments:
          type: array
          items:
            $ref: "#/components/schemas/DepartmentChild"
        members:
          type: array
          items:
            $ref: "#/components/schemas/DepartmentMember"

    DepartmentMutation:
      type: object
      required: [name]
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
        parentDepartmentId:
          type: string
          description: Omit or send an empty string for a root department.
        ownerUserId:
          type: string
          description: Omit or send an empty string when no owner is assigned.

    DepartmentPatch:
      type: object
      minProperties: 1
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
        parentDepartmentId:
          type: string
          nullable: true
          description: Null clears the parent and makes the department a root.
        ownerUserId:
          type: string
          nullable: true
          description: Null clears the owner.

    ScopeCatalogItem:
      type: object
      description: One entry of the backend-authoritative OAuth scope catalog.
      required: [scope, label, description, required]
      additionalProperties: false
      properties:
        scope:
          type: string
          example: reporting:read
        label:
          type: string
          example: 报表读取
        description:
          type: string
          example: 允许读取报表数据
        required:
          type: boolean
          description: Whether the scope is mandatory for the client profile.

    ReauthenticationRequest:
      type: object
      description: |
        Declares the high-risk action being prepared and verifies the current
        session user's password. Target resource IDs must match the eventual
        operation; the grant is bound to them.
      required: [action, password]
      additionalProperties: false
      properties:
        action:
          type: string
          enum:
            - application.delete
            - client.delete
            - client.secret.rotate
            - account.password.change
            - account.email.change
            - account.totp.enroll
            - account.totp.remove
            - account.passkey.enroll
            - account.passkey.remove
            - user.disable
            - user.sessions.revoke
            - employee.offboard
            - registration.abuse.unblock
          description: The high-risk action the grant will authorize.
        applicationId:
          type: string
          description: Target application for application-scoped actions; must be empty for account actions.
        clientId:
          type: string
          description: Target client (required for client actions).
        target:
          type: string
          description: |
            Required for `account.passkey.remove` (passkey ID) and for the
            Phase 5 target-user actions (stable user ID). For
            `registration.abuse.unblock`, use the exact server-defined binding
            `<dimension>_<sha256>` returned or derived by the operator tool;
            the revoke request must present a grant bound to the same target.
            Forbidden for actions that do not define a target.
        password:
          type: string
          minLength: 1
          format: password
          writeOnly: true
          description: Current session user's password. Never logged.

    ReauthenticationGrantResponse:
      type: object
      required: [status, reauthToken, expiresAt]
      additionalProperties: false
      properties:
        status:
          type: string
          enum: [granted]
        reauthToken:
          type: string
          description: |
            Single-use grant token submitted as `X-Reauthentication-Token`.
            Consumed atomically by the target operation.
        expiresAt:
          type: string
          format: date-time

    ReauthenticationChallengeResponse:
      type: object
      description: Returned with 202 when a second factor must complete verification.
      required: [status, reauthToken, availableMethods, expiresAt]
      additionalProperties: false
      properties:
        status:
          type: string
          enum: [mfa_required]
        reauthToken:
          type: string
          description: Opaque challenge token for `POST /api/v1/auth/reauthentication/mfa`.
        availableMethods:
          type: array
          items:
            type: string
            enum: [totp, passkey]
        passkeyRequestOptions:
          type: object
          description: WebAuthn request options when `passkey` is available.
        expiresAt:
          type: string
          format: date-time

    ReauthenticationMfaRequest:
      type: object
      required: [reauthToken, method]
      additionalProperties: false
      properties:
        reauthToken:
          type: string
          minLength: 1
          description: Challenge token from the `202` response.
        method:
          type: string
          enum: [totp, passkey]
        code:
          type: string
          minLength: 1
          writeOnly: true
          description: TOTP code; required for `totp`. Never logged.
        passkeyAssertion:
          type: object
          writeOnly: true
          description: WebAuthn assertion; required for `passkey`. Never logged.

    SecuritySummary:
      type: object
      required: [password, totp, passkeys, recoveryCodes]
      additionalProperties: false
      properties:
        password:
          type: object
          required: [set]
          additionalProperties: false
          properties:
            set: { type: boolean }
        totp:
          type: object
          required: [enabled]
          additionalProperties: false
          properties:
            enabled: { type: boolean }
        passkeys:
          type: array
          items:
            $ref: "#/components/schemas/PasskeySummary"
        recoveryCodes:
          type: object
          required: [available, deferredReason]
          additionalProperties: false
          properties:
            available:
              type: boolean
              const: false
            deferredReason:
              type: string
              const: provider_unsupported

    PasswordChangeRequest:
      type: object
      required: [newPassword]
      additionalProperties: false
      properties:
        newPassword:
          type: string
          minLength: 1
          format: password
          writeOnly: true
          description: New password only. The current password is never accepted by this endpoint.

    TotpEnrollmentResponse:
      type: object
      required: [enrollmentToken, secret, otpauthUri]
      additionalProperties: false
      properties:
        enrollmentToken:
          type: string
          minLength: 1
          writeOnly: true
          description: Single-use user/session-bound confirmation capability.
        secret:
          type: string
          minLength: 1
          writeOnly: true
          description: TOTP seed returned exactly once; never logged or persisted by United Pass.
        otpauthUri:
          type: string
          minLength: 1
          pattern: "^otpauth://"
          writeOnly: true
          description: Secret-bearing authenticator URI returned exactly once.

    TotpEnrollmentConfirmRequest:
      type: object
      required: [enrollmentToken, code]
      additionalProperties: false
      properties:
        enrollmentToken:
          type: string
          minLength: 1
          writeOnly: true
        code:
          type: string
          minLength: 1
          writeOnly: true
          description: User-entered TOTP code. Never logged.

    TotpEnrollmentConfirmation:
      type: object
      required: [status]
      additionalProperties: false
      properties:
        status:
          type: string
          const: confirmed

    TotpEnrollmentCancelRequest:
      type: object
      required: [enrollmentToken]
      additionalProperties: false
      properties:
        enrollmentToken:
          type: string
          minLength: 1
          writeOnly: true

    UserSession:
      type: object
      required:
        - sessionId
        - deviceName
        - clientName
        - approximateLocation
        - ipAddressMasked
        - lastActiveAt
        - createdAt
        - authenticationMethods
        - isCurrent
      additionalProperties: false
      properties:
        sessionId:
          type: string
          minLength: 1
        deviceName:
          type: string
          description: Normalized OS/device family; empty means unknown.
        clientName:
          type: string
          description: Normalized browser/client family; empty means unknown.
        approximateLocation:
          oneOf:
            - type: string
            - type: "null"
          description: Always null in P4; no GeoIP fact is fabricated.
        ipAddressMasked:
          type: string
          description: Masked address only; empty means unknown.
        lastActiveAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        authenticationMethods:
          type: array
          items:
            type: string
        isCurrent:
          type: boolean
          description: Computed by the backend from the authenticated principal.

    RevokedSessionCount:
      type: object
      required: [revoked]
      additionalProperties: false
      properties:
        revoked:
          type: integer
          minimum: 0

    PasskeySummary:
      type: object
      required: [passkeyId, createdAt, state]
      additionalProperties: false
      properties:
        passkeyId:
          type: string
          minLength: 1
        createdAt:
          oneOf:
            - type: string
              format: date-time
            - type: "null"
        state:
          type: string
          enum: [active, pending]

    PasskeyEnrollmentResponse:
      type: object
      required: [enrollmentToken, passkeyId, publicKeyCredentialCreationOptions]
      additionalProperties: false
      properties:
        enrollmentToken:
          type: string
          minLength: 1
        passkeyId:
          type: string
          minLength: 1
        publicKeyCredentialCreationOptions:
          type: object
          description: WebAuthn JSON wire shape; binary fields are base64url.

    PasskeyEnrollmentConfirmRequest:
      type: object
      required: [enrollmentToken, publicKeyCredential]
      additionalProperties: false
      properties:
        enrollmentToken:
          type: string
          minLength: 1
        publicKeyCredential:
          type: object
          writeOnly: true
          description: Browser-created credential JSON. Never logged or persisted.
        passkeyName:
          type: string
          maxLength: 200

    PasskeyEnrollmentCancelRequest:
      type: object
      required: [enrollmentToken]
      additionalProperties: false
      properties:
        enrollmentToken:
          type: string
          minLength: 1
          writeOnly: true

    PasskeyEnrollmentConfirmation:
      type: object
      required: [status, passkeyId]
      additionalProperties: false
      properties:
        status:
          type: string
          const: confirmed
        passkeyId:
          type: string
          minLength: 1

    RedirectUriEntry:
      type: object
      required: [uri, isLoopback, addedAt]
      additionalProperties: false
      properties:
        uri:
          type: string
          description: Registered URI, stored exactly as submitted (no normalization).
        isLoopback:
          type: boolean
          description: True for the RFC 8252 loopback HTTP exception.
        addedAt:
          type: string
          format: date-time

    ClientSecretRecord:
      type: object
      description: Secret metadata only — never a secret value.
      required: [secretId, label, createdAt]
      additionalProperties: false
      properties:
        secretId:
          type: string
          example: sec_01HQFGXKN8H2C4JVMW7K3R4D8Z
        label:
          type: string
          example: 初始 Secret
        createdAt:
          type: string
          format: date-time
        lastRotatedAt:
          type: string
          format: date-time
          nullable: true

    ApplicationGrantSummary:
      type: object
      description: User grant summary. Always an empty array until Phase 3.
      required: [grantId, userLabel, scopes, grantedAt, status]
      additionalProperties: false
      properties:
        grantId:
          type: string
        userLabel:
          type: string
        scopes:
          type: array
          items:
            type: string
        grantedAt:
          type: string
          format: date-time
        lastUsedAt:
          type: string
          format: date-time
          nullable: true
        status:
          type: string
          enum: [active, revoked]

    ApplicationAuditEntry:
      type: object
      description: Security event actually recorded for the application (no fake data).
      required: [eventId, eventType, actorName, occurredAt, result]
      additionalProperties: false
      properties:
        eventId:
          type: string
        eventType:
          type: string
          example: oauth_client.secret_rotated
        actorName:
          type: string
        occurredAt:
          type: string
          format: date-time
        result:
          type: string
          enum: [success, denied]

    OAuthApplication:
      type: object
      description: Application list item.
      required:
        [applicationId, name, audience, ownerId, ownerName, status, clientCount, updatedAt]
      additionalProperties: false
      properties:
        applicationId:
          type: string
          example: app_01HQFGXKN8H2C4JVMW7K3R4D8Z
        name:
          type: string
        audience:
          type: string
          enum: [internal, external, hybrid]
        ownerId:
          type: string
          description: Stable owner user ID (authoritative; G1 fix).
        ownerName:
          type: string
          description: Derived display name of the owner; never authoritative.
        status:
          type: string
          enum: [active, disabled]
        clientCount:
          type: integer
          minimum: 0
        updatedAt:
          type: string
          format: date-time

    ApplicationListResponse:
      type: object
      required: [items, page]
      additionalProperties: false
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/OAuthApplication"
        page:
          type: object
          required: [nextCursor, hasMore]
          additionalProperties: false
          properties:
            nextCursor:
              type: string
              nullable: true
              description: Opaque HMAC-signed cursor; null when no more pages.
            hasMore:
              type: boolean

    OAuthClient:
      type: object
      description: OAuth client detail. Secret values are never included.
      required:
        [clientId, applicationId, name, clientType, grantTypes,
         tokenEndpointAuthMethod, redirectUris, allowedScopes, consentMode,
         status, clientSecrets, createdAt, updatedAt]
      additionalProperties: false
      properties:
        clientId:
          type: string
          example: clt_01HQFGXKN8H2C4JVMW7K3R4D8Z
        applicationId:
          type: string
        name:
          type: string
        clientType:
          type: string
          enum: [public, confidential]
        grantTypes:
          type: array
          items:
            type: string
            enum: [authorization_code, refresh_token, client_credentials]
        tokenEndpointAuthMethod:
          type: string
          enum: [client_secret_basic, none]
          description: |
            Only values with verified provider support and a reviewed security
            model are exposed in Phase 2 (`client_secret_post` and
            `private_key_jwt` are rejected with 422).
        redirectUris:
          type: array
          items:
            $ref: "#/components/schemas/RedirectUriEntry"
        logoutUri:
          type: string
          nullable: true
        allowedScopes:
          type: array
          items:
            $ref: "#/components/schemas/ScopeCatalogItem"
        consentMode:
          type: string
          enum: [always, first_authorization]
          description: |
            For `server_to_server` clients only `always` is accepted and it is
            recorded as not-applicable metadata.
        status:
          type: string
          enum: [active, disabled]
        clientSecrets:
          type: array
          description: Secret metadata; empty for public clients.
          items:
            $ref: "#/components/schemas/ClientSecretRecord"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    OAuthApplicationDetail:
      type: object
      required:
        [applicationId, name, description, audience, ownerId, ownerName,
         status, clients, grants, auditEntries, createdAt, updatedAt]
      additionalProperties: false
      properties:
        applicationId:
          type: string
        name:
          type: string
        description:
          type: string
        logoUrl:
          type: string
          format: uri
          nullable: true
        audience:
          type: string
          enum: [internal, external, hybrid]
        ownerId:
          type: string
        ownerName:
          type: string
        status:
          type: string
          enum: [active, disabled]
        clients:
          type: array
          items:
            $ref: "#/components/schemas/OAuthClient"
        grants:
          type: array
          description: Real grants only; always empty until Phase 3.
          items:
            $ref: "#/components/schemas/ApplicationGrantSummary"
        auditEntries:
          type: array
          description: Only security events actually recorded.
          items:
            $ref: "#/components/schemas/ApplicationAuditEntry"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ApplicationCreateInput:
      type: object
      required: [name, audience, ownerId]
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 2
          maxLength: 80
        description:
          type: string
          maxLength: 500
          default: ""
        audience:
          type: string
          enum: [internal, external, hybrid]
        ownerId:
          type: string
          description: |
            Stable United Pass user ID of the owner. Must exist; ownership is
            never resolved from a display name (G1 fix).

    OAuthClientCreateRequest:
      type: object
      required: [name, profile, redirectUris, allowedScopes, consentMode]
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 2
          maxLength: 64
        profile:
          type: string
          enum: [web_server, spa_mobile, server_to_server]
          description: |
            `server_to_server` is kept in the enum for forward compatibility
            but is rejected with a 422 `profile` field error: ZITADEL v2.71
            serves client_credentials tokens only for machine users, so a
            provisioned OIDC app could never execute the grant.
        redirectUris:
          type: array
          maxItems: 20
          description: |
            Exact strings; https or loopback http only; no fragments,
            userinfo, wildcards or normalization. Required for interactive
            profiles, forbidden for `server_to_server`.
          items:
            type: string
            maxLength: 2048
        logoutUri:
          type: string
          maxLength: 2048
          default: ""
          description: Same security checks as redirect URIs.
        allowedScopes:
          type: array
          description: Registered scope IDs only; duplicates rejected.
          items:
            type: string
        consentMode:
          type: string
          enum: [always, first_authorization]

    ApplicationWithInitialClientRequest:
      type: object
      required: [application, initialClient]
      additionalProperties: false
      properties:
        application:
          $ref: "#/components/schemas/ApplicationCreateInput"
        initialClient:
          $ref: "#/components/schemas/OAuthClientCreateRequest"

    ApplicationWithInitialClientResponse:
      type: object
      required: [applicationId, clientId]
      additionalProperties: false
      properties:
        applicationId:
          type: string
        clientId:
          type: string
        clientSecret:
          type: string
          writeOnly: true
          description: |
            Present exactly once for confidential clients; absent for public
            clients. Never persisted, never queryable afterwards.

    OAuthClientCreationResponse:
      type: object
      required: [clientId]
      additionalProperties: false
      properties:
        clientId:
          type: string
        clientSecret:
          type: string
          writeOnly: true
          description: Present exactly once for confidential clients.

    ApplicationUpdateRequest:
      type: object
      description: Partial update; at least one field required.
      minProperties: 1
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 2
          maxLength: 80
        description:
          type: string
          maxLength: 500
        audience:
          type: string
          enum: [internal, external, hybrid]
        ownerId:
          type: string
          description: New owner user ID; must exist.

    OAuthClientUpdateRequest:
      type: object
      description: |
        Partial update; at least one field required. Profile is immutable.
        When `redirectUris` is present it replaces the full set.
      minProperties: 1
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 2
          maxLength: 64
        redirectUris:
          type: array
          maxItems: 20
          items:
            type: string
            maxLength: 2048
        logoutUri:
          type: string
          maxLength: 2048
          nullable: true
        allowedScopes:
          type: array
          items:
            type: string
        consentMode:
          type: string
          enum: [always, first_authorization]

    SecretRotationResponse:
      type: object
      required: [secretId, clientSecret, previousSecretExpiresAt]
      additionalProperties: false
      properties:
        secretId:
          type: string
          description: United Pass secret ID for the new secret.
        clientSecret:
          type: string
          writeOnly: true
          description: The new secret, shown exactly once. Never persisted.
        previousSecretExpiresAt:
          type: string
          format: date-time
          description: |
            Expiry of the previous secret. Against ZITADEL v2.71 this equals
            the rotation timestamp (zero grace period).

    PublicLoginProvider:
      type: object
      required: [providerId, displayName, loginEnabled]
      additionalProperties: false
      properties:
        providerId:
          type: string
        displayName:
          type: string
        loginEnabled:
          type: boolean

    PublicLoginProviderList:
      type: object
      required: [items]
      additionalProperties: false
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/PublicLoginProvider"

    IdentityProviderSummary:
      type: object
      required:
        [providerId, displayName, vendor, integrationLabel, status,
         loginEnabled, linkedUserCount, updatedAt]
      additionalProperties: false
      properties:
        providerId:
          type: string
        displayName:
          type: string
        vendor:
          type: string
          enum: [feishu, generic]
        integrationLabel:
          type: string
        status:
          type: string
          enum: [planned, active, disabled]
        loginEnabled:
          type: boolean
        linkedUserCount:
          type: integer
          minimum: 0
        updatedAt:
          type: string
          format: date-time

    IdentityProviderListResponse:
      type: object
      required: [items, page]
      additionalProperties: false
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/IdentityProviderSummary"
        page:
          $ref: "#/components/schemas/CursorPageMetadata"

    IdentityProviderDetail:
      type: object
      required:
        [providerId, displayName, vendor, integrationLabel, status,
         loginEnabled, linkedUserCount, updatedAt, appId, secretConfigured,
         callbackUrl, contactScope, lastValidatedAt, lastSyncAt, lastSyncResult]
      additionalProperties: false
      properties:
        providerId:
          type: string
        displayName:
          type: string
        vendor:
          type: string
          enum: [feishu, generic]
        integrationLabel:
          type: string
        status:
          type: string
          enum: [planned, active, disabled]
        loginEnabled:
          type: boolean
        linkedUserCount:
          type: integer
          minimum: 0
        updatedAt:
          type: string
          format: date-time
        appId:
          type: string
          description: Non-secret application identifier.
        secretConfigured:
          type: boolean
          description: Boolean server-side credential readiness only; never the secret.
        callbackUrl:
          type: string
          format: uri
        contactScope:
          type: string
        lastValidatedAt:
          type: string
          format: date-time
          nullable: true
        lastSyncAt:
          type: string
          format: date-time
          nullable: true
        lastSyncResult:
          allOf:
            - $ref: "#/components/schemas/DirectorySyncResult"
          nullable: true

    DirectorySyncResult:
      type: object
      required:
        [syncId, providerId, startedAt, completedAt, status, departmentsAdded,
         departmentsUpdated, employeesAdded, employeesUpdated,
         employeesOffboarded, conflictsDetected]
      additionalProperties: false
      properties:
        syncId:
          type: string
        providerId:
          type: string
        startedAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
          nullable: true
        status:
          type: string
          enum: [pending, running, success, partial, failed]
        departmentsAdded:
          type: integer
          minimum: 0
        departmentsUpdated:
          type: integer
          minimum: 0
        employeesAdded:
          type: integer
          minimum: 0
        employeesUpdated:
          type: integer
          minimum: 0
        employeesOffboarded:
          type: integer
          minimum: 0
        conflictsDetected:
          type: integer
          minimum: 0

    DirectorySyncHistoryEntry:
      type: object
      required: [syncId, providerId, startedAt, completedAt, status, summary]
      additionalProperties: false
      properties:
        syncId:
          type: string
        providerId:
          type: string
        startedAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
          nullable: true
        status:
          type: string
          enum: [pending, running, success, partial, failed]
        summary:
          type: string

    ProviderSyncConflict:
      type: object
      required:
        [conflictId, providerId, externalSubject, externalName, externalEmail,
         matchedUserId, matchedUserName, matchReason, status, detectedAt]
      additionalProperties: false
      properties:
        conflictId:
          type: string
        providerId:
          type: string
        externalSubject:
          type: string
        externalName:
          type: string
        externalEmail:
          type: string
        matchedUserId:
          type: string
          nullable: true
          description: Search suggestion only until an administrator explicitly resolves.
        matchedUserName:
          type: string
          nullable: true
        matchReason:
          type: string
          enum: [email, name, manual]
        status:
          type: string
          enum: [pending, resolved, ignored]
        detectedAt:
          type: string
          format: date-time

    ResolveProviderConflictRequest:
      type: object
      required: [userId]
      additionalProperties: false
      properties:
        userId:
          type: string
          description: Explicit existing stable United Pass user ID.

    PolicyClause:
      type: object
      required: [attribute, operator, value]
      additionalProperties: false
      properties:
        attribute:
          type: string
          pattern: "^[a-z][A-Za-z0-9_]{0,63}$"
        operator:
          type: string
          enum: [eq, neq, in, not_in, gt, lt, contains]
        value:
          type: string
          maxLength: 256

    PolicyDraftRequest:
      type: object
      required: [name, description, resource, action, effect, principals, conditions]
      additionalProperties: false
      properties:
        name: {type: string, minLength: 1, maxLength: 120}
        description: {type: string, maxLength: 1000}
        resource: {type: string, minLength: 1, maxLength: 128, example: "application:*"}
        action: {type: string, minLength: 1, maxLength: 128, example: application.manage}
        effect: {type: string, enum: [allow, deny]}
        principals:
          type: array
          maxItems: 20
          items: {$ref: "#/components/schemas/PolicyClause"}
        conditions:
          type: array
          maxItems: 20
          items: {$ref: "#/components/schemas/PolicyClause"}

    PolicyUpdateRequest:
      type: object
      required: [expectedVersion, name, description, resource, action, effect, principals, conditions]
      additionalProperties: false
      properties:
        expectedVersion: {type: integer, minimum: 1}
        name: {type: string, minLength: 1, maxLength: 120}
        description: {type: string, maxLength: 1000}
        resource: {type: string, minLength: 1, maxLength: 128}
        action: {type: string, minLength: 1, maxLength: 128}
        effect: {type: string, enum: [allow, deny]}
        principals:
          type: array
          maxItems: 20
          items: {$ref: "#/components/schemas/PolicyClause"}
        conditions:
          type: array
          maxItems: 20
          items: {$ref: "#/components/schemas/PolicyClause"}

    PolicyMutationResult:
      type: object
      required: [policyId, version]
      additionalProperties: false
      properties:
        policyId: {type: string}
        version: {type: integer, minimum: 1}

    PolicySummary:
      type: object
      required: [policyId, name, resource, version, status, updatedBy, updatedAt]
      additionalProperties: false
      properties:
        policyId: {type: string}
        name: {type: string}
        resource: {type: string}
        version: {type: integer, minimum: 1}
        status: {type: string, enum: [draft, published]}
        updatedBy: {type: string}
        updatedAt: {type: string, format: date-time}

    PolicyVersionSummary:
      type: object
      required: [version, status, updatedBy, updatedAt, changeSummary]
      additionalProperties: false
      properties:
        version: {type: integer, minimum: 1}
        status: {type: string, enum: [draft, published]}
        updatedBy: {type: string}
        updatedAt: {type: string, format: date-time}
        changeSummary: {type: string}

    PolicyDetail:
      type: object
      required: [policyId, name, description, resource, action, effect, version, status, principals, conditions, updatedBy, updatedAt, versionHistory]
      additionalProperties: false
      properties:
        policyId: {type: string}
        name: {type: string}
        description: {type: string}
        resource: {type: string}
        action: {type: string}
        effect: {type: string, enum: [allow, deny]}
        version: {type: integer, minimum: 1}
        status: {type: string, enum: [draft, published]}
        principals:
          type: array
          items: {$ref: "#/components/schemas/PolicyClause"}
        conditions:
          type: array
          items: {$ref: "#/components/schemas/PolicyClause"}
        updatedBy: {type: string}
        updatedAt: {type: string, format: date-time}
        versionHistory:
          type: array
          items: {$ref: "#/components/schemas/PolicyVersionSummary"}

    PolicyPage:
      type: object
      required: [items, page]
      additionalProperties: false
      properties:
        items:
          type: array
          items: {$ref: "#/components/schemas/PolicySummary"}
        page: {$ref: "#/components/schemas/CursorPageMetadata"}

    PolicySimulationRequest:
      type: object
      required: [principalAttributes, resourceAttributes, action]
      additionalProperties: false
      properties:
        principalAttributes:
          type: object
          maxProperties: 30
          additionalProperties: {type: string, maxLength: 256}
        resourceAttributes:
          type: object
          maxProperties: 30
          additionalProperties: {type: string, maxLength: 256}
        action: {type: string, maxLength: 128}

    PolicySimulationResult:
      type: object
      required: [decision, matchedPolicyId, matchedPolicyName, evaluatedAt, reasons]
      additionalProperties: false
      properties:
        decision: {type: string, enum: [allow, deny, no_match]}
        matchedPolicyId: {type: [string, "null"]}
        matchedPolicyName: {type: [string, "null"]}
        evaluatedAt: {type: string, format: date-time}
        reasons:
          type: array
          items: {type: string}

    AuditQuery:
      type: object
      additionalProperties: false
      properties:
        cursor: {type: string}
        limit: {type: integer, minimum: 1, maximum: 100}
        query: {type: string, maxLength: 200}
        eventType: {type: string, maxLength: 120}
        result: {type: string, enum: [success, denied]}
        actorName: {type: string, maxLength: 120}
        requestId: {type: string, maxLength: 200}
        from: {type: string, format: date-time}
        to: {type: string, format: date-time}

    AuditEvent:
      type: object
      required: [eventId, eventType, actorName, actorId, targetLabel, targetId, occurredAt, result, requestId, details]
      additionalProperties: false
      properties:
        eventId: {type: string}
        eventType: {type: string}
        actorName: {type: string}
        actorId: {type: string}
        targetLabel: {type: string}
        targetId: {type: string}
        occurredAt: {type: string, format: date-time}
        result: {type: string, enum: [success, denied]}
        requestId: {type: string}
        details: {type: string, description: Fixed safe operation label; never raw payload or provider error.}

    AuditEventPage:
      type: object
      required: [items, page]
      additionalProperties: false
      properties:
        items:
          type: array
          items: {$ref: "#/components/schemas/AuditEvent"}
        page: {$ref: "#/components/schemas/CursorPageMetadata"}

    AuditExport:
      type: object
      required: [exportId, status, downloadUrl, requestedAt, completedAt, totalEvents]
      additionalProperties: false
      properties:
        exportId: {type: string}
        status: {type: string, enum: [pending, processing, completed, failed]}
        downloadUrl: {type: [string, "null"]}
        requestedAt: {type: string, format: date-time}
        completedAt: {type: [string, "null"], format: date-time}
        totalEvents: {type: integer, minimum: 0}

    PersonalDataExport:
      type: object
      required: [exportId, status, requestedAt, completedAt, expiresAt, downloadUrl, totalSections]
      additionalProperties: false
      properties:
        exportId: {type: string, pattern: "^pexp_[A-Za-z0-9_-]{13,74}$"}
        status: {type: string, enum: [pending, processing, completed, failed]}
        requestedAt: {type: string, format: date-time}
        completedAt: {type: [string, "null"], format: date-time}
        expiresAt: {type: [string, "null"], format: date-time}
        downloadUrl: {type: [string, "null"]}
        totalSections: {type: integer, minimum: 0}

    AccountDeletion:
      type: object
      required: [deletionId, status, requestedAt, executeAfter, cancelledAt, completedAt]
      additionalProperties: false
      properties:
        deletionId: {type: string, pattern: "^del_[A-Za-z0-9_-]{13,75}$"}
        status: {type: string, enum: [pending, processing, provider_deleted, completed, cancelled, failed]}
        requestedAt: {type: string, format: date-time}
        executeAfter: {type: string, format: date-time}
        cancelledAt: {type: [string, "null"], format: date-time}
        completedAt: {type: [string, "null"], format: date-time}

    AccountDeletionState:
      oneOf:
        - type: object
          required: [status]
          additionalProperties: false
          properties:
            status: {type: string, const: none}
        - $ref: "#/components/schemas/AccountDeletion"

    PublicLegalPublication:
      type: object
      required: [documentKind, version, contentSha256, effectiveAt, publishedAt, status]
      additionalProperties: false
      properties:
        documentKind: {type: string, enum: [privacy, terms]}
        version: {type: string}
        contentSha256: {type: string, pattern: "^[0-9a-f]{64}$"}
        effectiveAt: {type: string, format: date-time}
        publishedAt: {type: string, format: date-time}
        status: {type: string, enum: [scheduled, effective]}

    LegalPublicationList:
      type: object
      required: [items]
      additionalProperties: false
      properties:
        items:
          type: array
          maxItems: 2
          items: {$ref: "#/components/schemas/PublicLegalPublication"}
