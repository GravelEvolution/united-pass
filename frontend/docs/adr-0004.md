# ADR-0004: API 客户端、浏览器会话与错误处理边界

- Status: Proposed
- Date: 2026-08-05
- Owners: United Pass frontend team

## Context

United Pass 前端目前通过 `UnitedPassDataSource` 接口和 `mockUnitedPassDataSource` 实现提供页面数据。OAuth 应用创建、授权确认状态机和账号中心授权应用页面已完成，Mock 数据结构趋于稳定。

下一步需要替换 Mock 为真实后端 API。`docs/api-contracts.md` 已定义了路径、分页模型和错误响应草案，但在实际接入前需要确定：

1. HTTP 客户端放在哪里、如何组织
2. 浏览器会话如何传递（Cookie、CSRF）
3. HTTP 状态码如何统一归一化为前端可消费的错误类型
4. 后端字段错误如何映射到 Semi Design Form
5. Cursor Pagination 如何映射到 Semi Design Table
6. 重认证 Challenge 如何在 UI 层表示
7. OpenAPI 生成的客户端最终放在哪里

## Decision

### 1. HTTP 客户端架构

在 `src/lib/api/` 下建立以下文件：

```
src/lib/api/
├── united-pass-data-source.ts    # 已有：数据源接口（页面消费的契约）
├── http-client.ts                # 新增：底层 fetch 封装
├── api-error.ts                  # 新增：错误归一化
├── united-pass-api-data-source.ts # 新增：真实 HTTP 实现
└── server-session.ts             # 新增：服务端会话读取
```

`http-client.ts` 职责：
- 封装 `fetch`，统一设置 `credentials: "same-origin"` 和 `Content-Type`
- 从 Cookie 读取 CSRF Token 并写入请求头（`X-CSRF-Token`）
- 解析响应体，处理非 2xx 状态码
- 返回类型化结果，不泄漏 `Response` 对象到上层

`united-pass-api-data-source.ts` 职责：
- 实现 `UnitedPassDataSource` 接口
- 将每个方法映射到对应的 API 路径
- 处理分页参数与响应解包
- 将 `ApiError` 传递给调用方，不在数据源层吞掉错误

页面和 Server Component 只依赖 `UnitedPassDataSource` 接口，不直接导入 `http-client`。

### 2. 浏览器会话

- 会话使用 `Secure; HttpOnly; SameSite=Lax` Cookie，由后端在 `POST /api/v1/auth/sessions` 后设置
- 前端不持有、不存储 Access Token、Refresh Token 或 ID Token
- 所有 API 请求使用 `credentials: "same-origin"`，浏览器自动携带会话 Cookie
- CSRF Token 通过后端下发的非 HttpOnly Cookie 读取，并在写操作时通过 `X-CSRF-Token` 请求头发送
- `server-session.ts` 在 Server Component 中通过 `next/headers` 的 `cookies()` 读取会话状态，用于决定是否重定向到 `/login`

### 3. 错误处理边界

`api-error.ts` 定义统一的错误类型：

```ts
type ApiErrorKind =
  | "network"
  | "unauthorized"       // 401
  | "forbidden"          // 403
  | "not_found"          // 404
  | "conflict"           // 409
  | "validation"         // 422
  | "rate_limited"       // 429
  | "reauthentication_required" // 后端要求重新验证身份
  | "server_error";      // 5xx

type FieldError = {
  field: string;
  message: string;
};

type ApiError = {
  kind: ApiErrorKind;
  message: string;          // 可安全展示给用户
  requestId?: string;       // 后端追踪 ID
  fieldErrors?: FieldError[];
  retryAfter?: number;      // 429 时的重试等待秒数
};
```

映射规则：
- 401 → `unauthorized`：重定向到 `/login`，保留 `redirect` 参数
- 403 → `forbidden`：展示权限不足提示，不重定向
- 404 → `not_found`：展示 notFound() 或空状态
- 409 → `conflict`：展示资源冲突提示（如邮箱已存在）
- 422 → `validation`：将 `fieldErrors` 映射到 Semi Form 的 `validateStatus` 和错误文案
- 429 → `rate_limited`：展示限速提示，读取 `Retry-After` 头
- 后端返回 `code: "session.reauthentication_required"` → `reauthentication_required`：触发重认证流程
- 5xx → `server_error`：展示通用错误页面，不暴露内部信息

### 4. 字段错误映射到 Semi Form

后端 422 响应的 `fieldErrors` 对象按字段名映射到 Semi Design Form：

```tsx
// 后端返回: { "redirectUri": "该重定向地址未登记。" }
// 前端映射:
formApi.setError("redirectUri", {
  text: apiError.fieldErrors?.find(e => e.field === "redirectUri")?.message
});
```

Semi Form 的 `validateStatus` 设为 `"error"`，错误文案显示在字段下方。

对于非字段级错误（如凭据错误、限速），使用 `Toast` 或页面级错误提示。

### 5. Cursor Pagination 映射到 Semi Table

后端使用 Cursor Pagination：

```json
{
  "items": [],
  "page": { "nextCursor": "opaque_cursor_or_null", "hasMore": false }
}
```

前端映射到 Semi Table：
- `dataSource` 使用当前页 `items`
- 分页器使用 `pagination={{ currentPage, pageSize, total: estimatedTotal }}`
- 当 `hasMore` 为 `true` 时，下一页请求携带 `?page[cursor]=nextCursor`
- 搜索、排序变更时重置 cursor，从第一页重新加载
- `server-session.ts` 不参与分页；分页状态由 Client Component 管理

### 6. 重认证 Challenge

后端在需要更强身份验证的操作（如密钥轮换、会话撤销、策略发布）时返回：

```json
{
  "error": {
    "code": "session.reauthentication_required",
    "message": "请重新验证身份后继续。",
    "challenge": {
      "methods": ["password", "totp"],
      "requestId": "reauth_01..."
    }
  }
}
```

前端表示：
- `ApiError.kind` 设为 `reauthentication_required`
- Client Component 收到此错误后打开重认证 Modal
- Modal 展示可用的验证方式（密码、TOTP、Passkey）
- 验证成功后自动重试原始请求
- `challenge.requestId` 用于关联重认证流程与原始操作

### 7. OpenAPI 客户端放置

当后端 OpenAPI 规范可用时：
- 生成的类型和客户端放在 `src/lib/api/generated/`
- `united-pass-api-data-source.ts` 内部调用生成的客户端，但对外仍暴露 `UnitedPassDataSource` 接口
- 页面不直接导入 `generated/` 下的代码
- 手写的传输类型逐步替换为生成类型
- 生成代码不手动编辑；变更时先更新 OpenAPI 规范再重新生成

## Alternatives Considered

- **在页面中直接调用 fetch**：简单直接，但会将 URL 构造、错误处理和 Cookie 逻辑扩散到每个页面，替换成本高
- **使用 axios 或 got 等第三方 HTTP 库**：增加依赖且不提供比 fetch 更多的能力；fetch 已满足需求
- **在 Next.js Route Handler 中实现 BFF 层**：增加了一层间接调用，但当前不需要端到端网络转换或服务端密钥注入
- **使用 React Server Actions 替代 API 层**：适合内部工具，但 OAuth/OIDC 流程需要标准 HTTP 重定向和 Cookie 语义，不适合 Server Actions

## Consequences

- 页面只依赖 `UnitedPassDataSource` 接口，Mock 和真实 API 实现可互换
- 错误处理集中在一处，不会在每个页面重复
- CSRF Token 的读取和注入对上层透明
- 重认证流程有统一的 UI 表示，不会散落在各组件中
- 当 OpenAPI 合同可用时，生成代码隔离在 `generated/` 下，不影响接口边界
- 前端不持有任何 Token，降低了凭据泄露风险
- Cursor Pagination 需要管理 `nextCursor` 状态，比一次性加载复杂

## Implementation Notes

- 数据源接口：`src/lib/api/united-pass-data-source.ts`（已有）
- Mock 实现：`src/lib/mock/united-pass-data-source.ts`（已有）
- HTTP 客户端：`src/lib/api/http-client.ts`（待实现）
- 错误类型：`src/lib/api/api-error.ts`（待实现）
- 真实 API 实现：`src/lib/api/united-pass-api-data-source.ts`（待实现）
- 服务端会话：`src/lib/api/server-session.ts`（待实现）
- API 草案：`docs/api-contracts.md`（已有）
- Semi Form 字段映射参考：`src/features/account/components/contact-verification-modal.tsx`
- Semi Table 分页参考：`src/features/admin/components/management-directory.tsx`

## Follow-up

- 与后端确认 OpenAPI 规范的发布方式和更新频率
- 确认 CSRF Token 的 Cookie 名称和请求头名称
- 确认重认证 Challenge 的完整协议（可用方式、过期时间、最大尝试次数）
- 实现 `http-client.ts` 和 `api-error.ts` 后添加单元测试
- 接入真实 API 后移除 Mock 中的 `getConsentRequest` 方法（仅 `getConsentResolution` 需要保留）
- 评估是否需要 Server Component 层面的请求去重（相同请求在多个组件间复用）
